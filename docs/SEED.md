# بذرةُ المنصة في «اِحْسِبْ» — جردُ ما نُسخ من «اِقْرَأْ» وما عُدِّل فيه

> **الجلسة ٠ (التأسيس)** · ١١ أغسطس ٢٠٢٦ · المصدر: مستودع `read` عند الالتزام
> **`ea07f350`** (٨ أغسطس ٢٠٢٦) — و**لم يُمَسّ منه ملفٌّ واحد** (يشهد `git status`).
>
> علّةُ هذه الوثيقة من `FAMILY.md §٤`: «البذرة تُنسخ ولا تُستورد … ويُقيَّد في
> `docs/SEED.md` **ما نُسخ ومن أيّ التزام** — فالاستخلاصُ اللاحق يعرف ما تفرّع».
> ولا مكتبةَ مشتركة الآن: الدراسة تنصّ على استخلاص المحرّك **بعد نضوج تطبيقين على
> الأقل**، وتَعُدّ التجريد المبكر خطراً. فهذا الجردُ هو ذاكرةُ التفرّع حتى ذلك اليوم.

## ١. كيف يُقرأ هذا الجرد

| العمود | معناه |
|---|---|
| **الملف** | موضعُه في `calc` |
| **البصمة** | التزامُ `read` الذي **آخِرُ من لمس الملفَّ المصدر** — لا رأسُ الفرع (فالرأسُ يتحرّك والبصمةُ تدلّ على النسخة بعينها) |
| **الحال** | 🟰 منسوخٌ حرفياً · ✂️ منسوخٌ ومجرَّد · 🏗️ بنيتُه منه ومادّتُه لنا · 🌱 جديدٌ لهذا التطبيق · ⏸️ مؤجَّلٌ بسببه (§٤) |

> **وما عدّلته الجلساتُ في وحدات البذرة بعد نسخها** مقيَّدٌ في §١٠ — فالجردُ يبقى
> صادقاً، ويُعرَف يومَ الاستخلاص ما كان في النسخة وما زادته الرحلةُ عليها.

**وثلاثةُ أنواعٍ من التعديل لا رابعَ لها**، وكلُّ سطرٍ أدناه واحدٌ منها:
1. **تجريدُ مادّة**: حذفُ ما يعرف حرفاً أو سورةً أو بستاناً — والهيكلُ يبقى.
2. **تكييفُ هوية**: العلامة، اللوح اللوني، الأيقونات، أسماءُ المخازن والمفاتيح.
3. **قلبُ الاعتماد**: ما كان يعرف المنهجَ بنفسه صار يقرؤه من `curriculum.js` — وهو
   أعمقُ الثلاثة أثراً، وموضعُه `progress.js` و`main.js` و`review.js` (§٣).

## ٢. `app/` — وحدات التطبيق

| الملف | البصمة | الحال | ما عُدِّل، ولماذا |
|---|---|---|---|
| `js/audio.js` | `41f8445` | 🟰 | **بلا حرفٍ واحد**. الوحدةُ محايدةٌ للمادة تماماً: sha1 خالص، فهرسٌ وبصماتُ محتوى، احتياطُ نطقٍ آليّ مضبوط، وإطلاقُ موارد العنصر بعد فراغه (بلاغُ صمت الآيباد). كلُّ دروسها دروسُ منصةٍ لا دروسُ قراءة. |
| `js/ui.js` | `8c034c2` | ✂️ | **تجريدُ مادّة**: سقط `letterTitle` و`wordText` و`nodeTitle`/`nodeWhere`/`nodeFace` بأنواع اقرأ، و`coverEl` (غلافُ القصة)، و`inkLift`/`giantInk` — رفعةُ حبر الحرف العربيّ في صندوقه، ولا حرفَ بطلاً هنا. **وتكييفُ هوية**: العلامة `اِحْسِبْ`، والمرشدُ بومةٌ لا عصفوراً، وألوانُ المراحل الأربعة الجديدة، ومعالمُ محطاتٍ حسابية (نقاط · عدّ · إطارُ عشرة · ميزان · جسر · آلة · نمط · بوابة) بدل (بيت · جسر · قبة · بستان · سلّم · كتاب). **وزيادةٌ لهذا التطبيق**: `latinNum` (لمحطة التعارف ٨·٥ وحدَها، ق١) و`seeded` (مولّدٌ حتميّ ببذرة — `METHOD.md §١٠.١`). وبقي محايداً كما هو: `h` · `toast` · `arNum` · `arCount` · `faceEl` · منظومةُ Twemoji كاملةً · `shuffle`/`pick` · `shake`/`pop`. |
| `js/progress.js` | `6cd3895` | ✂️🏗️ | **قلبُ الاعتماد**: كانت `journey()` تعرف المجموعاتِ والبساتينَ والسورَ فتؤلّف منها الأقسام (٣٠٠ سطر)؛ وصارت تقرأ `sections()` من `curriculum.js` وكفى. **وتبدّلُ مفتاح القياس**: `(حرف × حركة × تمرين)` ← **`(مفهوم × مدى × تمرين)`** (`METHOD.md §٦`)، و`letterStats` ← `conceptStats`. **وما سقط بعلّته**: عدّادُ خفوت التشكيل، وسجلُّ «اقرأ لي»، ومخزنُ تسجيلات الصوت، و`audioStored`، وترحيلاتُ رحلة اقرأ (`SPLIT_NODES` وأخواتها) — لا رحلةَ سابقة تُرحَّل، والنسخةُ ١ أوّلُها. **وبقيت كما هي بدروسها**: الجبهةُ الكسولة (`unlockFrontier`) والقفلُ التسلسليّ، وصناديقُ ليتنر بمواعيدها، ووضعُ المعاينة، والنسخةُ الاحتياطية بترويستها ورفضِها المعلَن، وتحكّمُ وليّ الأمر (`unlockUpTo` · `clearSection`)، وصلابةُ التخزين. ومفتاحُ التخزين `ihsib.progress.v1` و`BACKUP_KIND = 'ihsib.progress'` — **أصلٌ مستقلّ لا يخلط تقدّمين**. |
| `js/review.js` | `77a14b3` | 🏗️ | **هيكلُه لا تمارينُه** (نصُّ `FAMILY.md §٥`). كان الملفُّ شيئين مختلطين: محرّكَ جلسةٍ محايداً وثمانيةَ تمارينَ تعرف الحرفَ والحركة. فبقي **الأول وحدَه** (نقاطُ التقدّم، التسلسل، تسجيلُ المحاولة، الختام، الإعادةُ ببناءٍ جديد)، وصار الثاني **يُحقَن**: `setBuilders({item, fillers, view, longs})` تناديها وحدةُ التمارين (الجلسات ٣+). وبقيت **خوارزميةُ الاختيار** كما هي (الأضعفُ أولاً ثم تنويعٌ من حوضٍ **مخلوطٍ كلِّه** — درسُ الرتابة في المراجعة الخارجية). و`starsForReview` بعتبتها المتناسبة مع طول النشاط. |
| `js/gate.js` | `debfd77` | ✂️ | **نموذجُه مُقَرٌّ حرفياً** (`METHOD.md §٥`): `weakestSkills()` لا `dueSkills()`، وعشرةُ تمارين، و≥٨٠٪، ولا رسوب، وإعادةٌ تبني تمارينها من جديد. والمبدَّل: مادّتُه من `sessionItems` بدل `buildSession` بحصيلة الحروف، و«قَوِّ حُرُوفَكْ» ← «قَوِّ حِسَابَكْ». **والبوابات ثلاثٌ لا اثنتان**. |
| `js/parent.js` | `401be9b` | ✂️🏗️ | **البنيةُ والبوابةُ الحسابية** (نصُّ `FAMILY.md §٥`): البوابةُ بعملية ضربها ورفضِها المُعاد، وطلبُ التخزين الدائم عندها، وتوصيةُ اليوم بترتيبها المقصود (صحّةُ الطفل قبل التحصيل، والمراجعةُ قبل الجديد)، و`askThen` (تأكيدٌ في الصفحة لا `confirm()`)، وقسمُ النسخة الاحتياطية، وتحكّمُ الرحلة، وبابُ المعاينة. **وسقط**: لوحةُ الحروف والعلامات والجذور، وقسمُ الخفوت، وقسمُ التسجيلات ومنحنى الطلاقة، وشريطُ تحميل الصوت (يعود مع بنك الصوت — §٤). **وحلّ محلَّها** قسمُ **مفاهيم الحساب السبعة** (كمّ · عدّ · رموز · مقارنة · جسور · عمليات · أنماط) بوصلته إلى `conceptStats()` — **وترجمتُه إلى عبارةٍ بندُ الجلسة ٨**، فلا رقمَ كاذبٌ اليوم. وسقفُ اليوم ١٥ دقيقة لا ٢٠ (جمهورُنا من الرابعة). وحدُّ النطاق «راجِع مختصاً» منصوصٌ في اللوحة (`METHOD.md §١٣`). |
| `js/main.js` | `0a1669b` | 🏗️ | **أعمقُ الملفّات تجريداً**. خريطةُ اقرأ كانت تعرف أنواعَ محطاتها فلكلٍّ دالّةُ رسمٍ خاصّة (تسعُ دوالّ)، وموجِّهُها يعرف أسماءَ شاشاته واحدةً واحدة (اثنتا عشرة فرعاً). وهنا: **القسمُ يصف نفسَه** في `curriculum.js` (عنوانٌ ووجهٌ ومعلمٌ ولون) فترسمه `stationEl` واحدةٌ من وصفه، و**الموجِّهُ يفتح الشاشةَ من سِجلٍّ** تسجّل فيه كلُّ وحدةِ تمارينٍ نفسَها (`registerScreen`). فمحطةٌ جديدة تدخل الرحلة **بلا سطرٍ يُعدَّل هنا**. وبقي كما هو: الطيُّ الكسول بجبهته، والدُّرجُ يُفتح ويُغلَق بعلامته، وخيطُ الدرب المستنسَخ، ومعلمُ الإعادة، و«المقفلُ يُجيب» بهزّةٍ ورسالة، وساعةُ الاستعمال، وتسجيلُ عامل الخدمة، وشريطُ المعاينة. **وزيادةٌ لهذه الجلسة**: رسالةُ «الرحلة لم تُكتب بعد» — خريطةٌ فارغةٌ صامتة تُقرأ عطباً، وتسقط الرسالةُ من تلقائها يومَ تُكتب أوّلُ محطة. |
| `js/render.js` | — | 🌱 | **الجلسة ٢** — **مصيِّر الكميات**، ولا نظيرَ له في اقرأ البتّة: هناك بطلُ الشاشة **حرفٌ يرسمه خطّ**، وهنا **كميةٌ ترسمها هندسة**. شطران: `plan()` هندسةٌ **خالصة بلا DOM** (تُقرأ في node وتُجرَّب سالباً)، و`paint()` يبني الشكل **ويَعُدّ ما رسم من DOM نفسِه** (`[data-mark]`) لا من العدد المطلوب. وستةُ رسّامين من معجم المنهج العشرة: النرد ١–٦ · المبعثر · عناصر عالم الطفل · إطارا الخمسة والعشرة · الإطاران. وفيه **لوحُ المراجعة البصرية** (`?dev=1`) الذي تُلتقط منه اللقطات المرجعية ويقيس عليه حارسُ المتصفّح. |
| `emoji/` (٨ ملفات + فهرس) | — | 🌱 | **الجلسة ٢** — عناصرُ عالم الطفل من Twemoji 15.1 (CC-BY 4.0)، جلبها `fetch_twemoji.py` **بجرد المستعمَل وحدَه** من `render.js` نفسِه: تفّاحة · نجمة · سمكة · بالون · زهرة · فراشة · سيّارة · كعكة (١١ كيلوبايت). ويخزّنها عاملُ الخدمة **من فهرسها** لا من قائمةٍ يدوية. |
| `js/registry.js` | — | 🌱 | **الجلسة ٣** — **سِجلُّ الشاشات**، نُقل من `main.js` إلى وحدةٍ محايدة. وعلّتُه في رأسه: وحدةُ التمارين لا تستطيع أن تستورد `main.js` (**دَورٌ في الاستيراد** يُسقِط التطبيق عند أول تحميل، و**عقدُ المولّد النقيّ** في `check_range.py` يمنعه نصّاً) — فلو بقي السجلُّ هناك لَما أمكن أن «تسجّل الشاشةُ نفسَها» أصلاً. |
| `js/station.js` | — | 🌱 | **الجلسة ٣** — **حلقةُ المحطة** (`METHOD.md §٤`): شاهِدْ ← جَرِّبْ مَعِي ← وَحْدَكْ ← كافِئْ، ومعها معالجةُ الخطأ الموحَّدة (`countAloud` — تُعَدّ الكميةُ أمامه عنصراً عنصراً)، وطبقةُ اللمس (`touchLayer` — زرٌّ حقيقيّ فوق كل عنصرٍ مرسوم بموضعه من المصيِّر نفسِه)، وأسماءُ الأعداد ١–١٠ **مُعلَنةً** (`SPOKEN`)، ونجومُ المحطة بعتبتها المتناسبة. **وفيه يقع حقنُ المراجعة مرّةً واحدة**: `setBuilders` تُحقَن **موزِّعاً** يقرأ سجلَّ التمارين، وتسجّل فيه كلُّ وحدةٍ أنواعَها — فوحدةُ الجلسة ٤ تدخل المراجعةَ بلا سطرٍ يُعدَّل هنا (ولو نادتها كلُّ وحدةٍ لَمحت الثانيةُ الأولى). |
| `js/quantity.js` | — | 🌱 | **الجلسة ٣** — المرحلة ١ بثلاث شاشات: `subitize` («كم ترى؟» بالخطف) · `match` («طابِقِ الكميتين» بتوزيعين مختلفين) · `more` («أيهما أكثر» ومعها «سواء» في الختامية). تُعلن `CONSUMES` و`probeRounds` فيستيقظ بابا `check_range` ٤+٥. |
| `js/counting.js` | — | 🌱 | **الجلسة ٣** — المرحلة ٢ بشاشتين: `count` («المس وعُدّ» + العدديّة + «أعطني هذا العدد» + البعثرة التي لا تغيّر العدد) · `equal` («اجعلهما سواء» بكلمتَي أَضِفْ/أَزِلْ **لا بعلامتَي + و−**: المرحلة ٢ بلا عملياتٍ أصلاً). |
| `js/numerals.js` | — | 🌱 | **الجلسة ٤** — المرحلة ٣ بشاشةٍ واحدة (`numeral`) لثماني محطات: **حلقةُ تقديم الرمز** (كميةٌ تُعَدّ ← يُكشَف رمزُها فوقها ← مطابقةٌ **ذهاباً وإياباً**)، ومفتاحُ ليتنر **لكلِّ رمزٍ على حدة** مقروءاً من `skills` في المنهج (`rangesOf`)، والصفرُ **إطاراً فارغاً** في محطته. |
| `js/compare.js` | — | 🌱 | **الجلسة ٤** — المرحلة ٤ بأربع شاشات في ملفٍّ واحد (قاعدةُ «كلُّ نوع شاشةٍ في جلسةٍ واحدة»): `compare` بالرمزين والتحقّقُ بالكميات **صفّين متحاذيين** (تعديلُ `METHOD.md §٤`) · `line` بثلاث خاناتٍ متجاورة والتصحيحُ **عدّاً على الخطّ** · `order` بصفٍّ يمتلئ من اليمين · `neighbor` بأوجهه الثلاثة (سابقٌ وتالٍ ويساوي). |
| `js/curriculum.js` | — | 🌱 | **لا يُنسخ** (نصُّ `FAMILY.md §٥`). وهو هنا **بذرةٌ فارغة تُعلن عقدها**: `STAGES` و`GATES` و`sections()` وصفراً من كلٍّ. علّةُ وجوده فارغاً لا محذوفاً: الغيابُ التامّ يُسقِط التطبيقَ عند أول استيراد فلا تُفتَح شاشةُ الخريطة الفارغة التي هي معيارُ قبول هذه الجلسة. **صفرُ مرحلةٍ وصفرُ محطةٍ وصفرُ عدد** — والجلسة ١ تملؤه. **وقد ملأتها الجلسةُ ١** (المراحل الثماني · ٤٩ محطة · ٣ بوابات · ٥٢ عقدة)، **ولم يُعدَّل سطرٌ واحد في وحدةٍ أخرى** — فصحّ قلبُ الاعتماد عملاً لا دعوى. |
| `sw.js` | `41f8445` | ✂️ | منطقُ v20 كلُّه بدروسه: مخزنُ صوتٍ **ثابتُ الاسم** يعبر النسخ (لا يعيد التحديثُ تنزيل البنك)، وجلبٌ مدفَّعٌ (١٦) **معدودُ الإخفاق لا مبتلعه**، و«لا كنسَ إن أخفق شيء»، وشفاءٌ عند أول اتصال بمهلته وقيوده الثلاثة، وإبلاغُ النوافذ بعد كل دفعة، واستراتيجيتا الخزن، ووسمُ المحتوى مع كنسِ الأقدم، وخروجُ `welcome/` من القشرة. **والمبدَّل**: `muallim-*` ← `ihsib-*`، وقائمةُ SHELL لملفاتنا، وسقط `precacheStories` (لا مكتبةَ قصص) و`adoptLegacyAudio` (لا مخزنَ قديم يُتبنّى — أوّلُ نسخةٍ لنا). |
| `css/app.css` | `401be9b` | 🏗️ | **البنيةُ منه واللوحُ لنا** (`SESSIONS.md §٠.٣`: «لا نسخ لوح اقرأ بألوانه»). مفرداتُ التنسيق بمقاييسها ودروسها (`station` بطيّه · `track` بتعرّجه · `node` بحالاته الأربع · `dots` · `celebrate` · `vchip` · `gate` · `preview-bar` · `toast` · منظومةُ الأيقونات). **واللوحُ جديدٌ كلُّه**: أزرقٌ هادئ ← فيروزيّ ← مرجانيّ ← بنفسجيّ ناعم ← عنبريّ، والفعلُ الرئيس **فيروزيّ لا ذهبيّ** — فيعرف الطفلُ في نظرةٍ أنّه في تطبيقٍ آخر. ولونٌ لكل **عائلةِ مراحل** لا لكل مرحلة، فالتدرّجُ اللونيّ يحكي تدرّجَ المنهج. **و`--tap: 3.75rem` (٦٤بك) و`--node-size: 6rem`** — أعلى من اقرأ لأنّ جمهورَنا من الرابعة (`METHOD.md §١١`). وسقط تنسيقُ مادّته (أشكالُ الحرف · لوحُ التتبّع · صفحةُ القصة · وقارُ المصحف · بطاقاتُ التسجيل · الرفّ). |
| `index.html` | `297d9f7` | ✂️ | الهيكلُ نفسُه (RTL · `viewport-fit=cover` · وسمُ آبل · `noscript`)، والمبدَّل العنوانُ والوصفُ ولونُ الواجهة. |
| `manifest.webmanifest` | `297d9f7` | ✂️ | الشكلُ نفسُه بمسارِه النسبيّ (`./`) — والمبدَّل الاسمُ والوصفُ واللونان. |
| `fonts/` (٥ ملفات) | `e71e0e0` · `297d9f7` | 🟰 | Noto Naskh Arabic ×٢ · Baloo Bhaijaan 2 ×٢ · Marhey. **ولم يُنسخ `KFGQPCUthmanicHafs.woff2`**: خطُّ المصحف لمادّةٍ قرآنية لا وجودَ لها هنا — ٩٢ كيلوبايت لا يحملها جهازُ طفلٍ بلا سبب. |
| `icons/` (٤ ملفات) | — | 🌱 | مولَّدةٌ بـ`tools/make_icons.py` من `tools/icon.html` — لا أصلَ ثنائيّ يُحرَّر يدوياً. |

## ٣. `tools/` — العدّة والحرّاس

| الملف | البصمة | الحال | ما عُدِّل، ولماذا |
|---|---|---|---|
| `test_selftests.mjs` | `f4dbf1c` | 🟰 | **بلا حرفٍ واحد** — وهو أحقُّ ما يُنسخ حرفياً: يجرد `tools/` ويشغّل كلَّ ما يعلن `--self-test`، فلا يعرف مادّةً ولا يحتاج سطراً عند كل أداةٍ جديدة. «فحصٌ لا يُشغَّل ليس حارساً». |
| `test_pwa.mjs` | `41f8445` | ✂️ | فحوصُ القشرة والبيان والأيقونات وأحكامِ المخزون كما هي (وهي دروسُ اقرأ التي دُفع ثمنُها). **وسقط** ما يخصّ مادّتَه: بيانُ التلاوة، وفهرسُ القصص. **وأُضيف** فحصُ «الصفحةُ عربيةٌ من اليمين». **وما يفترض بنكَ صوتٍ نائمٌ يستيقظ ذاتياً** (§٥). |
| `test_nodes.mjs` | `6cd3895` | 🏗️⏸️ | علّتُه ودرسُه منه حرفاً بحرف («معرّفان لعقدةٍ واحدة ⇒ جبهةٌ تتجمّد فلا يُفتَح بعدها شيء»). وبُنيَ على **درجتين تنامان وتستيقظان ذاتياً** (§٥)، وأُضيفت فحوصُ بنيةٍ لم تكن في اقرأ: لكل عقدةٍ نوعٌ وجزءٌ وقسمٌ معلوم. |
| `test_measure.mjs` | `77a14b3` | 🏗️⏸️ | العقدُ نفسُه بأبوابه الأربعة (الإعلان · الشيفرة · المراجعة · لوحة وليّ الأمر). و**جدولُ `STATIONS` فارغٌ إلا من البوابة** — تملؤه الجلسة ١ على الرحلة التي تكتبها (بندُها ٣). وفيه سلفاً إعفاءُ البوابة مكتوباً، ومنصوصٌ في تعليقه إعفاءُ **محطة التعارف ٨·٥** (ق١). ينام ويستيقظ ذاتياً (§٥). |
| `browser_test.py` | `c7ae9cf` | 🏗️ | بنيةُ الخادم والمشغّل منه (خادمٌ يخدم `app/` ويضيف صفحاتِ الاختبار من `tools/`، والنتيجةُ بـ`POST /result`، و`/__queue.json` لاستثناء نصوص قائمة الانتظار). **وسقطت** صفحاتُه الثلاثون (مادّةُ اقرأ). **وأُضيف `--self-test`** لم يكن فيه. **وعُدِّل عيبٌ مقيسٌ**: مِلفُّ Chrome كان واحداً لكل المقاسات فلا تصل نتيجةٌ بعد الأول (المقتولُ يترك قفلَه لحظاتٍ) — فصار لكل مقاسٍ مِلفُّه، وصار المنظورُ الفعليّ يُقابَل بالمطلوب فيُكشَف اختلالُ `VIEWPORT_PAD`. |
| `browser_test.html` | — | 🌱 | صفحةُ فحصٍ جديدة لهذا التطبيق: تفتح البذرةَ وتفحص خمسةَ عشرَ أمراً (§٦). **وزادت الجلسةُ ٢ فيها ثمانيةً**: حارسُ المصيِّر **على الشاشة نفسِها** — لا صفحةَ ثانية (نصُّ رأسها: «الجلساتُ التالية تضيف فحوصَ شاشاتها هنا»). |
| `browser_device.html` | — | 🌱 | مقاساتُ الآيباد الخمسة — **لا فائض أفقيّ** (`METHOD.md §١٠.٧`). |
| `make_icons.py` | `297d9f7` | ✂️ | المنطقُ نفسُه (لقطةٌ بمقاس ٥١٢ ثم تصغيرٌ بـ`sips`، ومهلةٌ افتراضية تنتظر وصولَ خطّ العلامة). **والمبدَّل** لونُ الخلفية ووصفُ الأداة واسمُ المِلفّ المؤقّت. **وأُضيف `--self-test`** لم يكن فيه: يقابل **العلامةَ في الأصل بالعلامة في `ui.js`**، وكلَّ أيقونةٍ يَعِد بها البيانُ بما يولّده هذا الملف. |
| `icon.html` | `297d9f7` | ✂️ | البنيةُ والقياسُ (رفعةُ الحبر المقيسة) حرفاً بحرف — والمبدَّل الكلمةُ وتدرّجُ اللون، والنسبةُ ٠٫٣٤ لا ٠٫٣٧ («اِحْسِبْ» ستةُ حروفٍ بسنٍّ نازلة فتحتاج هواءً أوسع). |
| `fetch_twemoji.py` | `2345ecb` | 🟰 | **بلا حرفٍ واحد** — أداةُ أصولٍ محايدة تماماً (تجرد المستعمَل وحدَه فتجلبه). **واستعملتها الجلسة ٢** لعناصر عالم الطفل التي تُعَدّ: جردت ثمانيةَ رموزٍ من `render.js` فنزّلتها وكتبت الفهرس — **بلا حرفٍ يُعدَّل فيها**، ومصادرُها الخاصّةُ باقرأ (معجمُ الحديقة، مكتبةُ القصص) مسالكُ لا وجودَ لها هنا فتُتخطّى. |
| `check_render.mjs` | — | 🌱 | **الجلسة ٢** — **حارسُ المصيِّر** (الحارس ٢ في `METHOD.md §١٠.٢`)، ولا نظيرَ له في اقرأ. يقرأ **الهندسة** أعداداً في node لكل نمطٍ وكل عددٍ وست بذور: العددُ المرسوم هو المقصود · لا خروجَ عن الصندوق · لا تراكبَ ولا تلاصقَ دون الفاصل · الإطارُ يُملأ من اليمين · وجهُ النرد قياسيّ **بجدولٍ مكتوبٍ فيه مستقلاً** (مقابلةُ نسخةٍ بنسختها لا تُثبت شيئاً) · الحتميةُ ببذرة والتنويعُ بها · صدقُ الصورة · والمعجمُ يملكه المنهج. و`--self-test` **يُجرَّب سالباً** بخططٍ حيّةٍ مكسورة. **وزادت الجلسةُ ٣ فيه `spotStyle`** — موضعُ العنصر نسبةً من الصندوق، أُخرج من `paintObjects` ليقتسمه الرسمُ وطبقةُ اللمس: ولو حسبت الشاشةُ مواضعَ أهدافِ اللمس بنفسها لصار للعنصر موضعان يفترقان يومَ يتحرّك أحدهما، فيلمس الطفلُ فراغاً. |
| `check_speech.mjs` | — | 🌱 | **الجلسة ٣** — **حارسُ النصّ المنطوق**. علّتُه أنّ فصلَ الصوت عن التطوير يجعل **نسيانَ سطرٍ في `audio_queue.json` لا يُفشِل شيئاً**: التطبيق يعمل والاختباراتُ خضراء و`audio.js` يحتاط بالنطق الآليّ — فينكشف في جهاز طفلٍ لا في شجرة. فكلُّ وحدةٍ تُعلن `SPOKEN`، ويُطالَب كلُّ نصٍّ بملفٍّ مولَّد أو مكانٍ في القائمة. وأربعةُ أبواب: الإعلان · القائمة (ولا مدخلَ بائدٍ يُولَّد بلا مستهلك) · **قيدا المنهج** (لا رقمَ منطوق — ق١، ولا معدودٌ مقرونٌ بعدد — ق٢) · **الشكلُ الكامل** (كلُّ حرفٍ يحمل حركتَه، وحرفُ المدّ ولامُ الشمسية مستثنيان). و`--self-test` مُجرَّبٌ سالباً. |
| `check_range.py` | — | 🌱 | **الجلسة ١** — نظيرُ `check_decodable.py` في اقرأ **بابَه لا نصَّه** (`METHOD.md §١٠.١`): ذاك يسأل «أحرفٌ لم يُدرَّس بعد؟» وهذا «أعددٌ أو عمليةٌ أو رمزٌ فوق جبهة محطته؟». وبنيتُه من هناك: دوالُّ فحصٍ **خالصة** تُجرَّب سالباً بعبثٍ مقصود في المنهج الحيّ، و`--self-test` يُجرَد ويُشغَّل. والمفترقُ الجوهريّ: **يقرأ المنهج بتشغيل الوحدة لا بتعبيرٍ نمطيّ**، لأنّ الرحلة هنا **محسوبة** (`sections()` تؤلّف المراحلَ والبوابات) فقارئٌ نصّيّ يقرأ البيانات ولا يقرأ الرحلة — وعوَّض عن «حارس المحلّل» بمقابلة الجبهات المكتوبة بالمحطات الواصلة. |
| `generate_audio.py` | `1ccbda9` | ✂️ | **الجلسة ص** — مصرِّفُ القائمة. الدروسُ المدفوعُ ثمنُها باقيةٌ بحرفها: مفاتيحُ لا تُطبع · `KeyPool` بحصةٍ مستقلّة لكل مفتاح · سقفٌ ذاتيّ يوميّ لا يعتمد على الخادم · `_pace` يخنق كلَّ طلبٍ **ويعدّه** · قراءةُ 429 (يوميٌّ ودقيقيّ) · `EmptyAudio` تُعاد مرّتين لا خمساً · `trim_pcm` في الأنبوب · `mp3_duration` من الإطارات · الفهرسُ والبصماتُ ذرّياً بعد كل ملف · `mark_done` **دمجاً لا استبدالاً** · `archive_prev` بابَ الرجوع · شواذُّ المدة وأحكامُ الأذن. **وثلاثةُ مفارقٍ مقيَّدةٍ في رأسه**: مصدرٌ واحد (القائمةُ وحدَها لا مستخرجُ منهجٍ ثانٍ) · نموذجٌ واحد لا ثلاثة (بنكُنا مئاتٌ لا آلاف، ومادّتُه تُسمع متجاورة) · **حارسٌ عند البوّابة** يردّ ق١ وق٢ قبل أيّ طلب بدوالَّ مكتوبةٍ **مستقلّةً** عن `check_speech.mjs`. **وسقط**: محرّك edge · نماذجُ اقرأ الثلاثة وVertex · مفاضلاتُ الأصوات · احتياطُ المدود · صمامُ السياسة · الرسمُ الصوتيّ · عرفُ النثر · نسبُ Antura والتلاوة. **وزيادةٌ**: `--self-test` (٣٠ تحقّقاً، لم يكن في اقرأ) — فدخل الأداةُ سَوقةَ `test_selftests` **بالجرد**. |
| `audio_panel.py` | `42b1a50` | ✂️ | **الجلسة ص** — لوحةُ السماع بالأذن: تشغيلٌ بالتتابع، وسجلُّ ما سُمع وما بُلِّغ عنه في `localStorage`، ونسخُ البلاغات ليطبّقها `--requeue`. **والمبدَّل**: الأقسامُ **من `CATEGORY_AR` في المولّد نفسِه** لا مكتوبةً فيها (فئةٌ جديدة تظهر بلا سطر)، ولوحُ ألوان احسب، ومفاتيحُ تخزينٍ `ihsib.panel.*`. **وسقط**: النسبُ (Antura/الحصري) وقسمُ التلاوة. **وزيادةٌ**: حكمُ الأذن المقيَّد يُعرَض في السطر، ومجموعُ زمن السماع في الترويسة. |
| `trim_lead.py` | `42b1a50` | ✂️ | **الجلسة ص** — مِقصُّ الصمت الأول **بالإطارات لا بفكّ الترميز** (`frames`/`trim_bytes` و`--self-test` بحرفهما). **وثلاثةُ مفارقٍ**: أنبوبُنا يقصّ في PCM قبل الترميز فهذا علاجُ ما لم يُقَصّ فيه · **والأذنُ هي المقياس** (`--try` تبني «قبل/بعد» في `scratch`، و`--apply` يقصّ مقداراً معلوماً بعد السماع) · والمسحُ الآليّ للبنك كلِّه **نائمٌ بجرد** (§٥). و`--list` جديدةٌ: مدد البنك وأحجامه. |
| `queue_texts.mjs` | `77a14b3` | 🏗️ | **الجلسة ص** — **مسوِّي القائمة**: يقابل ما تنطقه الشجرة بالقائمة، فيضيف الناقص (`--add`) ويُسقط البائد (`--prune`). والكتابةُ الذرّية بإعادة قراءةٍ قُبيلها بحرفها. **والمفترقُ جوهريّ**: مستخرجُ اقرأ يمشي في بياناته **بمعرفته بها** ويخمّن الفئةَ **من شكل النصّ**؛ وهنا الجردُ **إعلانٌ** (`SPOKEN`)، والفئةُ **تُشتقّ من موضع النصّ في الشجرة** (جدولُ `NUMBER_NAME` · جدولُ `SAY` بمفتاحه · وحدةُ تمارينٍ تُعلن `CONSUMES`) — **ولا تُخمَّن فئةٌ البتّة**: مفتاحٌ جديد في `SAY` يوقف الأداةَ باسمه، لأنّ الفئة تختار تعليمةَ الأداء ومسحةٌ خاطئة تُسمَع. (وقد قوبلت الفئاتُ المشتقّةُ الخمسُ والعشرون بما صنّفته الجلسةُ ٣ يدوياً: **صفرُ اختلاف**.) |
| `test_audio_cache.mjs` | `41f8445` | ✂️ | **الجلسة ص** — يشغّل `app/sw.js` **نفسَه** على كاشٍ وشبكةٍ مزيَّفَين: الاستبدالُ يُخدَم فوراً بطلبٍ واحد، وغيرُ المستبدل بلا طلب، والوسمُ الأقدم يُكنَس، والإخفاقُ يمنع الكنس، والترقيةُ لا تجلب صوتاً. **وسقط**: بيانُ التلاوة وتلاوةُ الكلمة، و**تبنّي المخزن الموسوم بنسخة** (لا مخزنَ قديم عندنا — أوّلُ نسخةٍ لنا، و`adoptLegacyAudio` ساقطةٌ من `sw.js` أصلاً). **وزيادةٌ**: بابُ «اسمُ كلِّ ملفٍ هو sha1 نصِّه» — يُعاد اشتقاقُ المفتاح من نصّ الفهرس، فتُمسَك تسميةٌ بيد أو تصريفُ صورةِ نطقٍ مكانَ المكتوب. |
| `audio_queue.json` | — | 🌱 | `[]` — القائمةُ فارغة، والبروتوكولُ حيٌّ من اليوم الأول (`docs/AUDIO_QUEUE.md`). **ولم تَزِد الجلسةُ ١ فيها سطراً**: لا نصَّ منطوقاً في هذه الجلسة (بيانات منهجٍ وحرّاس، بلا شاشة تمرين). **وملأتها الجلسةُ ٣** بخمسةٍ وعشرين نصّاً، **وصرَّفتها الجلسةُ ص** فصارت كلُّها `done` بتاريخها ونموذجها. |
| `audio_verdicts.json` | — | 🌱 | **الجلسة ص** — أحكامُ أذن المالك: نصٌّ ← (الحكم، التاريخ). علّتُه من `AUDIO_QUEUE.md`: «الأحكامُ السمعية تُقيَّد بياناً فيقرؤها الحارس، فلا يتكرّر السؤالُ على المالك ولا يُنسى جوابُه» — وبه يتحوّل تنبيهُ شذوذ المدة من **مطالبة** إلى **خبر**. يُكتب بـ`generate_audio.py --verdict "النص=الحكم"`، ولا يُنشأ قبل أول حكم. |

## ٤. ما **لم** يُنسخ بعد — ولكلٍّ سببُه وجلستُه ⏸️

> هذا القسمُ عهدُ هذه الوثيقة: **كلُّ سطرٍ في `FAMILY.md §٥` مُحاسَبٌ هنا** — منسوخاً
> أو مؤجَّلاً بسببٍ مكتوب. والتأجيلُ ليس نسياناً: مكتوبٌ في بند جلسته في `SESSIONS.md`.

| المؤجَّل | السبب | جلستُه |
|---|---|---|
| ~~`generate_audio.py` · `audio_panel.py` · `trim_lead.py` · `queue_texts.mjs`~~ | ✅ **نُسخت في الجلسة ص** (الدفعة الأولى) — وصحَّ التأجيلُ عملاً: ما نُسخ اليومَ يشير إلى منهجٍ وبنكٍ **قائمين**، فسقط من الأربعة كلُّ ما كان يشير إلى ما لا وجود له (٣٠٠٠ سطرٍ ← نحو ١٢٠٠). وسطورُها في §٣. | ~~**ص**~~ ✅ |
| ~~`test_audio_cache.mjs`~~ | ✅ **نُسخ في الجلسة ص** — واستيقظ فورَ وجود الفهرس: ٢٥ ملفاً مبصومةً ببايتاتها، والسيناريو كاملاً على `app/sw.js` نفسِه. | ~~**ص**~~ ✅ |
| `audio_audit.py` (مقياسُ الموجة) | يقيس التأخّرَ الصامت والتكرارَ الداخليّ **بمتصفّحٍ حقيقيّ**، وهو رافدُ `trim_lead --measure/--apply` الجماعيّ. ولم يُنسخ لأنّ **الأذن هي المقياس** في هذا التطبيق (قرارُ المالك)، ولأنّ أنبوبَنا يقصّ في PCM قبل الترميز فلا ملفَ في بنكنا وُلِّد بلا قصّ. **والمسحُ الجماعيّ نائمٌ بجرد وجودِه** (§٥) فيستيقظ يومَ يُكتب. | يومَ تحتاجه دفعةٌ كبيرة |
| `check_live.py` · `cdn_check.py` | **فحصُ منشورٍ لا شجرة**: كلاهما يقيس `calc.mishkat.qa` بعد النشر، و`--self-test` فيهما يفحص عدّةَ النشر نفسَها (صفحاتُ `welcome/` وسيرُ `pages.yml` وبيانُ بصمات الصوت) — ولا شيءَ من ذلك موجودٌ اليوم، وحدودُ هذه الجلسة **«لا نشر»**. ونسخُهما الآن يُدخِل في جرد `test_selftests` فحصاً أحمرَ بحقّ فيُطفَأ بيدٍ — وذلك أسوأُ من تأجيله. | **٩** (النشر) |
| `perf_check.py` | يقيس `app/welcome/perf-check.html` — صفحةٌ من بناء الجلسة ٩، وخارج اعتراض عامل الخدمة عمداً. | **٩** |
| قوالبُ `welcome/` الأربع + «ثبّته أولاً» + زرُّ التثبيت | مادّةُ مرجعٍ تعريفيّ تُكتب على منهجٍ قائم — ولا منهجَ بعد. | **٩** |
| `docs/DESIGN.md` | كُتب **جديداً** لا منسوخاً (§٧): دستورُ اقرأ يخصّ الحرفَ والمصحف، ودستورُنا يخصّ الكمّ والرمز والإطار. | — |

## ٥. الحرّاسُ النائمون — **ونومُهم ذاتيٌّ يستيقظ بلا يد**

> بندُ الجلسة: «ما لا ينطبق بعد يُعطَّل **بقيدٍ مكتوب بسببه** لا يُترك أحمر صامتاً».
> وقد نُفِّذ بأقوى من ذلك: **لا رايةَ تُضبط بيد**. كلُّ حارسٍ نائمٍ **يجرد الشرطَ
> بنفسه** ويطبع `⏸ نائم، يستيقظ ذاتياً` بسببه، فيصير أخضرَ صادقاً لا أخضرَ كاذباً.
> ويومَ يوجد ما يقيسه **يعمل من تلقائه** — فلا يملك أحدٌ أن ينسى إيقاظه، ولا يُسقِط
> جلسةً بمطالبةٍ سابقةٍ لأوانها.

| الحارس | شرطُ نومه (يُجرَد لا يُضبَط) | مَن يوقظه | الحال |
|---|---|---|---|
| `test_pwa.mjs` — «الصوت مخزونٌ من بيانه» | غيابُ `app/audio/manifest.json` | الجلسة **ص** | ✅ استيقظ (ص): ٤ فحوصٍ طالب بها فوراً — بيانا الصوت في القشرة، وعدّادُ المخزون وشريطُه في لوحة وليّ الأمر |
| `trim_lead.py` — «المسحُ الجماعيّ لصمت الرؤوس» | غيابُ `tools/audio_audit.py` (مقياسُ موجةٍ في متصفّح) — **جردُ وجوده** لا رايةٌ تُضبط | مَن يكتب المقياس | نائم (جديدٌ في ص) — و`--try`/`--apply`/`--self-test` قائمةٌ عاملة |
| `test_nodes.mjs` — «بنيةُ معرّفات الرحلة» | `allNodes()` فارغة | الجلسة **١** | ✅ استيقظ (ج١) |
| `test_nodes.mjs` — «لكل سابقةٍ كاتبُ نجمة» | **سابقةٌ لم تُسجَّل شاشتُها في الموجِّه** (`registerScreen`) — نومٌ **بالسابقة لا بالرحلة** (§٩) | الجلسات **٣**–**٧**، كلٌّ يوقظ سوابقَه | ✅ استيقظ لسابقة `gate` (١ من ٢٢) |
| `test_measure.mjs` — «كلُّ نوع محطةٍ مُعلَن» | لا نوعَ عقدةٍ في الرحلة | الجلسة **١** | ✅ استيقظ (ج١) |
| `test_measure.mjs` — «الشيفرةُ تكتب ما أعلنت» | ملفُّ شاشة المحطة غير موجود | الجلسات **٣**–**٧** | ✅ عشرةُ أنواع (ج٣: ٥، ج٤: ٥)، ونائمٌ لـ١١ |
| `test_measure.mjs` — «لكل قياسٍ تمرينُه» | **نوعُ تمرينٍ لا ملفَّ لأيّ شاشةٍ تدرّسه** — نومٌ **بالنوع** لا بالجميع (§٩، ج٣) | الجلسات **٣**–**٧** | ✅ أحدَ عشرَ نوعاً (ج٣: ٦، ج٤: ٥)، ونائمٌ لأربعة |
| `check_range.py` — «المولّدات: ما تستهلكه ⊆ جبهتُها» | لا وحدةَ في `app/js/` تُعلن `CONSUMES` ولا `probeRounds` | الجلسة **٣** | ✅ استيقظ (ج٣): وحدتان و٣٩٢ جولة |
| `check_speech.mjs` — «لكل نصٍّ منطوقٍ ملفُّه أو مكانُه» | لا وحدةَ تُعلن `SPOKEN` ولا وحدةَ تنطق | الجلسة **٣** | ✅ استيقظ فور كتابته (ج٣): ٢٥ نصّاً، وصارت ٣٨ (ج٤) |
| `check_render.mjs` — «لكل نمطٍ في المعجم رسّامُه» | نمطٌ في `DISPLAYS` بلا رسّامٍ في المصيِّر — **جردُ الفرق بين القائمتين** لا رايةٌ تُضبط | الجلستان **٤** (`numeral` · `line`) و**٧** (`pattern` · `scene`) | ✅ استيقظ لـ`numeral` و`line` (ج٤)، ونائمٌ لاثنين |
| `check_render.mjs` — «لكل محطةٍ نمطٌ يبلغ جبهتَها» | محطةٌ **لا نمطَ مرسومٌ واحد** في جبهتها (`line:ten` و`intro:latin`: رمزٌ وخطٌّ لا كمية) | الجلسة **٤** | ✅ استيقظ (ج٤): ٤٩ محطةً كلُّها مقيسة |

**ولا حارسَ معطَّلٌ بيد، ولا فحصٌ محذوفٌ ليمرّ.** والنائمُ يُعَدّ ويُطبَع في ذيل كل
حارس (`و٢ نائم بقيدٍ في docs/SEED.md`)، فلا يمرّ النومُ صامتاً في مخرَجٍ يُقرأ سريعاً.

## ٦. حصيلةُ التشغيل على البذرة الفارغة (١١ أغسطس ٢٠٢٦)

```
node tools/test_selftests.mjs   ✓ أدواتٌ لها --self-test: 2 (browser_test.py، make_icons.py) — كلُّها خضراء
node tools/test_pwa.mjs         ✓ 39 فحصاً · 1 نائم
node tools/test_nodes.mjs       ✓ 2 نائم
node tools/test_measure.mjs     ✓ 3 فحوص · 1 نائم
python3 tools/make_icons.py --check    ✓ 4 أيقونات بمقاساتها
python3 tools/browser_test.py          ✓ 15 فحصاً في Chrome حقيقيّ
python3 tools/browser_test.py --device ✓ مقاسات الآيباد الخمسة: صفرُ فائضٍ أفقيّ
```

وما يفحصه المتصفّح على البذرة: أنّ التطبيق **يفتح** ويحلّ محلَّ سطر التحميل ·
والشاشةُ الأولى خريطةٌ · و`dir=rtl` و`lang=ar` · والعلامةُ «اِحْسِبْ» **مشكولةً بخطّ
Marhey وحدَه** · والمرشدُ رسمٌ متجه · و**لا رقمَ لاتينيّ في النصّ المعروض** (ق١) ·
و**لا محرفَ إيموجي يُسلَّم لخطّ النظام** · ولا فائضَ أفقيّ · وكلُّ زرٍّ يبلغ هدفَ
اللمس · و**الخريطةُ الفارغة تقول إنها فارغة** · ولوحةُ وليّ الأمر خلف بوابتها.

## ٧. زياداتٌ على البذرة (ليست من اقرأ)

1. **سِجلُّ الشاشات** (`registerScreen` في `main.js`): في اقرأ كان الموجِّه يعرف كلَّ
   شاشةٍ باسمها، فكلُّ محطةٍ جديدة سطرٌ فيه. وهنا تسجّل الوحدةُ نفسَها — والعلّةُ
   ليست الأناقة بل **قاعدةُ العائلة**: «الرحلة تُحسب من البيانات لا تُكتب بيد».
2. **حقنُ التمارين في محرّك الجلسة** (`setBuilders` في `review.js`): يفصل الهيكلَ عن
   المادّة فصلاً بنيوياً، فلا يستطيع تمرينٌ أن يتسرّب إلى المحرّك.
3. **`--self-test` لثلاث أدواتٍ** لم يكن لها (`browser_test.py` · `make_icons.py` ·
   **`generate_audio.py`**): الجردُ يجدها فتُشغَّل — فالعدّةُ نفسُها محروسةٌ من اليوم
   الأول. وفحصُ المولّد ثلاثون تحقّقاً **بلا شبكةٍ ولا مفتاح**: المفتاحُ (sha1) وصورةُ
   النطق وقصُّ الصمت ومدةُ mp3 والبصمةُ وقراءةُ 429 والاستجابةُ الفارغة —
   **ومنها بابٌ يقيس القائمةَ الحيّة**: مدخلٌ واحد يخالف ق١ أو ق٢ يجعل الفحصَ أحمر.
4. **النومُ الذاتيّ للحرّاس** (§٥) — نمطٌ جديد يُقترح على العائلة: يحلّ محلَّ
   «فحصٌ مُطفأ بيدٍ يُنسى إشعالُه»، وهو من بابِ حراسةِ الغياب نفسِه.
5. **`docs/DESIGN.md`** جديدٌ كلُّه — دستورُ تصميمٍ لتطبيق كمياتٍ لا لتطبيق حروف.
6. **مصيِّرُ الكميات وحارساه** (الجلسة ٢): في اقرأ يرسم الحرفَ **خطٌّ** فيُحرَس بمقاييس
   الحبر والرفعة؛ وهنا ترسم الكميةَ **هندسةٌ** فيُحرَس **العددُ نفسُه**. ومنه شكلٌ
   جديد في الحراسة: **الشيفرةُ تُرجِع ما فعلت لا ما نوت** (`drawn` مقروءٌ من DOM لا
   من `count`)، فيصير الفرقُ بين النية والفعل مقيساً لا مظنوناً.
7. **قيدا المنهج محروسان عند بابين لا باب** (الجلسة ص): ق١ (لا رقمَ منطوق) وق٢ (لا
   معدودٌ مقرونٌ بعدد) يفحصهما `check_speech.mjs` على الشجرة **و**`generate_audio.py`
   على كل نصٍّ **قبل أن يُطلَب** — والدوالُّ مكتوبةٌ في الأداتين **مستقلّةً** (بايثون
   وجافاسكربت) عمداً: مقابلةُ نسخةٍ بنسختها لا تُثبت شيئاً (درسُ جدول النرد في
   `check_render.mjs`)، فإن اختلف الحكمان ظهر الخلافُ ولم يُدفَن. وهو نصُّ
   `AUDIO_QUEUE.md` حرفاً: *الحارسُ عند البوابة وعند الصندوق معاً*.

## ٨. عهدُ هذه الوثيقة

- **تُحدَّث مع كل نسخٍ جديد من اقرأ**: أيُّ جلسةٍ تنسخ ملفاً من البذرة تُضيف سطرَه
  هنا ببصمته وما عدّلت فيه — وإلا كان النسخُ فقدَ ذاكرة.
- **وما انكسر من البذرة تحت الحمل الحقيقيّ يُصلَح في جلسته ويُقيَّد في §٩** (شرطُ
  مدير المشروع في مراجعة الجلسة ٠): فالبذرةُ صُمِّمت على رحلةٍ فارغة، وأوّلُ حملٍ
  حقيقيّ يكشف ما لا تكشفه الشجرةُ الخالية.
- **وتُقرأ يومَ يُستخلَص المحرّك** (بعد نضوج تطبيقين، `FAMILY.md §٤`): ما وقع في
  🟰 مرشَّحٌ للاستخلاص كما هو، وما وقع في 🏗️ يحتاج تصميماً يسع المادّتين، وما وقع في
  ✂️ يُقرأ عمودُ «ما عُدِّل» لِيُعرَف **أيُّ فرقٍ جوهريّ وأيُّه عرَضيّ**.

## ٩. ما انكسر من البذرة تحت أول حملٍ حقيقيّ — وأُصلح في جلسته

> **الجلسة ١** (١١ أغسطس ٢٠٢٦): أوّلُ رحلةٍ حقيقية (٥٢ عقدة، ٢٢ سابقة) أيقظت حارسين
> نائمين، فظهر أنّ **شرطَ نومهما كان أخشنَ مما ينبغي**: نومٌ بالكلّ لا بالجزء. وكلا
> الإصلاحين **يشدّ الحارسَ ولا يُرخيه** — ولا فحصَ أُطفئ ولا حُذف ليمرّ.

| ما انكسر | العلّة | ما صار |
|---|---|---|
| `test_nodes.mjs` §٢ «لكل سابقةٍ كاتبُ نجمة» | كان ينام حتى تظهر **أيُّ** وحدةٍ خارج البذرة، ثم يطالب **بالسوابق كلِّها دفعةً**. وسوابقُ الرحلة اثنتان وعشرون تُكتب شاشاتُها في خمس جلسات (٣–٧) — فكان يُسقِط الجلسة ٣ بذنب السابعة. وقد استيقظ فوراً في هذه الجلسة لأنّ `gate.js` وحدةٌ خارج قائمة `SEED` فيه. | **النومُ بالسابقة**: تُجرَد السوابقُ **المسجَّلة في الموجِّه** (`registerScreen('<السابقة>'`) فيُطالَب كلُّ مسجَّلةٍ بكاتب نجمتها، وتنام غيرُ المسجَّلة بشرطٍ مجرود. وهو **أشدُّ** لا أخفّ: كان يقيس بوجود ملفٍّ في المجلد، فصار يقيس بالتسجيل عينِه — وشاشةٌ سُجِّلت وكتبت نجمتَها بمعرّفٍ آخر تسقط يومَ تُسجَّل. وأُثبت أثرُه فوراً: أوّلُ ما فعله أن أقرّ كاتبَ سابقة `gate` وهي `gate.js` من البذرة. |
| `test_measure.mjs` §٣ «لكل نوع قياسٍ تمرينُه» | كان ينام بشرطٍ واحد: خلوُّ `STATIONS` من أنواع القياس. وهذه الجلسة تملؤه (بندُها ٣) بينما **بانِي التمارين لا يُحقَن قبل الجلسة ٣** (`setBuilders` في `review.js`) — فلا مهارةَ تستطيع أن تُنتج تمرينَها، والمطالبةُ سابقةٌ لأوانها بالبناء لا بالتقصير. | **درجتان كما في `test_nodes`**: تنام الأولى بخلوّ `STATIONS`، وتنام الثانية ما دامت **لا وحدةَ في `app/js/` خارج البذرة تنادي `setBuilders`** — وهو الشرطُ الحقيقيّ لإمكان الفحص، مجرودٌ لا مضبوطٌ بيد. |

> **الجلسة ٣** (١١ أغسطس ٢٠٢٦): أوّلُ **شاشاتِ تمارينَ** حقيقية (خمسةُ أنواع، ٣٩٢
> جولةً مولَّدة) كشفت في البذرة والعدّة أربعةَ مواضع. وكلُّها **تشدّ ولا تُرخي**: ثلاثةٌ
> منها تحوّل «أخضرَ كاذباً أو صامتاً» إلى بلاغٍ صادق، والرابعُ يوسّع ما يُقاس.

| ما انكسر | العلّة | ما صار |
|---|---|---|
| `test_pwa.mjs` — «كلُّ وحدة js تصل إليها شجرةُ `main.js`» | كان يقرأ `from './x.js'` وحدَه، ووحدةُ التمارين **تُستورَد لأثرها** (`import './quantity.js';` — تُحمَّل فتسجّل شاشاتِها في السجلّ). فعدَّ ثلاثَ وحداتٍ حيّةٍ **ميتة** — بلاغٌ كاذب لا فحصٌ ساقط. | يقرأ الصيغتين. و**أدقُّ لا أرخى**: ملفٌّ لا يستورده أحدٌ يبقى مكشوفاً كما كان. |
| `test_measure.mjs` §٣ «لكل نوع قياسٍ تمرينُه» | كان ينام بشرطٍ واحد (لا وحدةَ تحقن `setBuilders`) ثم يطالب **بالأنواع الخمسةَ عشرَ دفعةً**. وشاشاتُها تُكتب في خمس جلسات — فأسقط الجلسةَ ٣ بذنب السابعة، عينَ ما وقع لـ`test_nodes` في الجلسة ١. **ومعه عيبٌ ثانٍ**: كان يقيس بلا أن **يستورد** وحدات التمارين، فيجد الهيكلَ فارغاً ويقول «لا تمرين» وفي الشجرة تمرين. | **النومُ بالنوع**: نوعُ تمرينٍ لا ملفَّ لأيّ شاشةٍ تدرّسه ينام، وما وُجد ملفُّ شاشته **يُطالَب فوراً** — وهو شرطُ بابِ الشيفرة نفسِه. ومعه: تُحمَّل وحداتُ التمارين كلُّها قبل القياس، فيَثبُت بذلك **عقدُ الوحدة الخالصة** أيضاً (تُستورَد في node بلا متصفّح). |
| `browser_test.html` — الصفحةُ تموت صامتة | خطأٌ واحد في نصّها (أو انتظارٌ لحدثٍ لا يجيء) يوقفها فلا تصل نتيجةٌ أصلاً، فيُطبَع «لم تصل نتيجة» ويُقرأ بطءَ جهازٍ أو قِصَرَ مهلة. **وقد وقع فعلاً في هذه الجلسة مرّتين**: تصادمُ اسمٍ في النصّ، ثم إسنادُ `src` يختلف بالمقطع (`#`) وحدَه فلا يُطلِق `load`. | ثلاثةٌ معاً: **العطبُ يُبلَّغ** (ما مضى من الفحوص يُرسَل ومعه العطبُ سطراً أحمر)، و**التعلّقُ يُبلَّغ** (مهلةٌ حارسة ترسل ما تمّ)، و**النصُّ يُحلَّل** في `browser_test.py --self-test` بـ`node --check` قبل أن يُشغَّل المتصفّح أصلاً. |
| `browser_device.html` — يقيس الخريطةَ وحدَها | «لا فائضَ أفقيّ» عهدٌ مطلق، وأوسعُ ما في التطبيق **شاشاتُ التمارين** لا الخريطة (لوحُ العدّ يفرد الشكلَ ليبلغ العنصرُ هدفَ اللمس). فكان يشهد بسلامة ما لم يقسه. | يزور أربعَ شاشات (الخريطةُ · لوحُ العدّ · المقارنةُ · التسوية) ويبلّغ **أسوأَ** ما قِيس. وضجيجُ انكسار الأنبوب عند قتل المتصفّح أُسكِت في الخادم — أثرُ مكدَّسٍ فوق التقرير يدفع القارئَ إلى تخطّي المخرَج فيمرّ فشلٌ حقيقيّ تحته. |

> **الجلسة ص** (١١ أغسطس ٢٠٢٦): أوّلُ **بنكٍ صوتيّ** (٢٥ ملفاً) كشف موضعين — أحدُهما
> **بابٌ صحّ ما دامت القائمةُ كلُّها منتظِرة وبطل يومَ صار فيها منجَز**، والآخرُ
> **عيبٌ في التنسيق يحجب الكميةَ عن الطفل**، وُجد لأنّ شيفرةَ هذه الجلسة اتّكأت على
> `hidden` نفسِه فجُرِّب فسقط. ولم ينكسر من البذرة شيءٌ ثالث: `test_pwa` استيقظ بشرطه
> المجرود **وطالب فوراً بأربعة**، فأُدّيت ولم يُطفَأ منها شيء — وذلك الحارسُ **يعمل**
> لا الحارسُ ينكسر.

| ما انكسر | العلّة | ما صار |
|---|---|---|
| `check_speech.mjs` §٢ «لكل نصٍّ ملفُّه أو مكانُه» | كان السترُ «ملفٌّ **أو** مدخلٌ في القائمة» — وهو صحيحٌ يومَ كتبته الجلسةُ ٣ لأنّ كلَّ مدخلٍ كان `pending` فلا فرقَ يُقاس. وبعد التصريف صار كلُّ المدخلات `done` **وتبقى في القائمة أبداً** (سجلاً تاريخياً لا يُمَسّ) — فلو حُذف ملفٌّ من البنك غداً لَستَره مدخلُه، والطفلُ يسمع نطقاً آلياً والحارسُ أخضر. **وهو أخطرُ من الغياب: القائمةُ تدّعي «صُرِّف»**. | **السترُ يُشدّ**: ملفٌّ في البنك، **أو** مدخلٌ ما زال `pending`. ومدخلٌ يقول «صُرِّف» ولا ملفَ له يُمسَك **باسمه** في بابٍ مستقلّ. والحكمُ دالّةٌ خالصة (`coverOf`) **مُجرَّبةٌ سالباً** بخمس حالاتٍ في `--self-test`. ولا فحصَ أُطفئ: البابُ القديم باقٍ كما هو وزِيد عليه. |
| `css/app.css` — **`hidden` لا يُخفي شيئاً أعطيناه `display`** | قاعدةُ المتصفّح `[hidden] { display: none }` من **أصل المتصفّح**، وكلُّ قاعدةٍ من أصلِنا تعلو عليها في التتالي مهما ضعفت نوعيّتُها. فكلُّ عنصرٍ له `display` صريح **يتجاهل `hidden` بصمت**. وقد **قِيس في Chrome حقيقيّ**: `.q-cover` وهو `hidden=true` إذا `display:grid` و**٢٤٢ بكسل مرسومةً فوق كميةٍ ارتفاعُها ٢٣٠** — أي أنّ غطاء الخطف **لا يُرفع أبداً**، فتُحجَب الكميةُ عن الطفل في «جرِّب معي» وفي العدّ أمامه بعد الخطأ. ومثلُه `.beads` (`display:flex`). **ولم يمسكه فحصٌ**: كلُّ ما كُتب يقرأ `el.hidden` — أي يصدّق الوعدَ ولا يقيس الرسم. | سطرٌ واحد في التصفير: `[hidden] { display: none !important; }` — و`!important` ضرورةٌ لا زينة (بدونها تعود كلُّ قاعدةٍ من أصلِنا تغلبه). **وحارسٌ يقيس الرفعَ على الشاشة** في `browser_test.html`: الغطاءُ في «جرِّب معي» مرفوعٌ برمجياً **وغيرُ مرسوم**، ومعه جردٌ عامّ لكل `[hidden]` بعد التنسيق. **ومُجرَّبٌ سالباً**: بإسقاط السطر يصير الفحصُ أحمرَ ويطبع `grid`. |

**وزيادتان في `test_measure.mjs`** (لا إصلاح): بابُ **«لا انحرافَ بين الجردين»** —
صار للقياس مصدران (مفاتيحُ `skills` في المنهج، وأنواعُ التمارين في `STATIONS`)،
ومصدران لحقيقةٍ واحدة يفترقان بلا حارس؛ فيُقابَلان صنفاً بصنف، **ولا محطةَ معفاةٍ
تعلن مفتاحاً**. وقاعدةٌ لزمت منه: **كلُّ نوع شاشةٍ يُنجَز في جلسةٍ واحدة** — ولذلك
سكنت محطةُ التعارف ٨·٥ ملفَّها المفرد `intro.js` لا ملفَّ شاشات المرحلة ٨ (بابُ
الشيفرة يطالب المعفاةَ بألّا تكتب مهارةً البتّة، فلو سكنت ملفاً يقيس لَما ثبت إعفاؤها).

## ١٠. ما عدّلته الجلساتُ في وحدات البذرة بعد نسخها

> عهدُ §٨: النسخُ بلا ذاكرةٍ فقدٌ. وهذا سجلُّ ما تحرّك **في وحدات البذرة نفسِها** بعد
> يوم النسخ — لا في الملفات الجديدة (فتلك 🌱 في جدولها).

| الوحدة | الجلسة | ما تحرّك، ولماذا |
|---|---|---|
| `js/main.js` | ٢ | **سطرُ استيرادٍ وسطرُ تسجيلٍ وزرٌّ في صندوق التجربة**: `registerScreen('render', renderGallery)` **تحت `DEV` وحدَه**. وعلّتُه أنّ `test_pwa` يرفض وحدةَ جافاسكربت لا تصل إليها شجرةُ `main.js` («لا ملفٌّ ميّت يُنزَّل إلى جهاز طفلٍ ولا يُستعمَل») — والمصيِّرُ لا مستهلكَ له قبل الجلسة ٣. وهو **ليس محطةً**: لا عقدةَ له في الرحلة ولا نجمةَ يكتبها، ولا طريقَ إليه من شاشة طفلٍ لأنّ التسجيلَ نفسَه لا يقع إلا تحت `?dev=1`. (بندٌ يُرفع — `SESSIONS.md`.) |
| `js/sw.js` | ٢ | `VERSION` إلى `v2`، و`js/render.js` و**`emoji/index.json`** في قائمة القشرة — وملفاتُ الرموز تدخل المخزون **من فهرسها** (`precacheEmoji` من البذرة) لا بسطرٍ يدويّ. |
| `css/app.css` | ٢ | قسمُ **مصيِّر الكميات**: `--unit` (وحدةُ رسمٍ واحدة لكل الأنماط فيتساوى حجمُ العنصر)، وبطاقةُ الكمية، وخاناتُ الإطار، وموضعُ عنصر عالم الطفل، ولوحُ المراجعة. |
| `js/main.js` | ٣ | **نُقل سِجلُّ الشاشات** إلى `registry.js` (بعلّته في رأسه)، فصار `main.js` يستورد منه ويبقى مسجِّلاً للبوابة وللوح المصيِّر تحت `DEV`. **وسطران يستوردان وحدتَي التمارين لأثرهما** — سطرٌ لكل **وحدة** لا لكل محطة: به تصل إليها شجرةُ الاستيراد فيعرف عاملُ الخدمة ماذا يخزّن، وتدخل محطاتُ أنواعها الرحلةَ بلا سطرٍ يُعدَّل. |
| `js/sw.js` | ٣ | `VERSION` إلى `v3`، وأربعُ وحداتٍ في قائمة القشرة (`registry` · `station` · `quantity` · `counting`). |
| `css/app.css` | ٣ | قسمُ **حلقة المحطة وشاشات التمارين**: شريطُ الخطوات وخرزاتُ الجولات (**بلا رقمٍ واحد** — عهدُ المرحلة ١)، وساحةُ الكمية وغطاءُ الخطف، وبطاقاتُ الاختيار، وطبقةُ اللمس (`.fig-tap` زرٌّ حقيقيّ فوق كل عنصر)، وأثرُ العدّ على العنصر المرسوم **لوناً ومعه حجم** (اللون لا يحمل معنىً وحدَه). و`--unit` **يختلف بالدور لا بالنمط**: بطلُ الشاشة وبطاقةُ الاختيار والملموس، ويبقى حجمُ العنصر واحداً داخل الدور الواحد. |
| `js/sw.js` | **ص** | `VERSION` إلى `v4`، و**بيانا الصوت في قائمة القشرة** (`audio/manifest.json` · `audio/versions.json`) — ليُقرآ **دون إنترنت**: بلا `versions.json` لا وسمَ للروابط فيُخزَّن غيرُ ما يُطلَب. **ولم يُعدَّل سطرُ منطقٍ واحد**: `precacheAudio` كانت تحتمل غيابَ البيان بلا خطأ، فعملت من تلقائها يومَ وُجد — وهو ما وعد به تعليقُ رأسها منذ الجلسة ٠. |
| `js/progress.js` | **ص** | **`audioStored()`** — كم من البنك على هذا الجهاز، مقروءاً **من مخزن عامل الخدمة نفسِه** لا من تقديرٍ عندنا. علّتُها من اقرأ حرفاً: إخفاقُ الخزن كان **يُبتلَع صامتاً** (ضيقُ حصة التخزين على الأجهزة الأقدم) فتنقص ملفاتٌ ولا يعلم أحد، ثم يصمت الصوتُ في الطائرة فيُظنّ العيبُ في البرنامج. و`null` تعني «لا جواب» فيسقط السطرُ ولا ينكسر شيء. |
| `js/parent.js` | **ص** | **سطرُ الأصوات المخزونة** بشريطٍ حيٍّ وزرِّ «نزّل الأصوات الآن» — وهو ما نصّ عليه §٢ عند النسخ: «سقط شريطُ تحميل الصوت — **يعود مع بنك الصوت**». والشريطُ يتحرّك ببلاغات `audio-progress` من العامل بعد كل دفعة (لا بتقديرٍ منّا)، والزرُّ يبعث `audio-sync` فيتجاوز مهلةَ الشفاء. **ولا يظهر السطرُ قبل أن يُعرَف أنّ ثمّ بنكاً** (`hidden` حتى أوّل رقمٍ حقيقيّ). |
| `css/app.css` | **ص** | `.dl` و`.dl-bar` و`.dl-fill` — شريطُ خزن الصوت، وامتلاؤه يقلب لونَه إلى لون الصواب. **وسطرٌ في التصفير**: `[hidden] { display: none !important; }` — إصلاحُ عيبٍ مقيسٍ يحجب الكميةَ عن الطفل (§٩). |
| `tools/browser_test.html` | **ص** | **فحصُ «لا نطقَ آليّ» في مسار الطفل**: يُبدَّل في نافذة التطبيق `Audio` و`SpeechSynthesisUtterance` بشاهدَين، فيُقيَّد كلُّ ملفٍ شُغِّل وكلُّ لجوءٍ إلى النطق الآليّ — والحكمُ في آخر الصفحة، **ويستثني ما زال ينتظر في `/__queue.json`** فيصير صارماً من تلقائه يومَ تفرغ القائمة (وقد فرغت). ومعه **حلُّ بوابة وليّ الأمر** لِيُفحَص ما وراءها (سطرُ خزن الصوت)، و**فحصا الرفع المقيس** (§٩): الغطاءُ في «جرِّب معي» مرفوعٌ وغيرُ مرسوم، وجردٌ عامّ لكل `[hidden]` بعد التنسيق. |
| `js/station.js` | ٤ | **ثلاثُ زياداتٍ لا تعديل**: (١) **`model.reveal`** في خطوة «شاهِدْ» — حلقةُ تقديم الرمز (`METHOD.md §٣`): يُكشَف الرمزُ **بعد** العدّ و**فوق** الكمّية، ولا يُعرَض معها بحال. (٢) **`slotLayer` و`countAlongLine`** — خانةُ لمسٍ على خطّ الأعداد من `slots` في المصيِّر نفسِه، وعدٌّ على الخطّ علامةً علامةً للتصحيح. (٣) **`usedOf` تجرد الرموز** (`numeral` و`line`) فيقابلها الحارسُ بأقصى رمز الجبهة لا بأقصى كمّها. ودخل الصفرُ `NUMBER_NAME` مع محطته ٣·٨، و`countAloud` تَعُدّ على الخطّ إذا كان الشكلُ خطّاً. |
| `js/main.js` | ٤ | **سطرا استيرادٍ لوحدتَي التمارين الجديدتين** (`numerals` · `compare`) — سطرٌ لكل **وحدة** لا لكل محطة، كما في الجلسة ٣: اثنتا عشرةَ محطةً دخلت الرحلةَ بهما. |
| `js/sw.js` | ٤ | `VERSION` إلى `v5`، ووحدتان في قائمة القشرة (`numerals` · `compare`). |
| `css/app.css` | ٤ | قسمُ **شاشات المرحلتين ٣–٤**: بطاقةُ الرمز (رقمٌ مشرقيّ بخطّ النسخ في صندوق بطاقة النرد نفسِه)، وخطُّ الأعداد (محورٌ وعلاماتٌ وأرقامُ مرساة، وأثرُ العدّ عليه **لوناً ومعه حجم**)، وخاناتُ اللمس على الخطّ بدبابيسها، وصفُّ الترتيب، والكميّتان **صفّين متحاذيين** (`.q-rows`)، وموضعُ كشف الرمز (`.q-reveal`). |
| `tools/browser_test.html` | ٤ | **عشرةُ فحوصٍ لشاشات المرحلتين ٣–٤**: الرمزُ لا يُكشَف إلا بعد العدّ · والمكشوفُ رقمُ الكمّية المعدودة · والمطابقةُ ذهاباً وإياباً في المحطة الواحدة · ومحطةُ الصفر **إطارٌ فارغ خاناتُه مرسومة** · وخطُّ ٠–١٠ بأرقام مرساته وحدَها · **والصفرُ في يمينه مقيساً على الشاشة** · وخاناتُ الموضع ثلاثٌ تبلغ هدفَ اللمس · وخطأُ الموضع يُعَدّ على الخطّ · والمقارنةُ بالرمزين والكميّتان **صفّان متحاذيان مقيسَي التحاذي** · وصفُّ الترتيب ينقل الأصغرَ أولاً. **وقارئُ المرسوم صار بمسطرة صنفه** (`data-kind`) وجدولُ الأرقام مكتوبٌ في الصفحة مستقلاً. والمهلةُ الحارسة إلى ٢٢٠ثانية (خمسُ شاشاتٍ تُساق).
| `tools/browser_device.html` | ٤ | ثلاثُ شاشاتٍ زيادةً على الأربع: **خطُّ الأعداد** (أوسعُ شكلٍ في الرحلة) وصفُّ الترتيب وبطاقةُ الرمز — فيُقاس الفائضُ الأفقيّ حيث يقع فعلاً.
