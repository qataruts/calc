# خطة الجلسات التنفيذية — «اِحْسِبْ»

> كل جلسة تنفيذ (أوبوس ٥) تقرأ أولاً: `CLAUDE.md` ثم `docs/METHOD.md` (المرجع الملزم) ثم بند جلستها هنا، وتلتزم بحدود جلستها ولا تتجاوزها.
> عند الإتمام: حدِّث حالة الجلسة هنا (☐ ← ☑ مع ملخص المنتجات والفحوص)، والمنتجات تُعرض على مدير المشروع (فابل ٥) للمراجعة — **وما احتاج قراراً منهجياً يُرفع ولا يُقرَّر ذاتياً**.
> قواعد عامة: بيانات المنهج مصدرها الوحيد `app/js/curriculum.js` · **لا جلسة تولّد صوتاً أو تلمس `app/audio/`** (النصوص الجديدة إلى `tools/audio_queue.json`) · كل جلسة تشغّل فحوص الجلسات السابقة كلها قبل التسليم.

## ☑ الجلسة ٠ — التأسيس من بذرة اقرأ + `SEED.md` — **أُنجزت (١١ أغسطس ٢٠٢٦)**

**المنتجات:** بذرةُ المنصة منسوخةً من `read@ea07f350` ومجرَّدةً من مادّته — سبعُ
وحداتٍ في `app/js/` (`audio` حرفياً · `ui` · `progress` · `review` · `gate` · `parent`
· `main`) و`sw.js` و`app.css` بلوح **اِحْسِبْ** الخاص، وبذرةُ `curriculum.js` **فارغةً
تُعلن عقدها** (تملؤها الجلسة ١)، وهويةٌ كاملة (`index.html` · `manifest` · أربعُ
أيقوناتٍ مولَّدة من `tools/icon.html`) · وعدّةُ `tools/` (`test_selftests` حرفياً ·
`test_pwa` · `test_nodes` · `test_measure` · `browser_test` بصفحتيه · `make_icons` ·
`fetch_twemoji` حرفياً) · و`docs/SEED.md` كامل الجرد بالبصمات · و`AUDIO_QUEUE.md`
و`DESIGN.md`. و`.venv/` وخادمُ التطوير (`python3 -m http.server -d app`).

**قلبُ الاعتماد** (أعمقُ تعديلٍ في البذرة): كانت الرحلةُ تُبنى في `progress.js`
والخريطةُ تعرف أنواعَ محطاتها والموجِّهُ يعرف شاشاته. وصارت **الرحلة تُقرأ من
`sections()` في `curriculum.js`**، والقسمُ **يصف نفسَه** للخريطة، والشاشةُ **تسجّل
نفسَها** في الموجِّه، والتمرينُ **يُحقَن** في محرّك المراجعة — فمحطةٌ جديدة تدخل
الرحلة بلا سطرٍ يُعدَّل خارج بيانات المنهج.

**الفحوص:** `test_selftests` أخضر (أداتان تعلنان `--self-test`، وكلتاهما خضراء) ·
`test_pwa` ٣٩ فحصاً · `test_measure` ٣ · `browser_test` **١٥ فحصاً في Chrome حقيقي**
(يفتح · RTL · العلامة بخطّها · صفرُ رقمٍ لاتينيّ · صفرُ إيموجي نظام · هدفُ اللمس ·
الخريطةُ الفارغة تقول إنها فارغة · لوحةُ وليّ الأمر خلف بوابتها) ·
`--device` **صفرُ فائضٍ أفقيّ على مقاسات الآيباد الخمسة** · `git status` يشهد أنّ
`read/` لم يُمَسّ.

**الحرّاسُ النائمون — ونومُهم ذاتيٌّ لا بيد** (`SEED.md §٥`): ما يفترض محتوىً غير
موجودٍ **يجرد شرطَه بنفسه** ويطبع `⏸ نائم، يستيقظ ذاتياً` بسببه، ويعمل من تلقائه يومَ
يوجد ما يقيسه — فلا فحصٌ مُطفأ بيدٍ يُنسى إشعالُه، ولا أخضرُ كاذب.

**مؤجَّلٌ بسببه** (`SEED.md §٤`): عدّةُ توليد الصوت الأربعة و`test_audio_cache`
للجلسة **ص** (وحدةٌ مشدودةٌ إلى منهجٍ وبنكٍ لا وجودَ لهما — والبروتوكولُ حيٌّ دونها)،
و`check_live` · `cdn_check` · `perf_check` وقوالبُ `welcome/` للجلسة **٩** (فحصُ
منشورٍ لا شجرة، وحدُّ هذه الجلسة «لا نشر»).

**يُرفع إلى مدير المشروع** (لم يُقرَّر ذاتياً): بندٌ واحد في §«ما يُرفع» أسفل هذا الملف.

<details><summary>بندُ الجلسة كما وُضع</summary>

**المطلوب:**
1. نسخ **بذرة المنصة** من `read` (القائمة الملزمة في `FAMILY.md §٥`): وحدات `app/js/` المحايدة للمادة (`progress.js` · `review.js` · `gate.js` · `parent.js` · `audio.js` · `ui.js` · هيكل `main.js` · `sw.js`) ولوح `app.css` هيكلاً، وأدوات `tools/` المذكورة هناك، وقوالب `docs/` (`AUDIO_QUEUE.md` · بنية `DESIGN.md`). **لا يُنسخ** `curriculum.js` ولا أي محتوى مادة.
2. كتابة **`docs/SEED.md`**: جدول كل ملف منسوخ ← بصمة التزام اقرأ الذي نُسخ منه ← ما عُدِّل فيه وسببه. الاستخلاص اللاحق يعتمد عليه.
3. تكييف الهوية: `index.html` هيكل RTL فارغ بعلامة **اِحْسِبْ** (Marhey) · `manifest.webmanifest` وأيقونات مولّدة بـ`make_icons.py` · لوح ألوان مبدئي خاص بالتطبيق (لا نسخ لوح اقرأ بألوانه) · تجريد كل إشارة لمحتوى اقرأ من الوحدات المنسوخة (أسماء بستان/سورة/حرف…) مع إبقاء الهيكل.
4. تشغيل الحرّاس الهيكليين على التطبيق الفارغ: `test_selftests.mjs` يجرد `tools/` ويشغّل ما يعلن `--self-test`؛ وما لا ينطبق بعد (فحص يفترض محتوىً غير موجود) يُعطَّل **بقيدٍ مكتوب بسببه في `SEED.md`** لا يُترك أحمر صامتاً.
5. `.venv/` وبيئة الأدوات، وخادم التطوير (`python3 -m http.server -d app`).

**الحدود:** لا محتوى منهج، لا شاشة تمرين، لا صوت، لا نشر.
**معايير القبول:** التطبيق يفتح من خادم ملفات بعلامة احسب وشاشة خريطة فارغة · `SEED.md` كامل الجرد بالبصمات · `test_selftests` أخضر (وكل معطَّل مقيَّد بسببه) · `git status` يشهد أن `read/` لم يُمَسّ.

</details>

**قرارٌ يُرفع إلى مدير المشروع — ولم يُقرَّر ذاتياً:**

> **بذرةُ `curriculum.js` الفارغة.** حدودُ الجلسة «لا محتوى منهج»، ومعيارُ قبولها
> «شاشة خريطة فارغة» — والثاني يقتضي وجودَ الملفّ (غيابُه يُسقِط التطبيق عند أول
> استيراد). فكُتب **فارغاً يُعلن عقده**: `STAGES = []` و`GATES = []` و`sections()`
> تعود `[]`، وصفرُ مرحلةٍ وصفرُ محطةٍ وصفرُ عدد — ولا سطرَ منهجٍ واحد. وقُرئ ذلك
> **وصلةً لا محتوىً**. فإن رأى المدير غيرَ ذلك فالبديلُ أن تُنقل الوصلةُ إلى
> `progress.js` احتياطاً ويُنشئ الملفَّ أوّلُ من يكتب فيه — ويُعدَّل سطران.

**مراجعة المدير (١١ أغسطس ٢٠٢٦): مقبولة.** تحقّق مستقلّ كامل: `test_selftests`
(أداتان، خضراوان) · `test_pwa` ٣٩+١ نائم · `test_nodes` ٢ نائمان · `test_measure`
٣+١ نائم · `browser_test` ١٥/١٥ في Chrome حقيقي · `--device` صفرُ فائض على المقاسات
الخمسة · `make_icons --check` ٤/٤ · و`read/` لم يُمَسّ من أصوله (المعدَّل الوحيد
`read.code-workspace` — تعديلُ workspace إداريّ بيد المالك، خارج مسؤولية الجلسة) ·
و`SEED.md` يحاسب كلَّ سطرٍ من `FAMILY.md §٥` نسخاً أو تأجيلاً بسببه.
- **إقرار بذرة `curriculum.js` الفارغة كما نُفِّذت**: قراءتُها «وصلةً لا محتوىً»
  صحيحة، والبديل مرفوض — نقلُ الوصلة إلى `progress.js` احتياطاً يخلط الأدوار عينَ
  الخلط الذي فكّه قلبُ الاعتماد: «مصدرُ الحقيقة الوحيد» يجب أن يكون قائماً من اليوم
  الأول ولو صفراً، فالفراغُ المعلَن بياناتٌ صادقة والغيابُ عطب.
- **إقرار «الحرّاس النائمين ذاتياً» ترقيةً معتمدة على نصّ البند**: «فحصٌ مُطفأ بيدٍ
  يُنسى إشعالُه» هو عينُ الصنف الذي وُلد منه `test_selftests` في اقرأ، والجردُ
  الذاتيّ للشرط يغلقه بنيوياً. **تُرقّى قاعدةً دائمة في هذا التطبيق** (قُيّدت في
  `CLAUDE.md`)، واقتراحُ تعميمها على العائلة يُرفَع للمالك عند أول تحديثٍ لـ`FAMILY.md`
  — فالوثيقة نسخةٌ مطابقة في المستودعات ولا يعدّلها تطبيقٌ منفرداً.
- **إقرار قلب الاعتماد** (`sections()` · وصفُ القسم لنفسه · `registerScreen` ·
  `setBuilders`): ليس أناقةً بل شرطُ الجلسة ١ — المراحلُ الثماني تدخل الخريطةَ
  بياناتٍ خالصة. ومسؤوليةُ ما بعده: أيُّ سلوكٍ للبذرة ينكسر تحت الحمل الحقيقيّ
  يُصلَح في جلسته ويُقيَّد في `SEED.md §٨`.
- تنويهُ استحسان: سقوطُ خطّ المصحف من النسخ (٩٢ كيلوبايت بلا مادّة) — عينُ «خفّة
  التحميل» التي تعدّها الدراسة علّةَ استقلال التطبيقات.



## ☑ الجلسة ١ — المنهج والفاحص: `curriculum.js` + `check_range.py` — **أُنجزت (١١ أغسطس ٢٠٢٦)**

**المنتجات:** `app/js/curriculum.js` كاملاً — **المراحل الثماني (٤٩ محطة) والبوابات
الثلاث** كما في `METHOD.md §٣`، **كلُّ محطةٍ تُعلن جبهتَها** ومفاتيحَ ليتنر التي
تدرّسها، ومعجمُ الجبهة (`OPS` · `SIGNS` · `DISPLAYS` · `LEVEL_MAX` · `SUBITIZE_MAX`
· `COUNT_MAX`) مُعلَنٌ في المنهج لا في الحارس. و**الرحلةُ تُحسب**: `sections()` تؤلّف
مرحلةً ثم بوابتَها إن أعلنت موضعَها بعدها — **٥٢ عقدة** لم يُكتب عددُها في موضعٍ واحد.
و`tools/check_range.py` بأبوابه الخمسة. وتهيئةُ `test_measure.mjs` (جدول `STATIONS`:
٢٢ نوعاً بملفّاتها وأنواع تمارينها، وإعفاءان مكتوبان) و`test_nodes.mjs` على الرحلة.

**وعناوينُ المراحل والبوابات من `METHOD.md §٣` حرفاً بحرف**، وأما عناوينُ المحطات
فمن عمود **«التمرين الرئيس»** لا عمود «المحطة» («كَمْ تَرَى؟ ١–٣» لا «التقدير الفوري
١–٣»): الخريطةُ يقرؤها الطفلُ ووليُّه، والجدولُ في `METHOD.md` يقرؤه المنهجيّ.

**و«قلبُ الاعتماد» ثبت عملاً**: ظهرت الرحلةُ كاملةً على الخريطة بقفلها التسلسليّ
و**لم يُعدَّل سطرٌ واحد** في `main.js` ولا `progress.js` ولا `app.css` — بيانات المنهج
وحدَها. (اللقطةُ: أحدَ عشرَ قسماً بمعالمها وألوان عائلاتها، ومفتوحٌ منها ١·١ وحدَها.)

**`check_range.py` — نظيرُ المفكوكية** (`METHOD.md §١٠.١`)، خمسةُ أبواب:
١) **بنيةُ الجبهة** · ٢) **تدرّجُها** (لا صفرَ قبل محطته، ولا رمزَ قبل موضعه، ولا
رمزَ عمليةٍ قبل أن تُفعَل بلا رمز، ولا إطارَ عشرةٍ قبل إطار الخمسة) · ٣) **بواباتُ
المعرفة الثلاث** (`METHOD.md §٢.٢`): **كمّاً ← عدّاً ← رمزاً ← عمليةً** مشتقّةً من
ترتيب الرحلة نفسِه — فلا يدخل عمليةً عددٌ لم يُقرَأ رمزُه، ولا يُرسَم رمزُ عددٍ لم
يُعَدّ · ٤+٥) **المولّدات وجولاتُها** (نائمان ذاتياً حتى الجلسة ٣). ويقرأ المنهج
**بتشغيل الوحدة** لا بتعبيرٍ نمطيّ، لأنّ الرحلةَ هنا محسوبةٌ لا مكتوبة.

**ومُجرَّبٌ سالباً — ٣٠ مخالفةً مصنوعة** من المنهج الحيّ نفسِه (نسخةٌ عميقة يُعبَث بها)
يمسكها كلَّها: عددٌ فوق العشرين (ق٣) · رمزٌ في المرحلة الأولى · صفرٌ قبل محطته ·
ضربٌ خارج معجم المستوى · رمزُ عمليةٍ في مرحلة الجسور · أوّلُ عمليةٍ ومعها رمزُها ·
مبعثرٌ قبل النرد · عدٌّ قبل تقديرٍ فوريّ · رمزٌ قبل عدّ عدده · عمليةٌ قبل رمز عددها ·
محطتان بمعرّفٍ واحد · محطةٌ بلا قياسٍ ولا إعفاء · إعفاءٌ بكلمةٍ لا بجملة · سقوطُ
بوابةٍ من الثلاث · محطةٌ لا تصل إلى الخريطة · بوابةٌ تدّعي جبهة… إلخ.

**الفحوص (كلُّها خضراء):** `check_range` ٦ مجموعاتٍ خضراء و١ نائم · `check_range
--self-test` **٣٧/٣٧** (٣٠ منها مخالفةٌ مصنوعة) · `test_selftests` ٣ أدوات (دخل
الفاحصُ الجديد السَّوقةَ **بالجرد** بلا
سطرٍ يُضاف) · `test_nodes` ٥+٢ ونائمٌ واحد · `test_measure` ٦+١+٣ و٢٢ نائماً ·
`test_pwa` ٣٩+١ نائم · `browser_test` **١٥/١٥ في Chrome حقيقي** — ومنها أنّ **حارسَ
الخريطة الفارغة انقلب من تلقائه**: «الرحلةُ فيها ١١ محطةً ولا رسالةَ فراغ» ·
`--device` صفرُ فائضٍ أفقيّ على المقاسات الخمسة · `make_icons --check` ٤/٤ ·
و`read/` لم يُمَسّ من أصوله.

**بلا صوتٍ ولا نشر:** لم يُزَد سطرٌ في `tools/audio_queue.json` ولم يُلمَس `app/audio/`.

<details><summary>بندُ الجلسة كما وُضع</summary>

**المطلوب:**
1. `app/js/curriculum.js`: المراحل الثماني ومحطاتها والبوابات الثلاث كما في `METHOD.md §٣` حرفياً — **كل محطة تعلن جبهتها** (أقصى كمية · أقصى رمز · العمليات · أنماط العرض)، والرحلة تُحسب من البيانات (`journey()`) لا تُكتب بيد، ومحطة التعارف (٨·٥) بإعفائها.
2. **`tools/check_range.py`** (الحارس ١ في `METHOD.md §١٠`): يجرد كل مولّد تمارين بالصنف والحدود ويرفض ما فوق الجبهة — **ومُجرَّب سالباً** بحالات مخالفة مصنوعة، وبه `--self-test`.
3. تهيئة `test_measure.mjs` (جدول `STATIONS` بمفاتيح ليتنر من `METHOD.md §٦`) و`test_nodes.mjs` على الرحلة الجديدة.
4. الخريطة تعرض الرحلة كاملة بالقفل التسلسلي (المحطات هياكل تُفتح وتُغلق، بلا تمارين بعد).

**معايير القبول:** الفاحص يرفض تمريناً فوق الجبهة مُجرَّباً سالباً · عدد العقد محسوب آلياً ويطبعه الحارس · `test_measure` يعرف كل محطة (قياس أو إعفاء بسببه) · فحوص الجلسة ٠ تمرّ.

</details>

**يُرفع إلى مدير المشروع — ولم يُقرَّر ذاتياً (أربعة بنود):**

> **١) حقلٌ سادس في الجبهة: `signs` (رموزُ العمليات) مفصولاً عن `ops`.** نصُّ
> `METHOD.md §١٠.١` أربعةُ حقول (أقصى كمية · أقصى رمز · العمليات · أنماط العرض)، وقد
> صارت ستة: أُضيف `min` (أدنى عدد) و`signs`. **وعلّةُ `signs`** أنّ المنهج نفسَه يفرّق
> بين العملية وعلامتها في موضعين: ٦·١ «آلةُ الجمع **بلا رمز**» عمليةٌ بلا علامة،
> والمرحلة ٥ كلُّها «**لا رمز عملية واحد**» — ولا يُمكن أن يحرسهما حقلٌ واحد. **وعلّةُ
> `min`** أنّ «الصفر متأخر عمداً» (§٢.٦) حدٌّ **سفليّ** لا علويّ، فلا يحرسه سقف.
> والحقلان قراءةٌ للمنهج القائم لا زيادةٌ عليه — فإن رأى المدير غيرَ ذلك فالبديلُ
> دمجُهما في `ops` و`max`، وثمنُه سقوطُ حارسَي ٦·١ والمرحلة ٥ والصفر.
>
> **٢) شرطا نومٍ في حارسَين من البذرة صارا أدقّ** (`SEED.md §٩`): `test_nodes` §٢ صار
> ينام **بالسابقة** لا بالرحلة كلها (وإلا أسقطت الرحلةُ الجديدة الجلسةَ ٣ بذنب
> السابعة)، و`test_measure` §٣ صار على **درجتين** (مفاتيحُ ليتنر تكتبها هذه الجلسة،
> وبانيها تحقنه الجلسة ٣). وكلاهما **يشدّ الحارس**: الأول صار يقيس بالتسجيل في
> الموجِّه لا بوجود ملفٍّ في المجلد. ولا فحصَ أُطفئ ولا حُذف ليمرّ.
>
> **٣) قاعدةٌ لزمت من جدول `STATIONS`: كلُّ نوع شاشةٍ يُنجَز في جلسةٍ واحدة.** لأنّ
> بابَ الشيفرة يطالب النوعَ بأنواع تمارينه كلِّها متى وُجد ملفُّه. ولذلك شُقَّت
> «أيُّهما أكثر» شاشتين (`more` بالكميات في الجلسة ٣، و`compare` بالرموز في الجلسة ٤)
> **بمفهومٍ واحد** (`compare`) ومفتاحين (`compare|5|more` و`compare|10|pick`) — أي أنّ
> **النوع اسمُ شاشةٍ لا اسمُ مفهوم**. وكذلك سكنت محطةُ التعارف ٨·٥ ملفَّها المفرد
> `intro.js`، لأنّ المعفاةَ يجب أن تُثبِت أنّها لا تسجّل مهارةً.
>
> **٤) مدياتٌ ومفاتيحُ اجتهدت فيها الجلسةُ حيث كانت أمثلةُ `METHOD.md §٦` غيرَ حاصرة:**
> `subitize|2|flash` للمحطة ١·١ · `match|5|set` نُسبت إلى ١·٦ («المطابقة الختامية»
> فيها) لا إلى ١·٣ (وهي `match|3|set`) · ٦·٧ و٦·٨ بمفاتيح `add|10|solve` و`sub|10|solve`
> (لا مفتاحَ جديد لهما) بأنواع شاشات `zero` و`fluent` · ٧·٤ تكتب `teen|20|build`
> و`line|20|place` معاً · ٨·٣ بمفتاحَي `measure|length|pick` و`measure|weight|pick`
> و٨·٤ بـ`measure|time|sort`. وحدُّ «لا رمزَ قبل عدّ عدده» **موقوفٌ عند العشرة** لأنّ
> ما فوقها **يُبنى** حزمةً وآحاداً ولا يُعَدّ فرادى (المرحلة ٧) — وهو اجتهادٌ في قراءة
> §٢.٢ يُعرَض للإقرار.

**مراجعة المدير (١١ أغسطس ٢٠٢٦): مقبولة، والبنود الأربعة كلُّها مُقَرّة.** تحقّق
مستقلّ كامل: `check_range` ٦ مجموعات خضراء (٤٩ جبهة تامة، البوابات الثلاث في
مواضعها) و`--self-test` أخضر · `test_selftests` ٣ أدوات (دخل الفاحص بالجرد) ·
`test_nodes` (كاتب `gate` مُقَرّ، ٢١ سابقة نائمة بالسابقة) · `test_measure` (١٥
نوع قياس، نائم بالدرجة الثانية) · `test_pwa` ٣٩ · `browser_test` ١٥/١٥ (الرحلة
١١ قسماً على الخريطة ولا رسالة فراغ) · `--device` صفر فائض · و`read/` لم يُمَسّ.
**وقابلتُ المنهج بـ`METHOD.md §٣` محطةً محطة**: المراحل الثماني بمحطاتها الـ٤٩
(٦+٦+٨+٤+٦+٨+٦+٥) مطابقةٌ بجبهاتها، والصفر في أول محطةٍ تبلغ جبهتُها الصفر (٣·٨
بـ`min: 0`)، و٦·١ عمليةٌ بلا علامة، والمرحلة ٥ صفرُ علامات، و٨·٥ معفاةٌ بنصٍّ
مكتوبٍ بسببه، وقلبُ الاعتماد ثبت (`git diff` يشهد: بيانات المنهج وحدها).
- **إقرار البند ١ (`signs` و`min`)**: قراءةٌ صحيحة للمنهج لا زيادة عليه — والبديل
  (دمجُهما) يُسقط ثلاثة حرّاس منصوصة. **قُيّد تعديلاً معتمداً في `METHOD.md §١٠.١`**
  فبقيت الوثيقةُ الملزمة صادقة.
- **إقرار البند ٢ (شرطا النوم الأدقّ)**: كلاهما **يشدّ** الحارس ولا يرخيه (القياس
  بالتسجيل في الموجّه أصدقُ من وجود ملف)، وتقييدُه في `SEED.md §٩` هو عين عهد
  «ما انكسر تحت الحمل يُصلَح في جلسته ويُقيَّد».
- **إقرار البند ٣ (كل نوع شاشة يُنجَز في جلسة واحدة، والنوع اسمُ شاشة لا مفهوم)**:
  تُرقّى **قاعدةً دائمة** — لولاها طالب بابُ الشيفرة الجلسةَ ٣ بتمارين الجلسة ٤.
  وشقُّ «أيهما أكثر» شاشتين بمفهومٍ واحد (`compare|5|more` · `compare|10|pick`)
  يحفظ وحدة القياس في لوحة وليّ الأمر وليتنر معاً.
- **إقرار البند ٤ (المديات والمفاتيح)**: الاجتهادات كلها ضمن روح §٦ («القائمة
  النهائية يملكها `curriculum.js`»). ووقفُ «لا رمز قبل عدّ عدده» عند العشرة قراءةٌ
  صحيحة لمسارات التعلّم (١١–٢٠ تُبنى عشرةً وآحاداً لا عدّاً فرادى) — **قُيّد
  تعديلاً معتمداً في `METHOD.md §٢.٢`**.
- تنويهُ استحسان: عناوين المحطات من عمود «التمرين الرئيس» لا عمود «المحطة» —
  الخريطة يقرؤها الطفل ووليُّه، والجدول يقرؤه المنهجيّ. تمييزٌ موفَّق.

## ☑ الجلسة ٢ — مصيِّر الكميات وحارسه — **أُنجزت (١١ أغسطس ٢٠٢٦)**

**المنتجات:** `app/js/render.js` — **مصيِّر الكميات** بستّة رسّامين من معجم المنهج
العشرة: **النرد ١–٦** بالتوزيع القياسيّ · **المبعثر بلا تراكب** حتمياً ببذرة ·
**عناصرُ عالم الطفل** (ثمانيةٌ من Twemoji المحلية) · **إطارا الخمسة والعشرة**
و**الإطاران** تُملأ كلُّها **من اليمين**. و`tools/check_render.mjs` (**الحارس ٢**)
مُجرَّباً سالباً، وثمانيةُ فحوصٍ في `browser_test.html` **على الشاشة نفسِها**، ولقطاتٌ
مرجعية لكل نمط (`--render-shots`) **رُوجعت بصرياً كلُّها**.

**والعقدُ الذي يجعل الغلطَ مستحيلاً بنيوياً — شطران:**
`plan()` **هندسةٌ خالصة بلا DOM** تُقرأ في node وتُجرَّب سالباً بخططٍ مكسورة؛
و`paint()` يبني الشكل ثم **يَعُدّ ما رسم من الـDOM نفسِه** (`[data-mark]`) لا من العدد
المطلوب — فيصير الفرقُ **بين النية والفعل مقيساً لا مظنوناً**: رسّامٌ يُسقِط عنصراً
يُعلن ذلك بنفسه فيمسكه الحارس. (وقد جُرِّب: رسّامٌ عُبِث به ليُسقِط عنصراً أظهره
الحارسُ أحمرَ فوراً.)

**و«لا تراكب» برهانٌ بنيويّ لا إحصائيّ:** تُقسَم الساحةُ شبكةَ خلايا لا يقلّ ضلعُها
عن قطر العنصر وفاصله، ويُنتقى منها بالقرعة، ويُزاح مركزُ كل عنصرٍ **داخل خليته
وحدَها** بحيث يبقى قرصُه متراجعاً عن حدودها بنصف الفاصل — فعنصران في خليتين مختلفتين
يفصل بينهما الفاصلُ الأدنى **مهما كانت القرعة**، وشبكةٌ أوسعُ من الحاجة بالنصف تعطي
إزاحةً حرّة فلا تُقرأ الصورةُ شبكةً.

**وحارسان لا واحد، وكلٌّ يمسك ما يفوت الآخر:**
`check_render.mjs` يقرأ الهندسة أعداداً — **٥١٦ شكلاً** (٦ أنماط × مداها × ٦ بذور):
العددُ المرسوم هو المقصود · لا خروجَ عن الصندوق · لا تراكبَ ولا تلاصقَ دون الفاصل ·
الإطارُ يُملأ من اليمين · وجهُ النرد قياسيّ **بجدولٍ مكتوبٍ في الحارس مستقلاً** (مقابلةُ
نسخةٍ بنسختها لا تُثبت شيئاً) · الحتميةُ ببذرة **والتنويعُ بها** (والإطارُ لا يتزحزح:
مرساةٌ لا مفاجأة) · وخارجُ المدى **يُرمى لا يُقرَّب**. وأخوه في المتصفّح يقرأ الرسمَ
بعد أن يقع: **٨٦ شكلاً** على الشاشة، و**٤٢١٠ مقابلةَ تراكبٍ مقيسة**، و٣٨ إطاراً
يُقاس ملؤها من اليمين بالمقاسات المرئية، و٢١٠ صورةً يُثبَت أنّها **حُمِّلت فعلاً**.
وأُثبت الفرقُ عملاً: تنسيقٌ يقلب الشكل أفقياً (`scaleX(-1)`) **تعمى عنه الهندسةُ
ويمسكه القياس**، وصورةٌ محذوفةٌ من القرص كذلك.

**والمراجعةُ البصرية أثمرت تعديلاً**: لقطةُ المبعثر أظهرت أنّ الكميات **بلا حدٍّ تنزّ
إحداها في الأخرى** فلا يُعرَف أين تنتهي هذه وتبدأ تلك — وذلك هدمٌ لِما تقوم عليه
«أيُّهما أكثر» و«طابِقِ الكميتين». فصار للنرد والمبعثر وعناصر عالم الطفل **بطاقةُ
كمية** (وهي بندٌ يُرفع أدناه).

**الفحوص (كلُّها خضراء):** `check_render` ٤ أبوابٍ خضراء و٢ نائم · `check_render
--self-test` **٢٦/٢٦** (منها ١٦ مخالفةٌ مصنوعة) · `test_selftests` **٤ أدوات** (دخل
الحارسُ الجديد السَّوقةَ **بالجرد** بلا سطرٍ يُضاف) · `check_range` ٦ مجموعات و`--self-test`
٣٧/٣٧ · `test_nodes` · `test_measure` · `test_pwa` (٢٢ ملفَّ قشرة) · `browser_test`
**٢٣ فحصاً في Chrome حقيقي** · `--device` صفرُ فائضٍ على المقاسات الخمسة ·
`make_icons --check` ٤/٤ · `fetch_twemoji --check` ٨/٨ والفهرسُ يطابق القرص ·
و`read/` لم يُمَسّ من أصوله.

**بلا صوتٍ ولا نشرٍ ولا شاشة تمرين:** لم يُزَد سطرٌ في `tools/audio_queue.json` ولم
يُلمَس `app/audio/`، ولا شاشةَ محطةٍ واحدة ولا مولّدَ تمارين — فبابا `check_range`
٤+٥ وبابُ `test_measure` الثالث **باقون على نومهم بشرطهم المجرود**.

<details><summary>بندُ الجلسة كما وُضع</summary>

**المطلوب:**
1. وحدة المصيِّر (SVG برمجياً): أنماط **النرد ١–٦** القياسية · **المبعثر بلا تراكب** (حتمي ببذرة، لمنع حفظ النمط) · **إطار الخمسة والعشرة** (يُملأ من اليمين، RTL) · **عناصر عالم الطفل** من Twemoji المحلية (`fetch_twemoji.py` بجرد المستعمَل وحده) بعقد «صدق الصورة».
2. **حارس المصيِّر** (الحارس ٢): المصيِّر يُرجع العدد الذي رسم، والحارس يقابله بالمقصود في كل نمط وكل عدد من ٠ إلى ٢٠ — آلياً في `node` وعلى الشاشة نفسها بعدّة المتصفح.
3. لقطات مرجعية لكل نمط (`--shots`) للمراجعة البصرية.

**معايير القبول:** العدد المرسوم مُثبَت آلياً في كل نمط (٠–٢٠ حيث ينطبق) · لا تراكب في المبعثر مُثبَتاً هندسياً · نمط النرد مطابق للقياسي بصرياً (لقطات) · فحوص ٠–١ تمرّ.

</details>

**يُرفع إلى مدير المشروع — ولم يُقرَّر ذاتياً (خمسة بنود):**

> **١) سطران في `main.js` وزرٌّ في صندوق التجربة: لوحُ المصيِّر تحت `?dev=1`.**
> نصُّ العهد «محطةٌ جديدة تدخل الرحلة بلا سطرٍ يُعدَّل خارج بيانات المنهج»، وقد
> عُدِّل `main.js` بسطر استيرادٍ و`registerScreen('render', renderGallery)` **تحت
> `DEV` وحدَه**. **والعلّة** أنّ `test_pwa` يرفض وحدةَ جافاسكربت لا تصل إليها شجرةُ
> `main.js` («لا ملفٌّ ميّت يُنزَّل إلى جهاز طفلٍ ولا يُستعمَل») — والمصيِّرُ لا مستهلكَ
> له قبل الجلسة ٣، فكان الخياران: وصلُه بلوح مراجعةٍ في وضع التجربة، أو **إطفاءُ
> حارسٍ من البذرة** — والثاني ممنوع. وزيادةً على ذلك: اللوحُ هو ما تُلتقط منه
> اللقطاتُ المرجعية **من التطبيق نفسِه** (بلوحه وخطّه واتجاهه) وما يقيس عليه حارسُ
> المتصفّح — فلا صفحةَ عرضٍ مصنوعة تُراجَع بدل ما يراه الطفل. وهو **ليس محطةً**: لا
> عقدةَ له في الرحلة ولا نجمةَ يكتبها، ولا طريقَ إليه من شاشة طفلٍ لأنّ التسجيلَ نفسَه
> لا يقع إلا تحت `?dev=1`. **وقيَّدتُه في `SEED.md §١٠`** (سجلٌّ جديد: ما تحرّك في
> وحدات البذرة بعد نسخها) — فإن رأى المدير غيرَ ذلك فالبديلُ تأجيلُ الوحدة كلِّها إلى
> الجلسة ٣، وثمنُه أن تُسلَّم الجلسةُ ٢ بلا مصيِّرٍ في الشجرة.
>
> **٢) بطاقةُ الكمية — زيادةٌ على `DESIGN.md §٥` أظهرتها المراجعةُ البصرية.** الدستورُ
> يذكر أربعةَ أنماطٍ بقواعدها ولا يذكر حدّاً للساحة، فرُسمت أولاً بلا حدّ — فأظهرت
> اللقطةُ أنّ كميتين متجاورتين **تنزّان إحداهما في الأخرى**. فصار للنرد والمبعثر
> وعناصر عالم الطفل ورقةٌ هادئة تحدّهما، وحدُّها **ظلٌّ داخليّ لا إطار** (الإطارُ يقتطع
> من الصندوق فتزيح الهندسةُ عن حسابها). وهي قراءةٌ لِما تقتضيه «أيُّهما أكثر» لا زيادةٌ
> على المنهج — **وقُيّدت في `DESIGN.md §٥`** بانتظار الإقرار.
>
> **٣) مدى كلّ نمطٍ معلَنٌ فيه، وخارجُه يُرمى لا يُقرَّب.** نصُّ البند «كل عدد ٠–٢٠
> **حيث ينطبق**»، وقُرئ ذلك أنّ لكلّ نمطٍ مداه: النرد ١–٦ (**لا وجهَ نردٍ لصفر**) ·
> إطارُ الخمسة ٠–٥ · إطارُ العشرة ٠–١٠ · الإطاران والمبعثرُ وعناصرُ عالم الطفل ٠–٢٠.
> و**طلبُ كميةٍ خارج المدى يرمي خطأً** ولا يُقرَّب إلى أقرب ما يستطيع: كميةٌ لا يستطيع
> النمطُ رسمَها خطأُ برمجةٍ يجب أن يصرخ، لا أن يُرسَم غيرُها فيُقاس الطفلُ على ما لم
> يُقصَد. ويحرس الحارسُ أنّ **مجموع المديات يبلغ سقفَ المستوى** (٢٠) المقروءَ من
> المنهج لا مكتوباً فيه.
>
> **٤) عناصرُ عالم الطفل تُرسَم HTML لا SVG.** نصُّ `DESIGN.md §٥` «الكميةُ تُرسم
> برمجياً (SVG)»، ونصُّ §٥.٤ نفسِه «والمصيِّر واحدٌ (`faceEl`)» — والثاني يقتضي
> `<img>` (لا يسكن SVG إلا بـ`foreignObject`). فرُجّح **وحدةُ مصيِّر الرموز**: الهندسةُ
> تُحسب بالخوارزمية نفسِها ثم تُعبَّر نسبةً مئوية من الصندوق، والرمزُ يمرّ من `faceEl`
> وحدَه — فلا موضعَ ثانٍ في التطبيق يحوّل رمزاً إلى صورة. والنردُ والمبعثرُ والإطاراتُ
> كلُّها SVG كما نصّ الدستور.
>
> **٥) أربعةُ أنماطٍ من العشرة بلا رسّامٍ بعد، ومحطتان بلا نمطٍ مرسوم — نائمةٌ بالجرد.**
> `numeral` و`line` (الجلسة ٤) و`pattern` و`scene` (الجلسة ٧) ليست كمياتٍ تُعَدّ بل
> بطاقةُ رمزٍ وخطُّ أعدادٍ وشريطُ نمطٍ ومشهدُ قياس، وهي خارج حدود هذه الجلسة نصّاً.
> فشرطُ نومها **جردُ الفرق بين معجم المنهج ورسّامي المصيِّر** لا رايةٌ تُضبط بيد،
> ويستيقظ كلُّ نمطٍ يومَ يُكتب رسّامُه. وكذلك محطتا `line:ten` و`intro:latin`: جبهتُهما
> رمزٌ وخطٌّ لا كمية، فلا نمطَ مرسومٌ فيهما يُقاس عليه بعد.

**مراجعة المدير (١١ أغسطس ٢٠٢٦): مقبولة، والبنود الخمسة كلُّها مُقَرّة.** تحقّق
مستقلّ كامل: `check_render` بأبوابه و`--self-test` · `test_selftests` ٤ أدوات ·
`check_range` و`test_pwa` و`test_nodes` و`test_measure` كما كانت · `browser_test`
٢٣/٢٣ في Chrome حقيقي (٨٦ شكلاً، ٤٢١٠ مقابلة تراكب، ٣٨ إطاراً، ٢١٠ صور محمَّلة) ·
`--device` صفر فائض · و`read/` لم يُمَسّ. **وعينُ المدير على اللقطات الست**: النرد
قياسيّ الوجوه بشبكة ٣×٣ · المبعثر نظيف ببطاقاته والفصل بيّن · عناصر عالم الطفل
متجانسة **وعُدَّت بطاقةً بطاقة حتى ١١ فصحّت كلُّها** · وإطارات الخمسة والعشرة
والإطاران تُملأ من اليمين، الصفُّ العلوي أولاً، والإطاران يملآن الأيمن (العشرة)
قبل الأيسر (الآحاد) — مطابقةً لعقد `METHOD.md §٣`.
- **إقرار البند ١ (سطرا `main.js` تحت `DEV`)**: عهدُ «بلا سطرٍ يُعدَّل» عهدُ
  المحطات تدخل الرحلة، واللوحُ ليس محطة — وإطفاءُ حارسٍ من البذرة هو الممنوع
  الأكبر. والقيدُ في `SEED.md §١٠` (سجلٌّ جديد صائب) يحفظ الذاكرة. **وشرطُه
  الدائم**: لا طريقَ إلى اللوح إلا تحت `?dev=1` — يبقى محروساً.
- **إقرار البند ٢ (بطاقة الكمية)**: المراجعة البصرية عملت عملَها — كميتان بلا حدٍّ
  هدمُ «أيُّهما أكثر» قبل أن تُبنى. والظلُّ الداخلي لا الإطار (كي لا تزيح الهندسة
  عن حسابها) قرارٌ دقيق. **قيدُ `DESIGN.md §٥` مُقَرّ**.
- **إقرار البند ٣ (خارج المدى يُرمى لا يُقرَّب)**: إقرارٌ مؤكَّد — التقريبُ الصامت
  عينُ الصنف الذي وُجد حارسُ المصيِّر ليمنعه: طفلٌ يُقاس على ما لم يُقصَد.
- **إقرار البند ٤ (العناصر HTML عبر `faceEl`)**: الترجيح صحيح — علّةُ «SVG» الدقةُ
  المضمونة وهي محروسة بالعدّ من الـDOM لا بتقنية الرسم، وعقدُ «المصيِّر واحد» أثمن.
  **قُيّد تعديلاً معتمداً في `METHOD.md §١٠.٢`** مع البندين ٢ و٣.
- **إقرار البند ٥ (الأنماط الأربعة نائمة بالجرد)**: عينُ نمط النوم الذاتي المعتمد.
- تنويهُ استحسان: شطرا `plan()`/`paint()` والعدُّ من `[data-mark]` — جعلا الفرق
  بين النية والفعل **مقيساً**، وأثبتته المخالفات الثلاث المصنوعة على الحيّ
  (القلبُ بالتنسيق تعمى عنه الهندسة ويمسكه القياس). هذا فوق نصّ البند، وهو
  الحارس الذي وُعد به في `METHOD.md` حرفاً.

## ☑ الجلسة ٣ — المرحلتان ١–٢ (كمٌّ وعدّ) + المسار الحيّ — **أُنجزت (١١ أغسطس ٢٠٢٦)**

**المنتجات:** أربعُ وحداتٍ جديدة في `app/js/` — **`station.js`** (حلقةُ المحطة الأربعية
ومعالجةُ الخطأ الموحَّدة وطبقةُ اللمس وحقنُ المراجعة) · **`quantity.js`** (المرحلة ١:
`subitize` · `match` · `more`) · **`counting.js`** (المرحلة ٢: `count` بالعدديّة
و«أعطني ن» والبعثرة، و`equal`) · **`registry.js`** (سِجلُّ الشاشات منقولاً من `main.js`
بعلّةٍ بنيوية، §«يُرفع» أدناه). و**`tools/check_speech.mjs`** حارساً جديداً. و**٢٥ نصّاً
منطوقاً** مشكولاً بالكامل في `tools/audio_queue.json` (١٠ أسماءِ أعداد · ٩ تعليمات ·
٤ نمذجة · ٢ احتفال) — **ولم يُلمَس `app/audio/` ولا وُلِّد صوت**.

**والمرحلتان تُجتازان لمساً**: يُثبته حارسُ المتصفّح بسَوق محطةٍ إلى آخرها — شاهِدْ ثم
جرِّبْ معي ثم وحدك — حتى الاحتفال والنجوم الثلاث.

**العهودُ الأربعة التي بُنيت عليها الشاشات:**
1. **صفرُ رمزٍ عدديّ** في المرحلتين: خطواتُ الحلقة **أسماؤها** لا أعدادُها، وجولاتُها
   **خرزاتٌ تمتلئ**، وجوابُ كل تمرينٍ **بطاقةُ كمّ** — حتى سؤالُ العدديّة «كم صارت
   كلها؟» يُجاب بكمٍّ لا برقم. ويجرد حارسُ المتصفّح النصَّ المعروض على شاشتين حيّتين.
2. **التناظرُ الفرديّ امتناعٌ في البنية لا تحقّقٌ بعده**: زرُّ العنصر الملموس
   **يُعطَّل**، فلا سبيلَ إلى عدّ الواحد مرّتين ولو ألحّ الإصبع. (والحارس يلمس عنصراً
   مرّتين ويعدّ.)
3. **الشاشةُ لا تدّعي عدداً**: جوابُ كل جولةٍ يُقرأ من **ما رسمه المصيِّر** (`drawn`)
   لا من العدد الذي طُلب — امتدادُ عقد الجلسة ٢ إلى شاشات الجلسة ٣.
4. **الخطفُ طبيعةُ تمرين**: تُغطّى الكميةُ بعد لحظة، ومعها «أَعِدِ الْعَرْض» **بلا حدٍّ
   ولا احتساب** — ولا عدّادَ ينزل ولا شريطَ يفرغ (يفحصهما الحارس).

**ومعالجةُ الخطأ واحدةٌ في التمارين الستة**: الخيارُ الخاطئ **لا ينقل** — هزّةٌ ثم
**تُعَدّ الكميةُ المعنية أمامه عنصراً عنصراً** بالصوت والتعليم، ثم يحاول ثانية. ولا
تلقينَ قبل المحاولة، ولا شاشةَ خطأ، ولا عقاب.

**و«جرِّب معي» غيرُ مقيسةٍ في ليتنر** — نصُّ `METHOD.md §٤`: «وحدك … وهي المقيسة».
فلا يُحاسَب الطفلُ على خطوةِ تعلُّمٍ بعون.

**والمولّدات حتميةٌ ببذور تعلن ما تستهلك**: `CONSUMES` (أرضيةُ ما تشترك فيه جبهاتُ
محطات النوع) و`probeRounds` (**النمذجةُ والعونُ و«وحدك» كلُّها**، فلا شكلٌ يفلت من
الجرد) — فاستيقظ بابا `check_range` ٤+٥: **٣٩٢ جولةً مولَّدة كلُّها تحت جبهاتها**.

**الفحوص (كلُّها خضراء):** `check_range` ٦ مجموعاتٍ خضراء و**صفرُ نائم** (بابا
المولّدات استيقظا) و`--self-test` ٣٧/٣٧ · `check_speech` ٤ أبواب و`--self-test` مُجرَّباً
سالباً · `test_selftests` **٥ أدوات** (دخل الحارسُ الجديد السَّوقةَ **بالجرد** بلا سطرٍ
يُضاف) · `test_measure` (٥ أنواع تكتب قياسَها، و٦ أنواع تمارينَ تُنتج تمارينَها في
المراجعة، و٢٥ نائماً بالنوع) · `test_nodes` (٦ سوابق من ٢٢ لها كاتبُ نجمة) · `test_pwa`
(٢٦ ملفَّ قشرة، ١٣ وحدةً كلُّها حيّة) · `check_render` كما كان · `browser_test`
**٣٩ فحصاً في Chrome حقيقي** · `--device` **صفرُ فائضٍ على المقاسات الخمسة — والقياسُ
الآن على شاشات التمارين لا الخريطة وحدَها** · `make_icons --check` ٤/٤ ·
`fetch_twemoji --check` ٨/٨ · و`read/` لم يُمَسّ.

**والمراجعةُ البصرية أثمرت تعديلين**: نمذجةُ الكميتين (المقارنةُ والتسوية) كانت
تأخذ مقاسَ البطل الواحد فتخرج الثانيةُ عن الشاشة **فلا يُرى الطرفان معاً وهو المقصود**
— فأخذت مقاسَ المقارنة؛ وأثرُ العدّ على عناصر عالم الطفل كان ظلّاً لا يكاد يُرى —
فصار **حلقةً وحجماً** مع اللون (اللون لا يحمل معنىً وحدَه).

**وأربعةُ مواضعَ انكسرت من البذرة والعدّة تحت أوّل شاشةِ تمارين، وأُصلحت وقُيّدت في
`SEED.md §٩`** — وكلُّها تشدّ ولا تُرخي: `test_pwa` كان يَعُدّ الوحدةَ المستوردة لأثرها
**ميتة**؛ و`test_measure` §٣ كان يطالب بالأنواع الخمسةَ عشرَ دفعةً (وكان يقيس بلا أن
يستورد وحدات التمارين أصلاً)؛ و`browser_test.html` كانت **تموت صامتة** فيُقرأ ذلك
بطءَ جهاز (وقد وقع مرّتين في هذه الجلسة)؛ و`browser_device.html` كان يشهد بسلامة ما
لم يقسه.

<details><summary>بندُ الجلسة كما وُضع</summary>

**المطلوب:** شاشات تمارين المرحلتين (كم ترى؟ · مطابقة · أيهما أكثر · المس وعُدّ · اجعلهما سواء · أعطني ن) بحلقة المحطة (`METHOD.md §٤`) ومعالجة الخطأ المنصوصة (العدّ أمامه لا التلقين) والنجوم بعتباتها المتناسبة، وربطها بالخريطة والقفل. الخطف بصياغة «شاهِد ثم أجب» وزرّ الإعادة بلا حدّ. النصوص المنطوقة إلى `audio_queue.json` والاحتياط الآلي مؤقتاً.
**معايير القبول:** المرحلتان قابلتان للاجتياز كاملتين لمساً على آيباد · صفر رمز عددي على شاشات المرحلة ١ (يفحصه حارس المتصفح) · التناظر الفردي مفروض بنيوياً (لا احتساب عنصر مرتين) · `check_range` يمرّ على كل جولة مولَّدة · **ميدان: طفل حقيقي يجتاز الكمّ بلا قراءة — بلاغ ميدان موثَّق**.

</details>

**ما لم يُنجَز من بند الجلسة، ولا يملك إنجازَه من في الشجرة:** **بلاغُ الميدان**
(«طفل حقيقي يجتاز الكمّ بلا قراءة») — يحتاج طفلاً وجهازاً ووليَّ أمر، وهو موعدُ
المالك لا موعدُ جلسةٍ برمجية. والشجرةُ مهيّأةٌ له: المرحلتان تُجتازان لمساً على
مقاسات الآيباد الخمسة بلا فائض. **ويُرفَع الأمرُ إلى المدير ليقرّر موضعَه** — أفي
الجلسة ١٠ (ميدان الرحلة المتقدمة) أم في موعدٍ أقرب على المرحلتين وحدَهما.

**يُرفع إلى مدير المشروع — ولم يُقرَّر ذاتياً (خمسة بنود):**

> **١) وحدةٌ جديدة `registry.js`، وسطرا استيرادٍ في `main.js` لوحدتَي التمارين.**
> عهدُ البذرة «**الشاشةُ تسجّل نفسَها**»، وقد ظهر عند أوّل شاشةٍ حقيقية أنّه **لا
> يستقيم والسجلُّ في `main.js`**: وحدةُ التمارين تحتاج `registerScreen` منه، وهو
> يحتاج تحميلَها — فيقع **دَورٌ في الاستيراد** يُقيَّم فيه جسمُ وحدة التمارين قبل جسم
> `main.js` فتسقط على `screens` في منطقة الموت؛ ثمّ إنّ **عقد المولّد النقيّ** في
> `check_range.py` يمنع استيراد `main.js` نصّاً («ولا تستورد `main.js`»). فنُقل
> السجلُّ إلى وحدةٍ محايدة يستوردها الطرفان. والبديلُ الوحيد أن يعرف `main.js` كلَّ
> شاشةٍ باسمها — وهو عينُ ما فكّه قلبُ الاعتماد. **وسطرا الاستيراد سطرٌ لكل وحدةٍ لا
> لكل محطة**: محطاتُ الأنواع الخمسة تدخل الرحلةَ بلا سطرٍ يُعدَّل، وإنما تحتاج
> **الوحدةُ الجديدة** أن تصلها شجرةُ الاستيراد كي لا تكون ملفاً ميّتاً يُنزَّل ولا
> يُستعمَل. وقُيّد الأمرُ كلُّه في `SEED.md §٢` و§١٠.
>
> **٢) «صفّان متحاذيان» في معالجة خطأ المقارنة — مؤجَّلٌ برفعٍ لا بإغفال.** نصُّ
> `METHOD.md §٤`: «وفي المقارنة يُصفّ الجمعان صفّين متحاذيين فيُرى الفارق». والتحاذي
> عنصراً بعنصر يقتضي **نمطَ صفٍّ واحد** — وهو `five-frame`، ولا تُتيحه جبهةُ المرحلة ١
> (أنماطُها `dice` و`scatter`)، **وإطارُ الخمسة يُقدَّم درساً في ٣·٣** فاستعمالُه هنا
> يستبق تقديمَه. فنُفِّذ **الأصلُ المنصوص في §٤ نفسِه**: تُعَدّ الكميتان أمامه واحدةً
> بعد الأخرى فيسمع «واحد اثنان» هنا و«واحد اثنان ثلاثة» هناك. **والقرارُ للمدير**:
> إمّا أن يبقى العدُّ أمامه كافياً في المرحلة ١ ويدخل التحاذي من المرحلة ٣ (وهو
> ترجيحُ الجلسة)، وإمّا أن يُقَرّ إدخالُ نمطِ صفٍّ إلى جبهة المرحلة ١ — وهو **تعديلُ
> منهج** لا تعديلُ شاشة.
>
> **٣) «أعطني ن» بنموذجٍ كمّيّ مرئيّ لا بالصوت وحدَه.** المرحلة ٢ **بلا رمز**، فلا
> سبيل إلى عرض «ن» رقماً؛ والصوتُ وحدَه لا يكفي اليوم (البنكُ لم يُولَّد بعد، والاحتياطُ
> نطقٌ آليّ). فيُعرَض المطلوب **بطاقةَ كمٍّ** ويُنطَق اسمُه، ويُطالَب الطفلُ بأن **يبني
> كمّاً بقدره** من مَبْسَطٍ أوسع. وقُرئ ذلك أنّه عينُ المقيس («يلتقط ن عنصراً» —
> إنتاجُ كمٍّ بقدرٍ معلوم، وهو التناظرُ الفرديّ من جهته الأخرى) لا مطابقةً بصرية.
> فإن رأى المدير غيرَ ذلك فالبديلُ إخفاءُ النموذج في «وحدك» والاكتفاءُ بالصوت — ويُؤجَّل
> إلى ما بعد الجلسة **ص**، وإلّا صار التمرينُ غيرَ قابلٍ للإجابة اليوم.
>
> **٤) مدياتٌ ومقاديرُ اجتهدت فيها الجلسةُ حيث سكت المنهج:** «وحدك» **خمسُ جولات**
> (و**ستٌّ** في محطتين تدرّسان مهارتين) — و§٤ يقول «٤–٦»؛ و«جرِّب معي» **جولتان** كما
> نصّ؛ ونجومُ المحطة `errors ≤ ⌈الجولات/٢⌉` ← نجمتان، وهي قراءةُ «زلّة لكل جولتين»
> حرفاً؛ و**التركيزُ في محطات العدّ ثلثُ الجبهة الأعلى** (فمحطةُ «٦–٧» جبهتُها ١–٧
> وموضوعُها ٦ و٧) **محسوباً من الجبهة لا مكتوباً بيد**؛ ومهلةُ الخطف ١٫٢ ثانية
> والإعادةُ بلا حدّ.
>
> **٥) حارسٌ جديد لم يطلبه البند: `check_speech.mjs`.** علّتُه أنّ بروتوكول الصوت
> **يجعل النسيانَ صامتاً**: نصٌّ يُنطق ولا يُصفّ في القائمة يعمل التطبيقُ معه وتبقى
> الاختباراتُ خضراء ويحتاط `audio.js` بالنطق الآليّ — فينكشف في أذن طفلٍ لا في شجرة،
> وهو عينُ صنف «حراسة الغياب» الذي وُلد منه `test_measure`. ويحرس معه **قيدَي المنهج
> المنصوصين في `AUDIO_QUEUE.md`** (لا رقمَ منطوق — ق١، ولا معدودٌ مقرونٌ بعدد — ق٢)
> **والشكلَ الكامل**. وهو زيادةٌ على البند، فيُعرَض للإقرار أو الردّ.

**مراجعة المدير (١١ أغسطس ٢٠٢٦): مقبولة تقنياً، والبنود الخمسة كلُّها مُقَرّة —
والميدانُ معلَّقٌ بقرارٍ مقيَّد أدناه.** تحقّق مستقلّ كامل: `check_range` صفرُ نائم
(٣٩٢ جولة تحت جبهاتها) · `check_speech` بأبوابه و`--self-test` · `test_selftests`
٥ أدوات · `test_measure` و`test_nodes` و`test_pwa` و`check_render` خضرٌ ·
`browser_test` ٣٩/٣٩ في Chrome حقيقي (منها اجتياز محطة كاملةً لمساً، ولمستان على
عنصرٍ عدَّتاه مرة) · `--device` صفر فائض · و`read/` لم يُمَسّ. **وعينُ المدير على
الشاشات الخمس**: الحلقة بأسمائها لا أعدادها، بطاقاتُ كمٍّ بلا رقمٍ واحد، أثرُ العدّ
حلقةً وحجماً ولوناً، والمقارنةُ والتسوية يُرى طرفاهما معاً، وزرّا «تابِع» و«أعِد»
يبلغان الهدف.
- **إقرار البند ١ (`registry.js`)**: دَورُ الاستيراد علّةٌ بنيوية حقيقية والحلّ
  قياسيّ صحيح، والبديل (عودةُ `main.js` يعرف شاشاته) نقضٌ لقلب الاعتماد المُقَرّ.
  والعهدُ يُقرأ من الآن: **محطةٌ جديدة بلا سطر؛ ووحدةُ تمارين جديدة سطرُ استيرادٍ
  واحد** — وقد قُيّد في `CLAUDE.md`.
- **قرار البند ٢ (صفّان متحاذيان)**: **يبقى العدُّ أمامه في المرحلتين ١–٢، والتحاذي
  من المرحلة ٣** — إدخالُ نمط صفٍّ إلى جبهة المرحلة ١ يعرض بنيةَ الإطار قبل درسها
  (٣·٣) وهو خرقُ عقد التقديم نفسِه. **قُيّد تعديلاً معتمداً في `METHOD.md §٤`**.
- **إقرار البند ٣ («أعطني ن» بنموذج كمّيّ)**: في مرحلةٍ بلا رمزٍ النموذجُ الكمّيّ
  هو اللسانُ الوحيد الصادق، وإنتاجُ كمٍّ بقدر كمٍّ معروضٍ هو التناظر الفردي من جهة
  الإنتاج. **وقرارٌ مؤجَّل بنصّه**: بعد اكتمال الصوت يُعاد النظر — هل يُخطَف النموذج
  في «وحدك» فيصير الالتقاط عن ذاكرة (قُيّد في بند الجلسة ص).
- **إقرار البند ٤ (المديات)**: كلها داخل نصوص §٤، والتركيزُ على ثلث الجبهة الأعلى
  محسوباً قراءةٌ موفّقة لعمود «ما يتقنه» (محطة ٦–٧ موضوعها ٦ و٧ لا إعادة ١–٥).
- **إقرار البند ٥ (`check_speech.mjs`)**: إقرارٌ مؤكَّد — نصٌّ منطوق خارج القائمة
  ينكشف في أذن طفلٍ لا في شجرة، وهذا عين «حراسة الغياب»؛ وحراستُه ق١–ق٢ آلياً
  تجعل قرارَي المالك محروسَين لا موعودَين.
- **قرار الميدان**: **موعدُه الآن على المرحلتين ١–٢، لا الجلسة ١٠** — درسُ اقرأ أن
  بلاغ الميدان المبكر يرى ما لا يراه عشرون حارساً. **لكن قبله الدفعةُ الصوتية
  الأولى** (الجلسة ص تصير دفعات — قرارٌ في بندها): طفلُ الرابعة لا يُمتحَن له تطبيقٌ
  بصوت نطقٍ آليّ، فيلتبس عيبُ الصوت بعيب التصميم ويفسد البلاغ. الترتيب: دفعةُ
  الصوت (٢٥ نصاً) ← ميدانُ المالك على المرحلتين ← بلاغُه يُقيَّد في `docs/` قبل
  فتح الجلسة ٥ (والجلسة ٤ لا تنتظر الميدان — رموزُها لا تعتمد بلاغَه).

## ☐ الجلسة ٤ — المرحلتان ٣–٤ (رموز ومقارنة) + ليتنر بمفاتيحه

**المطلوب:** حلقة تقديم الرمز (كمية ← عدّ ← رمز ← مطابقة ذهاباً وإياباً) لمحطات ٣·١–٣·٨ بالصفر متأخراً · تمارين المقارنة والترتيب وخط الأعداد ٠–١٠ · تفعيل القياس بمفاتيح `METHOD.md §٦` في كل محطات ١–٤ (رجعياً للمرحلتين ١–٢) · المراجعة اليومية (٦ تمارين، الأضعف أولاً).
**معايير القبول:** كل محطة تكتب قياسها (يثبته `test_measure` على الرحلة الحية) · مفتاح `numeral` لكل رمز على حدة · المراجعة اليومية تعمل من سجلّ حقيقي · فحوص ٠–٣ تمرّ.

## ☐ الجلسة ٥ — ركِّب وفكِّك + 🚪 بوابة ١

**المطلوب:** محطات المرحلة ٥ («اصنع العدد» بطريقتين، جسور في الإطارات، أصدقاء العشرة) — **لا رمز + أو − فيها** يفرضه الفاحص · بوابة ١ بنموذج اقرأ (`gate.js`: أضعف المهارات، عشرة تمارين، ≥٨٠٪، لا رسوب، إعادة فورية).
**معايير القبول:** البوابة تُبنى من أضعف مهارات ليتنر فعلاً (فحص بسجلّات مصنوعة) · لا رمز عملية في المرحلة ٥ (سالباً) · فحوص ٠–٤ تمرّ.

## ☐ الجلسة ٦ — الجمع والطرح ضمن ١٠ + 🚪 بوابة ٢

**المطلوب:** محطات المرحلة ٦ (آلة الجمع بلا رمز أولاً، ثم الرمزان، العدّ التصاعدي من الأكبر، الطرح إزالةً وفرقاً، الصفر في العمليات، الطلاقة) · حقائق العمليات كلها إلى ليتنر · بوابة ٢.
**معايير القبول:** كل حقيقة تُعرَض كميةً ورمزاً معاً أول لقاء (يفحصه حارس المتصفح) · حقائق ضمن ١٠ في ليتنر بمفاتيحها · فحوص ٠–٥ تمرّ.

## ☐ الجلسة ٧ — ١١–٢٠ والأنماط والقياس + 🚪 بوابة ٣

**المطلوب:** محطات المرحلة ٧ (العشرة وحدةً، الإطاران، العبور صعوداً ونزولاً) والمرحلة ٨ (الأنماط، القياس الوصفي، **محطة تعارف المغربية بإعفائها المكتوب**) · بوابة ٣ من أضعف الرحلة كلها.
**معايير القبول:** **الرحلة كاملة قابلة للاجتياز** — محاكاة آلية تجتاز كل عقدة من الصفر حتى الختام · عدد العقد النهائي يطبعه الحارس ويطابق الخريطة · فحوص ٠–٦ تمرّ.

## ☐ الجلسة ٨ — لوحة وليّ الأمر + النسخ الاحتياطي

**المطلوب:** اللوحة خلف البوابة الحسابية (بنية `parent.js` المنسوخة): **مفهومٌ بعملية لا درجة**، بأقسام الحساب (كمّ · عدّ · رموز · مقارنة · جسور · عمليات · أنماط) ودقائق الاستخدام وتوصية اليوم وعبارة «راجِع مختصاً» عند التعثّر المتكرر (بحدود `METHOD.md §١٣`) · النسخ الاحتياطي والاستعادة (نموذج `progress.js` في اقرأ).
**معايير القبول:** كل قسم يقرأ من ليتنر الحيّ (لا رقم مكتوب بيد) · التصدير والاستيراد يحفظان الرحلة كاملة · فحوص ٠–٧ تمرّ.

## ◐ الجلسة ص — الصوتيات (دفعاتٍ لا دفعةً واحدة) — **الدفعة الأولى صُرِّفت وراجعتها أذنُ المالك فأقرّتها (١١ أغسطس ٢٠٢٦)**

> **قرار المدير (١١ أغسطس ٢٠٢٦، مراجعة الجلسة ٣):** تُعقد الجلسة ص **دفعاتٍ** كلما
> امتلأت القائمة بما يكفي ميداناً أو مرحلة — **وأولاها الآن** (٢٥ نصّ المرحلتين ١–٢):
> فالميدان المبكر قرارُنا، وطفلُ الرابعة لا يُمتحَن تطبيقٌ له بصوت نطقٍ آليّ.
> الدفعة الأولى تنسخ عدّة التوليد الأربعة من اقرأ (SEED §٤) وتقيّدها في `SEED.md`.

**المطلوب (في كل دفعة):** تصريف `tools/audio_queue.json` بصوت **سُلافات** · مراجعة بشرية بالأذن عبر `audio_panel.py` · بصمات المحتوى في `versions.json` · تحديث احتياط «لا نطق آلي» ليعود صارماً على المصروف.
**معايير القبول:** الدفعة مراجَعة بالأذن كاملة · القائمة فارغة · `test_audio_cache` أخضر (يستيقظ مع أول `manifest.json`) · فحص «لا لجوء للنطق الآلي» صارم على كل شيء.
**وبعد اكتمال الصوت** (قرار مؤجَّل بنصّه — مراجعة الجلسة ٣، البند ٣): يُعاد النظر في «أعطني ن»: هل يُخطَف نموذجُ الكمّ في «وحدك» بعد سماع الاسم فيصير الالتقاط عن ذاكرةٍ لا مطابقةً بصرية — يُقرَّر يومها بمعاينة.

### الدفعة الأولى (١١ أغسطس ٢٠٢٦) — المنتجات

**العدّةُ الخمس منسوخةٌ ومقيَّدةٌ بالبصمات في `SEED.md §٣`:** `generate_audio.py`
(`read@1ccbda9`) · `audio_panel.py` و`trim_lead.py` (`42b1a50`) · `queue_texts.mjs`
(`77a14b3`) · `test_audio_cache.mjs` (`41f8445`). ونحو ٣٠٠٠ سطرٍ في اقرأ صارت نحو
١٢٠٠ — **لأنّ ما نُسخ اليوم يشير إلى منهجٍ وبنكٍ قائمين**، فسقط كلُّ ما كان يشير إلى
ما لا وجود له (مفاضلاتُ الأصوات، احتياطُ المدود، صمامُ السياسة، الرسمُ الصوتيّ، نسبُ
Antura والتلاوة، محرّكُ edge، ونماذجُ اقرأ الثلاثة). **وصحَّ التأجيلُ عملاً.**

**والبنكُ مصروف: ٢٥ ملفاً بصوت سُلافات** في `app/audio/` (٤٧ ثانية سماعاً · ٣٦٦
كيلوبايت) ومعها `manifest.json` و`versions.json` ببصمات البايتات. **صفرُ فشل، وصفرُ
محجوز، والقائمةُ فارغة.**

**والدروسُ المدفوعُ ثمنُها في اقرأ نُقلت بحرفها**: مفاتيحُ لا تُطبع · حصةٌ مستقلّة
لكل مفتاح (وقد تناوب المفتاحان ١١ و١١) · سقفٌ ذاتيّ يوميّ لا يعتمد على الخادم ·
إيقاعٌ يخنق كلَّ طلب **ويعدّه** · `EmptyAudio` تُعاد مرّتين لا خمساً · قصُّ صمت
الطرفين في الأنبوب · الفهرسُ والبصماتُ **بعد كل ملف** فلا لقطةَ كاذبة إن توقّف
التصريف · `mark_done` **دمجاً لا استبدالاً** · وسلفُ كل ملفٍ محفوظٌ قبل الكتابة فوقه.

**والحارسُ عند البوّابة وعند الصندوق معاً** (نصُّ `AUDIO_QUEUE.md`): ق١ (لا رقمَ
منطوق) وق٢ (لا معدودٌ مقرونٌ بعدد) يُفحصان **قبل أيّ طلب**، والمخالفُ يُحجَز بعلّته
مكتوبةً في مدخله — والدوالُّ مكتوبةٌ في المصرِّف **مستقلّةً** عن `check_speech.mjs`
(بايثون وجافاسكربت)، فمقابلةُ نسخةٍ بنسختها لا تُثبت شيئاً.

**ولوحةُ السماع مبنيّةٌ ومعروضةٌ على المالك** — ٢٥ ملفاً في أربعة أقسام، تشغيلٌ
بالتتابع وسجلُّ ما سُمع وزرُّ بلاغٍ ينسخ نصوصاً تُمرَّر إلى `--requeue` مباشرة.
**والمراجعةُ بالأذن بشرية، لم تُقرّها هذه الجلسة** (§البروتوكول ٣).

**وتنبيهٌ واحدٌ للأذن يقدّمه الحارس**: «كَمْ تَرَى؟» ٠٫٩٦ث = ٠٫٤× وسيطَ فئتها — أقصرُ
من نظائرها، يُسمَع لاحتمال نطقٍ مبتور. (وما قبِلته الأذنُ بعلّةٍ يُقيَّد بـ`--verdict`
فيصير التنبيهُ خبراً لا مطالبة.)

**واحتياطُ النطق الآليّ لم يعد يعمل لشيء، والفحصُ عاد صارماً بلا تعديلٍ في الصفحات**:
`browser_test` صار يبدّل `Audio` و`SpeechSynthesisUtterance` في نافذة التطبيق فيقيس
**مسار الطفل نفسَه** — ٣١ تشغيلاً من البنك و**صفرُ لجوءٍ** إلى النطق الآليّ، والمنتظِر
في `/__queue.json` صفر. **ومُجرَّبٌ سالباً**: أُسقط مفتاحٌ من الفهرس مؤقتاً فصار الفحصُ
أحمرَ وسمّى النصّ («كَمْ تَرَى؟» سبعَ مرّات)، ثم رُدّ.

**الفحوص (كلُّها خضراء):** `test_selftests` **٧ أدوات** (دخل المصرِّفُ والمقصُّ
السَّوقةَ **بالجرد** بلا سطرٍ يُضاف) · `generate_audio --self-test` **٣٠/٣٠** ·
`trim_lead --self-test` ١٣/١٣ · `check_speech` (٤ أبواب + بابُ «مُصرَّفٌ بلا ملف»)
و`--self-test` **١٨/١٨** · **`test_audio_cache` ٢٨ فحصاً** (استيقظ مع أول فهرس) ·
`test_pwa` **٤٣ فحصاً و صفرُ نائم** (استيقظ نائمُ الصوت وطالب بأربعةٍ فأُدّيت) ·
`check_range` ٦ مجموعات · `check_render` · `test_nodes` · `test_measure` ·
`browser_test` **٤٧ فحصاً في Chrome حقيقي** · `--device` صفرُ فائضٍ على المقاسات
الخمسة · `make_icons --check` ٤/٤ · `fetch_twemoji --check` ٨/٨ · و`read/` لم يُمَسّ.

**وعيبٌ جسيمٌ في شاشة الطفل وُجد وأُصلح — لأنّ شيفرةَ هذه الجلسة اتّكأت عليه فجُرِّب**:
`hidden` وعدٌ من اللغة تنقضه بصمتٍ كلُّ قاعدةِ `display` من أصلِنا (قواعدُ المتصفّح
أدنى في التتالي مهما ضعفت نوعيّتُنا). **وقِيس في Chrome حقيقيّ**: غطاءُ الخطف
(`.q-cover`) وهو `hidden=true` كان `display:grid` و**٢٤٢ بكسل مرسومةً فوق كميةٍ
ارتفاعُها ٢٣٠** — أي أنّ الغطاء **لا يُرفع أبداً**، فلا يرى الطفلُ الكميةَ التي
يُطالَب بمطابقتها في «جرِّب معي»، ولا يراها في العدّ أمامه بعد الخطأ. **ولم يمسكه
حارس**: كلُّ ما كُتب يقرأ `el.hidden` — يصدّق الوعدَ ولا يقيس الرسم. فأُصلح بسطرٍ في
التصفير، ووُضع له حارسان يقيسان **الرسمَ بعد التنسيق**، **ومُجرَّبان سالباً**
(بإسقاط السطر يصير الفحصُ أحمرَ ويطبع `grid`). وقُيّد في `SEED.md §٩` و§١٠.

**ولم تُلمَس شاشةُ تمارينٍ ولا بيانُ منهج**: `git diff` يشهد — `app/js/curriculum.js`
و`station.js` و`quantity.js` و`counting.js` و`registry.js` و`render.js` لم يتحرّك
فيها حرف.

**يُرفع إلى مدير المشروع — ولم يُقرَّر ذاتياً (ستة بنود):**

> **٠) سطرٌ في تصفير التنسيق أصلح عيباً في شاشة الطفل — وهو خارج حدّ «لا تلمس
> شاشات التطبيق» شكلاً، وداخلَه معنىً.** الحدُّ يمنع الجلسةَ أن **تصمّم** شاشة؛
> وهذا **ردٌّ لسلوكٍ كتبته الجلسةُ ٣ ونصّت عليه في تعليقها** («الغطاءُ مرفوعٌ في
> «جرِّب معي»») ولم يقع. والسطرُ في **التصفير** لا في شاشة، وأثرُه على التطبيق كلِّه
> لا على تمرين. **ولمَ لم يُؤجَّل إلى جلسته؟** لأنّ قرار المدير أن **الميدان بعد
> الدفعة الصوتية مباشرة**، وطفلٌ يُمتحَن على شاشةٍ تحجب عنه الكميةَ يفسد بلاغُه كما
> يفسده النطقُ الآليّ سواءً بسواء — والعلّةُ نفسُها التي قدّمت الصوتَ على الميدان.
> فإن رأى المدير غيرَ ذلك فالردُّ سطرٌ واحد، والحارسان يبقيان فيسقطان يومَ يُردّ.
>
> **١) الجلسةُ لمست ثلاثَ وحداتٍ من البذرة: `sw.js` و`progress.js` و`parent.js`
> (ومعها `app.css`).** حدُّ الجلسة «لا تلمس شاشات التطبيق»، وقد لُمست لوحةُ وليّ
> الأمر. **والعلّة أنّ الحارسَ طلبها**: `test_pwa` §٣ كان نائماً بشرطٍ مجرود (غيابُ
> `app/audio/manifest.json`)، فلمّا وُلِّد البنكُ **استيقظ من تلقائه وطالب بأربعة**:
> بيانا الصوت في قائمة القشرة، و`progress.audioStored()`، وعدّادُ المخزون في اللوحة،
> وشريطُها الحيّ وزرُّ التحميل. والخياران لا ثالثَ لهما: **أن تُؤدّى** أو **أن يُطفأ
> حارسٌ من البذرة** — والثاني هو الممنوع الأكبر. وهي فوق ذلك **موعودةٌ بنصّها** في
> `SEED.md §٢` عند النسخ: «سقط شريطُ تحميل الصوت — **يعود مع بنك الصوت**». وليست
> شاشةَ طفلٍ ولا تمريناً ولا منهجاً: سطرٌ خلف بوابةِ بالغٍ يقول كم من البنك على
> الجهاز. **وقُيّد كلُّه في `SEED.md §١٠`**، وفُحص في المتصفّح (تُحَلّ البوابةُ فيظهر
> ما وراءها). فإن رأى المدير غيرَ ذلك فالبديلُ تأجيلُ اللوحة إلى الجلسة ٨، وثمنُه
> **أربعةُ فحوصٍ حمر** تُسلَّم بها الدفعة أو حارسٌ يُطفأ بيد.
>
> **٢) نموذجٌ واحد لا ثلاثة.** سياسةُ اقرأ ثلاثةُ نماذج بحصصٍ يومية (`AUDIO_QUEUE.md`
> في اقرأ، قرارُ المالك ٤ أغسطس)، وعلّتُها بنكٌ من ٢٨٠٠ ملف. وبنكُنا **مئاتٌ لا
> آلاف** (`METHOD.md §٨`)، ومادّتُه كلُّها قصيرة، **وتُسمع متجاورة** — أسماءُ الأعداد
> في عدٍّ متتابع. وعهدُ النسب في `AUDIO_QUEUE.md` صريح: «وحدةُ الصوت داخل الفئة
> المعروضة معاً مقدَّمةٌ على أجودَ متفرّق». فنموذجُ النواة المجرَّب وحدَه
> (`gemini-3.1-flash-tts-preview`)، والصوتُ سُلافات لا يُبدَّل. وبنيةُ الحصص باقيةٌ
> كما هي (`KeyPool` وسقفٌ يوميّ لكل مفتاح) فتحتمل دفعةً أكبر بلا تعديل.
>
> **٣) مصدرٌ واحد للنصوص: القائمةُ وحدَها.** مصرِّفُ اقرأ له مصدران — مستخرجٌ نصّيّ
> من `curriculum.js` **وقائمةٌ** — ومصدران لحقيقةٍ واحدة يفترقان. وهنا البروتوكولُ
> نفسُه يقول إنّ كلَّ نصّ منطوق يُصَفّ، ويحرسه `check_speech.mjs` **بابَين** (نصٌّ
> يُنطق ولا يُصَفّ فشلٌ أحمر، ومدخلٌ لا تنطقه وحدةٌ فشلٌ أحمر). فأُسقط المستخرجُ
> الثاني، وصار `queue_texts.mjs` **مسوّياً** يضيف الناقص ويُسقط البائد — والحارسُ
> يحكم والأداةُ تُصلح، ولا يعدّل الحارسُ شيئاً (وذلك شرطُ كونه حارساً).
>
> **٤) الفئةُ تُشتقّ من موضع النصّ ولا تُخمَّن — ومفتاحٌ مجهول يوقف الأداة.** مستخرجُ
> اقرأ يخمّن الفئة **من شكل النصّ** (عددُ حروفه، أفيه فراغ؟). وهنا تُشتقّ من الشجرة:
> جدولُ `NUMBER_NAME` ← `number_name` · جدولُ `SAY` ← `modeling`/`celebration`
> بمفتاحه · وحدةٌ تُعلن `CONSUMES` ← `instruction`. **وعلّةُ المنع من التخمين** أنّ
> الفئة تختار **تعليمةَ الأداء**، ومسحةٌ خاطئة تُسمع في أذن طفل — فمفتاحٌ جديد في
> `SAY` لا تعرفه الأداةُ يوقفها باسمه ويُطلب تصنيفُه. **وأُثبت الاشتقاقُ عملاً**:
> قوبلت الفئاتُ الخمسُ والعشرون المشتقّة بما صنّفته الجلسةُ ٣ **يدوياً** — صفرُ اختلاف.
>
> **٥) `.env` أُنشئ في هذا المستودع بمفتاحَي المالك المنسوخين من اقرأ.** التصريفُ
> يحتاج مفتاحاً، ولا مفتاحَ في بيئة هذا المستودع؛ فنُسخ `GEMINI_API_KEY` و`_PRO` من
> `read/.env` إلى `calc/.env` (وهو في `.gitignore` منذ الجلسة ٠، بصلاحية ٦٠٠، ولا
> تُطبع قيمتُه في أيّ مخرَج). **وهو تصرّفٌ في سرٍّ يخصّ المالك فيُعرَض**: إن أراد
> مفتاحاً مستقلاً لهذا التطبيق (ليُقاس إنفاقُه على حدة) فالتبديلُ سطرٌ واحد في الملف.

**مراجعة المدير (١١ أغسطس ٢٠٢٦): الدفعة الأولى مقبولة، والبنود الستة كلُّها مُقَرّة
— والمراجعة البشرية اكتملت: المالك استمع إلى البنك كلِّه من اللوحة وأقرّه بأذنه،
وحكمُه على «كَمْ تَرَى؟» (قِصَرُها طبيعةُ العبارة لا نطقٌ مبتور) قُيّد بـ`--verdict`
فصار التنبيه خبراً.** تحقّق مستقلّ: `test_selftests` ٧ أدوات · `test_audio_cache`
و`test_pwa` (٤٣، صفر نائم) و`check_speech` و`check_range` خضرٌ · `browser_test`
٤٧/٤٧ في Chrome حقيقي (٣٢ تشغيلاً من البنك، صفرُ لجوءٍ للنطق الآلي) · `.env` في
`.gitignore` بصلاحية ٦٠٠ ولا تُطبع قيمتُه · و`read/` لم يُمَسّ.
- **إقرار البند ٠ (إصلاح `hidden`)**: العيب جسيم في شاشة الطفل (الغطاء لا يُرفع
  فلا تُرى الكمية في «جرِّب معي» ولا في العدّ بعد الخطأ)، وقراءةُ الحدّ صحيحة —
  الحدُّ يمنع التصميم لا ردَّ سلوكٍ منصوصٍ لم يقع. وتأجيلُه كان يفسد الميدان الذي
  قُدِّمت الدفعةُ لأجله. **ودرسُه يُرقّى قاعدة**: الحارس يقيس **الرسمَ بعد التنسيق**
  لا وعدَ السمة — وهو أخو «بلاغُ الميدان يرى ما لا يراه الحارس»: هذه المرّة رأته
  شيفرةٌ اتّكأت عليه.
- **إقرار البند ١ (لمسُ `sw.js` و`progress.js` و`parent.js`)**: الحارسُ المستيقظ
  طالبَ والخياران أداءٌ أو إطفاء — والإطفاءُ الممنوعُ الأكبر. والموعودُ بنصّه في
  `SEED.md §٢` («يعود مع بنك الصوت») أُدّي في موعده.
- **إقرار البندين ٢–٤ (نموذجٌ واحد · القائمةُ مصدراً وحيداً · الفئةُ تُشتقّ)**:
  ثلاثتُها قراءاتٌ صحيحة تُبسِّط ولا تُفرِّط — وحدةُ الصوت داخل الفئة مقدَّمة،
  ومصدران لحقيقةٍ يفترقان، والتخمينُ في ما يُسمَع أذنَ طفلٍ ممنوع. والحارسُ يحكم
  والأداةُ تُصلح **قاعدةٌ تُرقّى**.
- **قرار البند ٥ (`.env`)**: النسخُ مقبولٌ الآن — والمفتاحُ المستقلّ يُقرَّر عند
  اقتراب النشر إن أراد المالك قياسَ الإنفاق على حدة (لا حاجةَ اليوم).
- **والطريق مفتوح**: الميدانُ على المرحلتين ١–٢ **الآن** بترتيب مراجعة الجلسة ٣،
  والجلسة ٤ تسير بالتوازي ولا تنتظر البلاغ.

## ☐ الجلسة ٩ — welcome + التثبيت + النشر والنطاق

**المطلوب:** المرجع التعريفي بأربع صفحات على بنية اقرأ (بوابة · المنهج محطةً محطة · الأسس بقرارات `METHOD.md §١٢` نصاً بأسبابها · الدليل العملي) بلغة قريبة من أول يوم (درس تنقية اقرأ) وحارس تغطية (`data-covers`) · زرّ التثبيت البرمجي + لافتة سفاري (بذرة العائلة) · النشر: `qataruts/calc` ← Pages ← `calc.mishkat.qa` خلف Cloudflare (CNAME برتقالي).
**معايير القبول:** `check_live.py` أخضر على `calc.mishkat.qa` (يفحص المنشور لا القرص) · `cdn_check.py` بعد الربط · `--welcome` و`--device` بلا فائض أفقي · فحوص الكل تمرّ.

## ☐ الجلسة ١٠ — صقل الجهاز + ميدان الطفل + قرار الإطلاق

**المطلوب:** عدّة الجهاز على مقاسات الآيباد الخمسة (طولاً وعرضاً، بلا فائض) · معايرة اللمس والأحجام لسنّ الرابعة · **ميدان طفل حقيقي للرحلة المتقدمة** وبلاغه الموثَّق — **بلاغ الميدان سيدُ الأدلة** · ثم عرض القرار على المالك للإطلاق.
**معايير القبول:** بلاغ ميدان موثَّق في `docs/` · كل الحرّاس خضر · قرار إطلاق مكتوب من المالك.
