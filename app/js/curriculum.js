// بيانات المنهج — **المصدر الوحيد للحقيقة** في «اِحْسِبْ» (قاعدة `SESSIONS.md` العامة).
//
// ————— هذا الملفّ بذرةٌ فارغة، وتملؤه **الجلسة ١** —————
//
// الجلسة ٠ (التأسيس) لا تكتب محتوى منهجٍ البتّة (حدودُها المعلنة)، وإنما تُثبِّت
// **الوصلة** التي تتعلّق بها بقيةُ الوحدات: `progress.js` تبني الرحلةَ من `sections()`،
// و`main.js` يرسم الخريطةَ من وصفِ الأقسام، فلا يعرف أيٌّ منهما مرحلةً ولا محطةً ولا
// عدداً. فمتى كتبت الجلسةُ ١ بياناتِ `METHOD.md §٣` هنا، ظهرت الرحلةُ كاملةً على
// الخريطة **بلا سطرٍ يُعدَّل في وحدةٍ أخرى** — وهو معنى «الرحلة تُحسب من البيانات».
//
// **ولماذا فارغةٌ لا محذوفة؟** لأنّ الغيابَ التامّ يُسقِط التطبيقَ عند أول استيراد،
// فلا تُفتَح شاشةُ الخريطة الفارغة التي هي معيارُ قبول هذه الجلسة. والفراغُ المعلَن
// خيرٌ من ملفٍّ ناقصٍ يُظنّ تامّاً: صفرُ مرحلةٍ وصفرُ محطةٍ وصفرُ عدد.
//
// ————— العقد الذي تلتزم به الجلسة ١ —————
//
// ١) **كلُّ محطةٍ تُعلن جبهتَها** (`METHOD.md §١٠.١`): أقصى كمية · أقصى رمز ·
//    العمليات المتاحة · أنماط العرض المتاحة — وعليها يقوم `check_range.py`.
// ٢) **الرحلةُ محسوبةٌ لا مكتوبة**: عددُ العقد يُشتقّ من البيانات ويطبعه الحارس،
//    ولا يُكتب رقمٌ بيد في حارسٍ ولا في وثيقة (عهدُ «أرقامٌ محسوبة لا مكتوبة»).
// ٣) **القفلُ تسلسليّ**: ترتيبُ `sections()` هو ترتيبُ الرحلة، وتُفتَح العقدةُ
//    بإتمام كل ما قبلها — لا يعرف `progress.js` غيرَ هذا الترتيب.
// ٤) **البوابات الثلاث** (`METHOD.md §٥`) أقسامٌ في مواضعها من `sections()`.
// ٥) محطةُ التعارف (٨·٥) بإعفائها المكتوب — لا قياسَ عليها (`METHOD.md §٩`).

/**
 * مراحل الرحلة الثماني (`METHOD.md §٣`) — **الجلسة ١**.
 * الشكل المتوقَّع لكل مرحلة: `{ id, title, stations: [...] }`،
 * وكلُّ محطة تحمل `frontier` تُعلن فيها حدودَها (كمية · رمز · عمليات · أنماط).
 */
export const STAGES = [];

/**
 * البوابات الثلاث (`METHOD.md §٥`) — **الجلسة ١**.
 * الشكل المتوقَّع: `{ id, after, title, hint, face }`.
 */
export const GATES = [];

/**
 * **أقسامُ الرحلة بالترتيب** — الوصلةُ الوحيدة بين المنهج والواجهة.
 *
 * يُرجِع مصفوفةَ أقسامٍ، كلُّ قسمٍ يصف نفسَه للخريطة ويحمل عقدَه:
 *
 * ```
 * {
 *   kind,            // نوعُ القسم (مرحلة · بوابة …) — للتنسيق لا للمنطق
 *   id,              // معرّفٌ فريد (تصفيرُ المحطة في لوحة وليّ الأمر يستعمله)
 *   title, sub,      // ما يُقرأ على الخريطة
 *   face, mark,      // وجهُ القسم ومعلمُه البصريّ (اختياريان)
 *   accent,          // متغيّرُ لونِ القسم من `app.css` (اختياريّ)
 *   nodes: [ { id, type, part, title, face } ]   // عقدُه بالترتيب
 * }
 * ```
 *
 * **العقدةُ معرّفُها `<النوع>:<الجزء>`** — والنوعُ هو ما يوجّه به `main.js` إلى
 * شاشتها، وهو نفسُه ما يُجرَد في `test_measure.mjs` (قياساً أو إعفاءً مكتوباً).
 */
export function sections() {
  return [];
}

/** البوابةُ التي تقف قبل هذا الموضع، أو `null` — بيانٌ مُعلَن لا شرطٌ مضمر. */
export function gateBefore(where) {
  return GATES.find((gate) => gate.after === where) || null;
}

/** بوابةٌ بمعرّفها (تستعملها شاشةُ البوابة في `gate.js`). */
export function gateById(id) {
  return GATES.find((gate) => gate.id === id) || null;
}
