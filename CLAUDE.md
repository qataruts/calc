# مشروع «اِحْسِبْ» — الحسّ العددي المبكر لطفل ٤–٧

تطبيق ويب (دون أدوات بناء، HTML/CSS/JS خالص بوحدات ES) يعلّم الحسَّ العددي والعمليات الأساسية على **مسارات التعلّم (Clements & Sarama)** — ثاني عائلة تطبيقات التأسيس بعد «اِقْرَأْ»، ومسارُه (ب — حساب) مستقلٌّ يُبنى بالتوازي.

**الهوية**: العلامة **«اِحْسِبْ»** بخط **Kufam** (حكمُ المالك على لوحة المرشَّحات، ١٢ أغسطس — الجلسة هـ؛ وMarhey لاقرأ وحدها فأُسقط من الشجرة، `docs/REVIEW_IDENTITY.md`) · تقنياً `calc` · النطاق `calc.mishkat.qa` · لاتينياً `Calc`.

## طريقة العمل — اقرأ هذا أولاً
- **مدير المشروع**: جلسة فابل ٥ خاصة بهذا التطبيق — تضع المنهج والخطة وتراجع المنتجات. **جلسات التنفيذ**: أوبوس ٥، كلُّ جلسةٍ ببند حدودٍ ومعيار قبول في `docs/SESSIONS.md`.
- **الوثيقتان المؤسِّستان**: `docs/FAMILY.md` (خارطة العائلة ومبادئها التسعة وبذرة المنصة) ثم `docs/PLAN.md` (خطة احسب المفصّلة: المنهج، الحرّاس، الجلسات، القرارات). **اقرأهما قبل أي عمل.**
- **المرجع الحيّ**: تطبيق اقرأ في مجلد `read` بهذا الـworkspace (يُقرأ ولا يُمَسّ) — `read/CLAUDE.md` يشرح الثقافة: حرّاسٌ يحرسون الغياب، أرقامٌ محسوبة لا مكتوبة، بلاغُ الميدان سيدُ الأدلة، فصلُ الصوت عن التطوير.
- **الحال الآن**: **`docs/METHOD.md` هو المرجع الملزم** و`docs/SESSIONS.md` تحمل بنود الجلسات ٠–١٠ وص بمعايير قبولها. **الجلسات ٠–٣ أُنجزت** (١١ أغسطس ٢٠٢٦): بذرةُ المنصة منسوخةٌ من `read@ea07f350` ومجرَّدة (الجردُ في **`docs/SEED.md`**)؛ و**الرحلةُ مكتوبة**: المراحل الثماني (٤٩ محطة) والبوابات الثلاث في `app/js/curriculum.js`، كلُّ محطةٍ تعلن جبهتَها ومفاتيحَ ليتنر، والخريطةُ تعرض ٥٢ عقدةً بالقفل التسلسلي، و`tools/check_range.py` يحرس الجبهة مُجرَّباً سالباً؛ و**الكمياتُ تُرسم**: `app/js/render.js` بستّة أنماط، وحارساه `tools/check_render.mjs` (هندسةً في node) و`browser_test.html` (رسماً على الشاشة)؛ و**المرحلتان ١–٢ تُلعبان**: `station.js` (حلقةُ المحطة الأربعية ومعالجةُ الخطأ وطبقةُ اللمس) و`quantity.js` و`counting.js` بخمس شاشات. **وأُنجزت الدفعات الصوتية الثلاث** (الجلسة ص): **٦٩ نصّاً مُصرَّفةٌ** بصوت **سُلافات** في `app/audio/` ببصمات محتوى (٢٥ للمرحلتين ١–٢ و١٣ للمرحلتين ٣–٤ **أقرّتهما أذنُ المالك**، و**٣١ للمراحل ٥–٨ ومعها أسماءُ ١١–٢٠ تنتظر أذنَه** — `tools/audio_panel.py --serve --since`)، **والقائمةُ فارغة والرحلةُ كلُّها منطوقة**، واحتياطُ النطق الآليّ لم يعد يعمل لشيء والفحصُ صارمٌ مُجرَّبٌ سالباً. **والفئةُ تتبع موضعَ النصّ**: جملُ الكشف الثلاث نُقلت إلى `SAY` فصارت نمذجةً، وبابٌ خامسٌ في `check_speech` يحكم و`queue_texts --retag` يُصلح. **وأُنجزت الجلسة ٤**: **المرحلتان ٣–٤ تُلعبان** — `numerals.js` (حلقةُ تقديم الرمز: كميةٌ تُعَدّ ← يُكشَف رمزُها فوقها ← مطابقةٌ ذهاباً وإياباً، والصفرُ آخراً بإطارٍ فارغ) و`compare.js` (`compare` · `line` · `order` · `neighbor`)، **والمصيِّر صار ثلاثةَ أصنافٍ** (كمّيةٌ تُعَدّ · رمزٌ يُقرأ نصُّه · خطٌّ تُعَدّ علاماتُه) ولكلٍّ قارئُ مرسومه وحارسُه، **والقياسُ حيٌّ في المراحل ١–٤ كلِّها** (١١ نوعَ تمرين، ١٠ أنواع شاشات) **والمراجعةُ اليومية تعمل من سجلٍّ حقيقيّ** (٦ تمارين، الأضعف أولاً). و١٣ نصّاً منطوقاً جديداً **صُرِّفت في الدفعة الثانية** (أعلاه). **وفُتح الميدان فجاء البلاغ ١** (`docs/FIELD.md`: صوتٌ متراكبٌ مقطوش، ولا انتظارَ للكلام) **فنُفِّذت جلسةُ الإصلاح م١**: **للصوت قناةٌ واحدة بطابور** — `play()` تُرجِع وعداً يتمّ بتمام الكلام و`stop()` يُفرغ الطابور، **والانتقالُ بعد تمام الجملة** لا بمهلةٍ ثابتة (و`BEAT` باقٍ فاصلاً بين الجمل)، **وحارسا التعاقب** يقيسان ما سمعته الأذن لا عددَ التشغيلات: بابٌ سادس في `check_speech` (لا صوتَ خارج القناة، ونداءان في لحظةٍ يتتابعان) وخمسةُ أحكامٍ زمنية في `browser_test` — **مُجرَّبةً سالباً** بردّ الشيفرة القديمة. **والبلاغ ١ لا يُغلَق إلا ببلاغٍ معادٍ من المالك** (وقشرةُ `sw` رُفعت إلى `v6` كي يجرّب الشيفرةَ الجديدة لا المخزونة). **وأُنجزت الجلسة ٥**: **المرحلة ٥ تُلعَب** — `bonds.js` بتمرين «اِصْنَعِ العَدَد» (`METHOD.md §٧`) بوجهيه: جزآن يُوضَعان في خانتَي الكلّ (و«بطريقةٍ أخرى» في ٥·٣)، و«ما بقي للعشرة» حيث **خاناتُ الإطار الفارغة هي السؤال** — **ولا رمزَ عمليةٍ واحد** يفرضه الفاحصُ وحارسُ المتصفّح معاً. **والكمّيةُ تنشقّ شقّين في المصيِّر لا في الشاشة** (`split` — الشقُّ يتبع ترتيبَ الرسم فيوافق ما يُعَدّ، وبابُه في `check_render`). **وبوابةُ الكمّ تعمل** — وقد كانت لا تُرسَم أصلاً (`renderSession` تطلب مُصيِّرَ التمرين ونسيته البوابة) — وحارسُها `test_gate.mjs` يسألها **بسجلٍّ مصنوع** فيُثبت أنّها من **أضعف** المهارات لا من المستحقّ. **وسِجلُّ التمارين صار نوعاً ← قائمةَ مالكين** (`equal|5|make` و`bond|10|make` اسمُ نوعهما واحد، فكان أحدُهما يمحو الآخر صامتاً). **والسَّوقةُ تُشغَّل بأمرٍ واحد**: `node tools/guards.mjs --browser` يجرد الحرّاسَ **بالنمط** (`test_*.mjs` · `check_*.py` · `check_*.mjs`) ويشغّلهم ويفشل بفشل أيٍّ منهم باسمه، ويسمّي سَوقةَ المتصفّح أُشغِّلت أو تُركت. **وأُنجزت الجلسة ٦**: **المرحلة ٦ تُلعَب** — `ops.js` بخمسة أنواع شاشات (`add` · `sub` · `diff` · `zero` · `fluent`) وثلاثةِ أوجهِ رسم، **و«الجمعُ تركيبُ معلومٍ لا طقسٌ رمزيّ» على ثلاث درجات**: ٦·١ **بلا علامةٍ البتّة** (كميتان تلتقيان فتُعَدّ الحصيلة — ولوحُها **صورةُ الجسر مقلوبةً**: مجموعتان فوق وحصيلتُهما تحتهما، **ولونُ كلِّ مجموعةٍ لونُ شقّها** من المصيِّر)، ثم **الجملةُ وكمّيتُها معاً أوّلَ لقاء**، ثم **الرمزُ وحدَه والكمّيةُ عوناً عند النقر**. **ورمزُ العملية من جبهة المحطة حصراً** (`signFor`) يحرسه `check_range` جولةً جولة **مُجرَّباً سالباً** وحارسُ المتصفّح بجرد النصّ المعروض. **والجوابُ من المرسوم** (مجموعُ الجمع `drawn` · وباقي الطرح `split` · والفرقُ فرقُ الصفّين)، **ولكلِّ عمليةٍ عدُّ درسِها** عند الخطأ (تصاعديٌّ من الأكبر · تنازليٌّ مع كلِّ مغادر · عدُّ الفائض) بـ`countMarks` **واحدة**. **وبوابةُ العمليات من مدَاها المعلَن** (`from` في `curriculum.js` و`gateSkills` — فلا يتصدّر جلستَها ضعفٌ في التقدير الفوريّ) وحارسُها بسجلٍّ مصنوع **أضعفُ ما فيه خارج المدى**، مُجرَّباً سالباً. **وأُنجزت الجلسة ٧ فتمّت الرحلة**: **المرحلتان ٧–٨ ومحطةُ التعارف تُلعَب** — `teens.js` (`teen` بوجهَي البناء والقراءة، و`bridge` بسؤالَي «اصنعْ عشرةً أولاً») و`patterns.js` (`pattern` و`measure`) و`intro.js` (٨·٥ معفاةً بشاشة عرضٍ بلا سؤال). **وما فوق العشرة يُبنى ولا يُعَدّ فرادى**: `countUp` تُعلِّم الحزمةَ دفعةً واحدة وتسمّيها تامّةً ثم تَعُدّ الآحادَ فوقها، **والعبورُ نازلاً** في `sub:twenty` يقف عند العشرة (`countDown` واحدةٌ للنمذجة وللخطأ) **والإطارُ يفرغ من حيث امتلأ**. **والمصيِّرُ خمسةُ أصناف** بعد أن كان ثلاثة (شريطُ نمطٍ تُعَدّ خاناتُه · مشهدُ قياسٍ تُعَدّ أشياؤه) فاستيقظ آخرُ نائمَي `check_render`. **وقيدُ إصلاح الجلسة ٦ نُفِّذ**: نمذجةُ الفرق تُرَصّ صفّاً تحت صفّ (`model.rows`) وحارسُها يقيس الرصَّ لا يقرأ النصّ. **وحارسٌ جديد `test_journey.mjs`** يمشي الرحلةَ **عقدةً عقدة من سجلٍّ نظيف حتى الختام**، و**يطبع عددَ العقد محسوباً من ثلاثة مصادر مستقلّة** (٥٢) ويقابله المتصفّحُ بما رسمته الخريطة. **و٢٢ نصّاً منطوقاً جديداً** في القائمة (منها أسماءُ ١١–٢٠) **صُرِّفت في الدفعة الثالثة** (أدناه).

**وأُنجزت الجلسة هـ** (حزمة الهوية): **العلامة صار خطُّها Kufam بحكم المالك** على لوحةٍ مُصيَّرة (`tools/identity_fonts.html` — ثمانيةٌ صُيِّروا فسقط خمسةٌ بأسبابهم المعلَنة، والحكمُ يُقيَّد في `docs/REVIEW_IDENTITY.md`)، **وأُسقط Marhey** من `app/fonts/` ومن قشرة `sw` (رفعةً إلى `v11`)، **والأيقوناتُ الأربع أُعيدت** بمقاسٍ **مقيسٍ بحبر الكلمة** لا برقمٍ ضُبط لخطٍّ بعينه، **ومُتحَنُ الصفّ** (`tools/identity_row.html`) يصفّ أيقونتَنا بجوار اقرأ فيفترقان **لوناً ورسماً معاً**. **وحارسُ الهوية `test_identity.mjs`** تسعةُ أبوابٍ مُجرَّبةٌ سالباً بمرجعٍ مبصوم من `read@9220ab1`: الورقُ يخالف، **والغالبُ يفارق بُعداً تراه العين** (٤٠° حدّاً)، وخطُّ العلامة ليس خطَّ اقرأ **ولا حِملَ لملفّه في الشجرة**. **واسمُ خطّ العلامة صار يُقرأ من `--font-brand`** في حارسَي الأيقونة والمتصفّح بعد أن كان مكتوباً بيد. **وبندٌ مرفوعٌ للمدير**: نكهةُ الاحتفال — عالمُ العدّ حاضرٌ في الرحلة غائبٌ عن الميدالية (المقترحُ في `REVIEW_IDENTITY.md §٤`، ولا نصَّ منطوقاً جديداً).

**وأُنجزت الجلسة ٨** (لوحة وليّ الأمر + النسخ الاحتياطي + مشاهد القياس): **ميداليةُ الختام تحمل معلمَ مرحلتها** (قيدُ الجلسة هـ — `landmark()` صار يتبع **حبرَ موضعه** لا لونَ المرحلة فلا يختفي على قرصها، و`mark` تُعلنه `stations()` فتقرؤه `medalOf`، وبابٌ سادس في `test_journey` **مُجرَّبٌ سالباً بثلاث**، وحارسُ المتصفّح **يمشي المراحلَ الثماني** بزرّ الإنهاء فيقابل ما رُسِم برسم المعلم من `ui.js` **ويقيس أنّ حبرَه يفارق لونَ القرص**). **ولوحةُ وليّ الأمر تتكلّم**: أقسامُ الحساب السبعة **مفهومٌ بعملية لا درجة** — **العباراتُ بياناتُ منهج** (`CONCEPTS` في `curriculum.js`: مَن كتب مفتاحَ ليتنر يسمّيه) **واللوحةُ تُترجم ولا تخترع**، **والمدى من ليتنر الحيّ** (أقصى ما بلغ صندوقَ الإتقان في «يفعل»، ومدى **أضعف** مهارةٍ في «يحتاج») فتقول «يقدّر حتى ٥ فوراً» و«يحتاج تثبيت جسور ١٠» بنصّ المنهج حرفاً، **والمديات الوصفية تُسمّى** فلا مفتاحَ خامٌ لوالد. **و«راجِع مختصاً» صارت إشارةً تقع** لا سطراً يُقرأ دائماً: **`since` في سجلّ المهارة** وعتبتُها **ثلاثةُ أسابيع** في الصندوق الأدنى (بحدود `METHOD.md §١٣` — إشارةٌ لا حكم). **والنسخةُ الاحتياطية صارت مقيسة** (لا سطرَ تغيّر فيها): الرحلةُ كلُّها تُصدَّر، **ويُمحى الجهازُ**، ثم تُستعاد فتُقابَل **حقلاً حقلاً** وتُحسب جبهةُ الفتح من المستعاد، وأربعةُ ملفّاتٍ ليست نسخةً تُرَدّ بأسبابها. **وحارسٌ جديد `tools/test_parent.mjs`** ستةُ أبوابٍ **مُجرَّبةٌ سالباً بسبع**، أشدُّها «**الرقمُ يتحرّك بحركة السجلّ**». **ومشاهدُ القياس عُرِضت ولم تدخل**: ٢٠ رسماً من Twemoji في `tools/emoji_candidates/` بـ`fetch_twemoji --stage` (خارج شجرة النشر، و`--check` أخضرُ على المنشور)، ولوحةُ `tools/measure_scenes.html` تعرض **ثمانيةَ مرشَّحين بهندسة المصيِّر نفسِها** وتحت كلٍّ ما يراه الطفلُ اليوم، وأربعةٌ سقطوا قبل العرض — **وبندٌ مرفوعٌ للمدير**: دخولُها يقتضي أن يصير **مصدرُ الجواب رتبةً مُعلَنةً في المنهج** لا `data-size` المرسوم (`docs/REVIEW_SCENES.md §٤`). وقشرةُ `sw` رُفعت إلى `v12`.

**وأُنجزت الجلسة ٨ب + م٣** (مشاهد القياس تدخل، وقرصةُ الطفل تُردّ): **الستةُ المُقَرّة دخلت والاثنان لم يدخلا** — **`SCENES` جدولٌ مستقلٌّ في المنهج** (لا `OBJECTS`: عناصرُ المشاهد تُقارَن ولا تُعَدّ) بوجهين وكلُّ رمزٍ **برتبته المعلَنة**، و١٣ رسماً نزلت إلى `app/emoji/` **بجرد `fetch_twemoji`** (٢١ رمزاً، `--check` أخضر) **مطابقةً بايتاً ببايت لمرشَّحات اللوحة** — فما رآه المديرُ هو ما يراه الطفل. **والمشهدُ صار وجهين**: مقاديرُ الشيء الواحد (الطولُ باقٍ) وأشياءُ مختلفة **بمقدارٍ واحد** لا يحمل حكماً — **و`data-size` غائبٌ عن الثاني عمداً**، فالحكمُ `worthOf(face)`: مقدارٌ مرسوم في الطول **ورتبةُ الرمز المقروء من الـDOM** فيما سواه، **وتأخذها `walkScene` نفسُها** فما يُضاء عند الخطأ عينُ ما يُحكَم به. **والزوجُ الفاصلُ يتصدّر** (🚗/🎈 — البالونُ قد يُرى كبيراً وهو أخفّ) ثم تدور المشاهدُ كلُّها. **وثلاثةُ حرّاسٍ امتدّت مُجرَّبةً سالباً**: `check_range` بابٌ ١ب (بنيةُ الجدول، **وكلُّ جولةٍ تُجرَد بمشاهدها** فيُطلَب لكلِّ رمزٍ رتبتُه) · `check_render` بابٌ ٣و (**تساوي المقادير** ومطابقةُ المرسوم للمُعلَن، **وصدقُ الصورة امتدّ إلى أشياء المشاهد**) · **وحارسُ المتصفّح بجدول حقيقةٍ مكتوبٍ بيدٍ عمداً** لا يُقرأ من المنهج — **فرتبةٌ انقلبت هناك تمرّ على الفاحص ببنيةٍ سليمة ولا تمرّ عليه**. ونصٌّ منطوقٌ واحد زيد بعلّته (`revealFirst`: تسلسلُ الزمن ليس فيه أصغرُ وأكبر) **وصُفّ في القائمة** (وصُرِّف في الدفعة الثالثة). **وم٣: القرصةُ رُدّت** — أُسقط قفلُ الميتا الذي نقلته م٢، و`resetZoom` يشدّ المقياسَ إلى ١ لحظةً عند العودة من الخلفية ثم يردّ الحرية بعد ٨٠ مللي، و`touch-action` باقٍ للنقر المزدوج العارض وحدَه، **والحارسُ قُلب ولم يُحوَّط** (يحرس الحريةَ والشدّةَ معاً، وقصةُ القلب في تعليقه: **«بلاغُ الميدان فوق الحارس»**) مُجرَّباً سالباً بثلاث — **والبلاغُ العائليّ أُغلق بإيصاله** في `FAMILY_MAIL.md`. وقشرةُ `sw` إلى `v13` رفعةً واحدة للحزمة.

**وأُنجزت الدفعة الصوتية الثالثة** (الجلسة ص، ١٢ أغسطس): **الرحلةُ كلُّها منطوقة** — ٣١ نصّاً للمراحل ٥–٨ ومعها أسماءُ ١١–٢٠ (٩٢٫٤ث · ٧٢٢ك.ب) فصار البنكُ **٦٩ ملفاً** (١٧٣٫٨ث)، **والقائمةُ صفر**. **وبابُ ق٢ عمل يومَ جيء به**: عشرةُ أعدادٍ لفظُ كلٍّ منها كلمتان مرّت البوّابةَ **صفرَ محجوز** بفئتها لا بشكلها. **والفحصُ الصارم مُجرَّبٌ سالباً** (أُسقط مفتاحٌ فاحمرّ وسمّى نصَّه عشراً، ثم رُدّ مطابقاً بايتاً ببايت) — ٤٨٠ تشغيلاً من البنك وصفرُ لجوءٍ، والسَّوقةُ كاملةً خضراء (١٥ حارساً). **والمراجعةُ بالأذن معلَّقةٌ على المالك** (لوحةُ `--since` بالدفعة وحدَها). **وخبرُها الأهمّ: المسطرةُ تحرّكت** — وسيطُ `number_name` قفز ١٫٠٣ ← ١٫٦٦ث بدخول أعداد الكلمتين، فنُبِّه على «صِفْرْ» و«تِسْعَةْ» **بلا تغيّرِ بايتٍ فيهما** وقد أُقرّتا سلفاً؛ والقرارُ مرفوعٌ. **وثلاثةُ بنودٍ للمدير** (منها أنّ بند «خطف نموذج أعطني ن» صار مستحقَّ النظر — ولم يُنفَّذ).

**وأُنجزت الجلسة ٩** (المرجع التعريفي + التثبيت + النشر): **القيدان الافتتاحيان نُفِّذا** — **نموذجُ «أعطني ن» يُسمّى ثم يُخطَف في «وحدك» وحدَها** (والاسمُ من المرسوم، والغطاءُ غطاءُ «كم ترى؟»، **والإعادةُ بلا حدٍّ ولا احتساب**، و«جرِّب معي» يبقى ظاهرَ النموذج) وحارسُه خمسةُ أحكامٍ في المتصفّح **مُجرَّبةٌ سالباً** (وبابُ الاسم قِيس بالتتابع بعد أن كان يمرّ من ضجيج العدّ)؛ و**بوابةُ المرمِّز** في `generate_audio.py` تجرد `lameenc` **قبل أوّل طلب** لا بعده، وحارسُها يقيس **موضعَ الجرد من موضع الطلب** — مُجرَّباً سالباً، **وقد أمسكت التجربةُ عيباً في الحارس نفسِه** (كان يقتطع `main` بعبارةٍ تقع في سطره هو فيقيس شيفرتَه). **والمرجعُ التعريفيّ `app/welcome/` قائم**: أربعُ صفحاتٍ بقشرةٍ واحدة على بنية `read@260bf48` **ومادّتُها كلُّها لنا** (بوّابة · **المنهج ٢٢ بطاقةَ نوع محطة بنمطٍ ثلاثيّ** · **الأسس بقرارات `METHOD.md §١٢` نصّاً بأسبابها** · الدليل العملي)، **بصفر جافاسكربت وصفرِ مَوردٍ خارجيّ**، خارج قشرة `sw` **والاستثناءُ قبل ردّ التنقّل**، وبابُها في ذيل الخريطة (`map-about`)، **ولقطاتُها اثنتا عشرة من التطبيق نفسِه** بمولّدها (`make_welcome_shots.py` — تُزرَع رحلةُ طفلٍ ثم تُفتَح الشاشةُ من الموجِّه). **وحارسُه `test_welcome.mjs` ثمانيةُ أبواب**، أشدُّها **حارسُ التغطية**: لكل نوع عقدةٍ بطاقتُه بعددها المحسوب ومجموعُها الرحلةُ كلُّها (٥٢) — **مُجرَّبٌ سالباً بثلاث**، ومعه **كلُّ `data-stat` محسوبٌ من المنهج** و**لا وعدَ بزرٍّ ليس في التطبيق** (١٦ اسماً). **وبابُ التثبيت نُقل مكيَّفاً** (`install.js` بنصوص «اِحْسِبْ» ومفتاح `ihsib`، معفىً في المعاينة، في قشرة `sw`) وحارسُه `test_install.mjs` بجدوله **وبابٌ رابع للتكييف** (لا اسمَ أخيه فيما يُعرَض، ولا كتابةَ في مفتاحه). **والنشرُ جاهز**: `pages.yml` ينشر `app/` عند الدفع وPages مُفعَّلٌ بنمط `workflow`، وثلاثُ عُددٍ منقولةٌ مكيَّفة (`check_live` يفحص **المنشور** وقائمتُه مشتقّة · `cdn_check` يشتقّ خادمَ الأسماء · `perf_check` بصفحته) — **و`check_live` يُعلن في رأسه `سَوقة: --self-test`** فتشغّله السَّوقةُ بلا شبكة. **والسَّوقةُ ١٩ أمراً كلُّها خضراء** (وسَوقةُ المتصفّح ثلاثاً: الفحوص · `--device` · `--welcome` بـ٤٥ فحصاً)، وقشرةُ `sw` إلى `v14`. **والنشرُ وقع**: الحزمةُ مدفوعةٌ إلى `main` ونشرها السير، **و`check_live` أخضرُ كلُّه على العنوان الافتراضيّ** (`qataruts.github.io/calc/` — خمسُ صفحاتٍ بعناوينها وثمانيةُ أصول). **وأوّلُ دفعةٍ كشفت ما لا يمسكه فحصُ قرص**: `shots/` في `.gitignore` ابتلع `app/welcome/shots/` فمضت الصفحاتُ بلا صورةٍ واحدة — فأُضيف استثناءٌ بعلّته **وبابٌ يمسكه قبل النشر** (اللقطاتُ متتبَّعةٌ في git). **والنطاقُ رُبط في الجلسة نفسِها**: ضبط المالكُ سجلَّ Cloudflare (برتقاليّاً) فضُبط الربطُ في Pages ونُقل ملفُّ النطاق إلى `app/CNAME` — **والتطبيقُ حيٌّ على `https://calc.mishkat.qa/`**: `check_live` أخضرُ كلُّه (٥ صفحاتٍ و٩ أصول)، و`cdn_check` يقول **الوسيطُ يخدم الصوتَ من حافّته** (١٢/١٢) **وبياناتُ البصمات طازجةٌ عمداً** (`.json` خارج قاعدة التخزين — وأُصلح حكمُ العدّة فصار يعدّ طزاجتَها صواباً وتخزينَها إخفاقاً باسمه). وبقيت **شهادةُ GitHub** تصدر من تلقائها ثم يُنفَّذ HTTPS بنقرة.

**وأُنجزت الجلسة ك** (التكثيف المستهدف — قرار المالك على `REVIEW_SCOPE.md`): **سبعُ محطاتٍ دخلت المواضعَ الخمسةَ ناقصةَ الكثافة فصارت الرحلةُ ٥٩ عقدة** (يطبعها `test_journey` من ثلاثة مصادر ويقابلها المتصفّحُ برسم الخريطة)، **وق٣ لم يُمَسّ بحرف** ولا محطةٌ قائمة تبدّلت بياناتُها: المراحلُ ٦→٩ و٧→**١١** (لا ١٠ — بندٌ مرفوع) و٨→٦، والمفاتيحُ ٥١→٥٦، وأنواعُ العقد ٢٢→٢٤. **والمزدوجُ مرآةٌ تُرى** (`double` في `ops.js` — محطتاه في مرحلتين): النصفُ والنمطُ **والبذرةُ** واحدة فيُرسَم الشكلان سواءً بسواء وبينهما **خطُّ مرآة** (ولا يُقلَب إطارٌ فيتبدّل ملؤه)، **والجوابُ مجموعُ ما رُسِم**، **وعدُّ الخطأ عدُّ درسِه**: يُسمّى النصفُ دفعةً ثم يُعَدّ الآخرُ فوقه، **وأنصافُه من ترتيب المنهج** (`halvesOf`). **والعشرةُ صارت محطّةً في العدّ الصاعد** (`countAcross`) كما كانت في النازل — عهدُ المرحلة ٧ كان محروساً نزولاً مكشوفاً صعوداً، **ولا وقفةَ إن كانت العشرةُ آخرَ ما يُعَدّ** فلم يتبدّل حرفٌ في المرحلة ٦. **و«التثبيت» تثبيتٌ لا إعادة** مقروءاً من ترتيب المنهج (`isFix`): العبورُ صعوداً من نصف العشرة لا من قريبها، والنزولُ **في كلِّ جولة**. **والعدُّ القفزيُّ على الخطّ والنمطُ العدديُّ على الشريط وقفزاتُهما مصدرٌ واحد** (`SKIP_STEPS` في المنهج — ٢ · ٥ · ١٠): المواقعُ تُضاء ويُقرأ منها الجوابُ (`skip` في `teens.js`)، والشريطُ **صار يحمل رقماً كما يحمل صورة** (`render.js` — مشرقيّاً من `arNum` وحدَه، ومداه مدى بطاقة الرمز، وقفزتُه فرقُ خانتين مرسومتين). **وثلاثةُ حرّاسٍ امتدّت مُجرَّبةً سالباً** (`check_range` بابا ١ج «كلُّ قفزةٍ تقسم السقف» و٤ب «**رقمُ خانة الشريط رمزٌ معروض**» — وعددُ الشريط طولُه لا مادّتُه · `check_render` بابٌ ٣ز بجدول أرقامٍ مكتوبٍ عنده مستقلاً) **ومعها ١٣ حكماً في المتصفّح** (١٥٤ فحصاً كلُّها خضراء). **وأمسك حارسُ البوابة عيباً كامناً منذ الجلسة ٤**: `parseSkillKey` تردّ المدى نصّاً فلا يتطابق مع مدى المحطة، **فكانت مراجعةُ الرمز الضعيف تُبنى على رمزٍ مجاورٍ بالقرعة** — فصار لقاعدة «المدى عدداً إن كان عدداً» **مصدرٌ واحد** (`spanOf` من `progress.js`). ونصٌّ منطوقٌ واحد في القائمة للدفعة الرابعة («إِلَى أَيْنَ تَقْفِزْ؟» — وسائرُه مصروفٌ أصلاً)، وقشرةُ `sw` إلى `v15`، **ولم يُدفَع شيءٌ إلى GitHub** (المديرُ يدفع بعد المراجعة). **وثلاثةُ بنودٍ مرفوعة** في `SESSIONS.md`.

**وأُنجزت الجلسة و** (١٢ أغسطس ٢٠٢٦ — البلاغ ٣ بتوضيح المالك: اللقطاتُ المنشورة كلُّها من أول الرحلة فكذبت على مداه): **صورُ التعريف صارت تمثّل مدى التطبيق لا أوّلَه** — علّةُ الكذب كانت في المولّد نفسِه (صوتٌ لا ينتهي يجمّد كلَّ شاشةٍ على أول لحظة من «شاهِدْ» فلا رمزَ ولا جملةَ عملية)، فصار لكل لقطةٍ **مشهدٌ يُساق إليه** (`SCENES` في `welcome_shots.html`: تجميدٌ · تمامُ النمذجة بكشفها · جولةُ «جرّب معي» · ختامٌ بميداليته عبر `dev=1`) **ببذرةٍ مجمّدة** (`Date.now` ثابتة و`Math.random` مبذور) فما يُقاس هو عينُ ما يُلتقَط، والمُهَلُ تُطوى بـ`--virtual-time-budget`. **والتشكيلةُ ١٧ لقطة** (كانت ١٢): الصدرُ **جملةُ جمعٍ بالرموز المشرقية وخانةُ السؤال**، وقاعدةُ «كمّاً ← عدّاً ← رمزاً» ثلاثاً جنباً إلى جنب في «الأسس»، وآلةُ الجمع ثم جملتُها، والطرحُ والفرق، وخطُّ ٠–٢٠ يبدأ يميناً تحت جدول القرارات بسببه، وبناءُ ١٥ في الإطارين، والمقارنةُ بالرموز، وفي «الدليل» بوابةُ الإتقان وميداليةُ المعلم. **والمولّد يكتب بيانَ اللقطات** (`shots/manifest.json` — مسارٌ وأبعادٌ ونصُّ ما تراه العين من `innerText`) **وحارسُ المدى** بابٌ تاسع في `test_welcome.mjs` يقرؤه: البيانُ وصورُه صنوان بأبعاد ترويسة PNG، **ولكل مرحلةٍ من الثماني لقطةٌ من محطاتها، ورمزا العمليتين من `SIGNS` والرمزُ المشرقيّ معروضةٌ في الصور فعلاً**، والصدرُ جملةُ جمع — **مُجرَّباً سالباً بتشكيلة أولِ رحلةٍ مصنوعة** (خمسةُ أبوابٍ احمرّت بأسمائها). **والقيدُ الافتتاحي نُفِّذ**: `fillersOf` وُحّد على `spanOf` (كان مفتاحُ المرحلة ٨ الوصفيّ يصير `NaN` فيبني التنويعُ **أولَ وجهٍ بدل المعلَن** — إحلالٌ صامت) بامتحانٍ بمفتاحٍ وصفيّ في `test_gate.mjs` مُجرَّبٍ سالباً. **ونظافةُ الحظيرة** (بريد العائلة): `sweep_stale()` في `browser_test.py` يكنس يتائمَنا عند الإقلاع بشرطين (سابقةُ `ihsib-*` **ووالدٌ ميّت** `ppid == 1`) مُجرَّباً بثلاثة أوجه (يتيمةٌ قُتلت، وجارٌ ونسخةٌ حيّة لم يُمَسّا)، و`make_welcome_shots` تكنس بسابقتها هي، والإيصالُ أُغلق في `FAMILY_MAIL.md`. وقشرةُ `sw` إلى `v16`، والسَّوقةُ ١٩ حارساً خضراء. **والبلاغ ٣ يبقى مفتوحاً حتى بلاغ المالك المعاد** بعد رؤية الصفحات المغناة على الحيّ.

**وأُنجزت الجلسة م٥** (١٣ أغسطس ٢٠٢٦ — بلاغا **أوّل امتحان طفلٍ حقيقيّ**: حاجزُ القراءة، وتسريبُ العدّ عبر التنقل): **«اجعلهما سواء» بلا نصٍّ تشغيليٍّ واحد** — سقطت خمستُها («أَضِفْ» و«أَزِلْ» تحت كلِّ لوح وزرُّ «صَارَا سَوَاء») فصار الفعلُ **لمساً مباشراً** (نقرُ عنصرٍ يُبعده، ونقرُ فراغِ اللوح يأتي بواحد) **ولا زرَّ تأكيدٍ أصلاً**: بلوغُ التساوي **صنعٌ يُحكَم به** بعد وقفةٍ قصيرة يلغيها تعديلٌ خلالها (والمقيسُ `equal|5|make` كما هو). **والخطأُ صار الحركةَ المبعِدة** — من باعد بينهما عُدَّت الكميتان أمامه، فبقي «الخطأُ يُعَدّ أمامه» في تمرينٍ لا خيارَ فيه يُنقَر. **واللوحُ ينمو وينقص في مكانه** (`room` في المصيِّر: تُحسَب شبكتُه على سَعته لا على عدده اليوم) — وبابُه في `check_render` **مُجرَّبٌ سالباً بلوحٍ بلا سَعةٍ يزحزح ما بقي فعلاً**. **والتساوي يُرى بالميزان المتوازن** (أمرُ المالك): معلمُ مرحلة المقارنة نفسُه من `ui.js` — بين اللوحين لحظةَ بلوغه مع «صَارَا سَوَاءْ»، وفي نمذجة «شاهِد»، **وعلى زرّ «هُمَا سَوَاء»** الذي وقف عنده الطفل (وأوّلُ لقاءٍ به في نمذجة محطته مع «هُمَا سَوَاءْ») — **ولا رمزَ «=»** فهو غير مُدرَّس في المستوى. **وجردُ الصنف تمّ** فسُمّيت خمسةُ أزرارٍ نصيّةٍ إلزامية وأُصلحت (**هُمَا سَوَاء** ميزاناً · **تَمَّ** صحّاً · **تَابِعْ** سهمَ مضيٍّ بعد أن كان محرفَ «←» · **الخريطة** خريطةً مطويّة · **أعِد المحاولة** في البوابة سهمَي إعادة)، **وحارسُ الصنف** يجرد كلَّ زرٍّ ظاهرٍ على **عشر شاشاتِ طفل**: فيه ما يُرى أو في قائمة إعفاءٍ **بسببٍ مكتوب** — **مُجرَّباً سالباً بزرٍّ نصيٍّ مصنوع** يُدسّ في شاشةٍ حيّة فيُمسَك ثم يُرفَع. **وتسريبُ العدّ شُخِّص قياساً وعُولج في موضعه**: `alive` كانت رمزَ جولةٍ داخل الشاشة (`token`) لا حياةً لها، والموجِّهُ يستبدل الشجرةَ **ولا يمسّ المغادَرة** — فتستأنف الحلقةُ بعد الإسكات وتَصُفّ جملتَها التالية **بالجيل الجديد** فتُسمَع فوق الخريطة، والعودةُ تفتح ثانيةً فيتناوبان. فصارت الحياةُ **بقاءَ الجذر في المستند** (`onScreen`) مع رمز الجولة، وأُغلقت ثلاثةُ مواضعَ تصفّ عملاً بمهلة (سؤالُ العدديّة · انتقالُ المراجعة · إعادةُ عرضها). **وحارسُه يمشي المشهد**: عدٌّ حتى «اثنين» ثم مغادرةٌ بزرّ الخريطة، فصمتٌ تامّ ثلاثَ ثوانٍ (صفرُ تشغيل)، ثم عودةٌ تسمع عدّاً واحداً من «واحد» — **مُجرَّباً سالباً بردّ الإصلاح**. **والقاعدةُ دخلت `CLAUDE.md` و`DESIGN.md §٦`**، ونصّان منطوقان جديدان في القائمة، وقشرةُ `sw` إلى `v17`. **والبلاغان ٤ و٥ لا يُغلقان إلا ببلاغِ ميدان الطفل المعاد.**

**وأُنجزت الجلسة م٦** (١٣ أغسطس ٢٠٢٦ — حسمُ بند المراقبة بشهادة الميدان: «لا يوجد submit لصارا سواء فالطفل يبقى يزيد حتى يكونوا سواء أوتوماتيكياً»): **الجوابُ يُعلَن كبساً ولا يقع صدفة** — عاد زرُّ الحكم في «اجعلهما سواء» **ميزاناً بلا حرف** (`markButton` في `station.js`: **صانعٌ واحد** لزرّ الشاشة ولزرّ نمذجتها، ومعلمُه معلمُ مرحلة المقارنة نفسُه من `ui.js`، ومقاسُه فوق هدف اللمس لا عند حدّه) يكبسه الطفلُ ليعلن: سواءٌ ⇒ ميزانٌ يُرى و«صَارَا سَوَاءْ» واحتفال؛ ومختلفان ⇒ **معالجةُ الخطأ القائمة** (تُعَدّ الكميتان أمامه) ثم يعدّل ويُعلن **بلا حدٍّ ولا عقاب** — **وسقط الحكمُ التلقائي ووقفتُه معاً**، واللمسُ المباشر كما هو. **وموضعُ الخطأ انتقل من الحركة إلى الكبسة**: م٥ عدّت الحركةَ المبعِدة خطأً بعلّةٍ مكتوبةٍ يومَها («في تمرينٍ لا خيارَ فيه يُنقَر»)، فلمّا عاد الخيارُ عاد الخطأُ إلى الجواب المُعلَن — **فلا يُكافَأ وصولٌ بلا قصد ولا يُعاقَب تجريبٌ بلا دعوى**، والمقيسُ `equal|5|make` كما هو. **والنمذجةُ تكبس الميزانَ بنفسها** (`model.reveal.press` — بابٌ مُعلَنٌ في `watchStep` كأخيه `sign`، لا تعمل به إلا نمذجةٌ تطلبه): يُخفى الميزانُ حتى تُكبَس صورتُه معطَّلةً، فيرى الطفلُ **ترتيبَ ما سيصنعه بيده** — **بلا نصٍّ جديد ولا صوتٍ جديد**. **وحارسُ الصنف أخضرُ بلا تحويط** (الزرُّ بلا حرفٍ أصلاً، وحكمُ «لا زرَّ نصيّ» صار يقيس النصَّ في الأزرار لا عددَها)، **وحارسُ المتصفّح امتدّ** (§١١ي‌ج — ثلاثةُ أحكامٍ جديدة وحكمان أُعيدت صياغتهما): الزرُّ يبلغ هدفَ اللمس فأكثر **وتبلغه العينُ بلا تمرير** · وكبسةٌ قبل التساوي **تُعالَج ولا تُحتسب نجاحاً** (وذروةُ ما عُلِّم = مجموعُ اللوحين، فلا يُقال «عُدَّ» لأنّ عنصراً أضاء) · **ولا حكمَ يقع من تلقائه** (استواءٌ يُترَك ٢٫٣ ثانية) · والميزانُ يظهر بعد الكبسة · والجولةُ تمضي بإعلان الطفل — **مُجرَّبةً سالباً بردّ الحكم التلقائي**، فاحمرّت ثلاثةُ أبوابٍ ونطق أوّلُها بلفظ البلاغ: «**وقع الحكمُ صدفةً بلا كبسة**». **واللوحان صارا جنباً إلى جنب** (أمرُ المالك في الجلسة): الحارسُ الجديد نفسُه كشف أنّ زرَّ الحكم يقع **تحت طيّة الشاشة بعشرة بكسل** (١٢٠٤ من ١١٩٤) بسبب رصِّ م٥ صفّاً تحت صفّ — فصارا متجاورين ويُرى الثلاثةُ في نظرةٍ واحدة، **وحقُّ الإصبع محفوظ**: الرسمُ يأخذ أكبرَ ما يسعه العرض (`min()` بسقفٍ وحاشيةٍ محسوبة) ولهدف اللمس **أرضيّةٌ ٤٤بك** لا ينزل عنها. وقشرةُ `sw` إلى `v18`، والسَّوقةُ ١٩ حارساً خضراء (١٦٧ فحصاً في المتصفّح، كانت ١٦٤). **وبندٌ مرفوع**: شقُّ «أزرار +/− بأيقونات» لم يُنفَّذ — الميدانُ لم يشكُ اللمسَ المباشر، وردُّ الوسيطين قرارُ منهجٍ لا إصلاح. **والبلاغ ٤ لا يُغلَق إلا بامتحان الطفل المعاد.**

**وأُنجزت الجلسة ١٠** (١٣ أغسطس ٢٠٢٦ — الختام): **إشعارُ التحديث الهادئ** (تتمةُ أمر المالك): قشرةٌ جديدة تتفعّل والصفحةُ مفتوحة تُعلِن نفسَها سطراً «وَصَلَ تَحْدِيثٌ — أَغْلِقْ وَافْتَحْ» — **لا يقطع تمريناً** (لا صوتَ ولا إعادةَ تحميل، **ولا يُرسَم على شاشة تمرين أصلاً**: المواضعُ الهادئة معلَنة فيغيب بالدخول ويعود بالخروج) **ولا يحجب** (سطرٌ في مجرى الصفحة يُقابَل صندوقُه بصناديق الأزرار) **ولا فعلَ فيه**، **وأولُ تولٍّ يُبتلَع** (تثبيتٌ لا تحديث) — ستةُ أبوابٍ في `test_pwa` وتسعةٌ في المتصفّح بحدثٍ مبعوث، **مُجرَّبةً سالباً**. **وأمرا العائلة أُدّيا**: البريدُ المرجع `info@mishkat.qa` **صار محروساً**، وسطرُ بوابة العائلة في فوتر الصفحات الأربع، **وحارسُ «لا رابط خارجيّ» صُحِّح بقراره المعلَن** (استثناءان: عنوانُنا وبوابةُ العائلة) مُجرَّباً سالباً بثلاثة. **والعدّةُ عايرت اللمس فقالت ما لم يكن يُقال**: عدّةُ الجهاز صارت **عشرَ جولاتٍ** (المقاساتُ الخمسة **طولاً وعرضاً**) على **٢٥ شاشةً محسوبةً من المنهج** (نوعٌ جديد يدخلها يومَ يُكتب)، وتقيس **هدفَ اللمس** بعتبة `METHOD.md §١١` **باستثنائها المعتمد وحدَه** — والتنقّلُ بالمقطع لا بإقلاعةٍ لكل شاشة. **فوُجد أربعةُ مواضعَ دون العهد وأُصلحت**: `--tap` كانت **٦٠ لا ٦٤** (`rem` من جذر الصفحة، والتعليقُ يقول «≈٦٤»)، و`.btn` ٤٨ (وهي زرُّ الخروج و«أَعِدْ» وزرّا الميدالية وأزرارُ البوابة)، ورأسُ المحطة المطويّ ٥٢ وزرُّ الترويسة عرضاً ٦١، **و`.same-btn` ٥٦ — وهو الزرُّ الذي وقف عنده الطفلُ في أول امتحان ميدانيّ**. **والمسطرةُ في حارس المتصفّح صُحِّحت** (كانت ٤٤/٤٠ فمرّت خضراءَ على ما ينقض العهد) **وأُضيف بابٌ يقيس نوبةَ الطفل** على عشر شاشاتٍ حيّة — **فأمسك `.same-btn` يومَ كُتب**. **وقياسُ صفحات التعريف دخل `perf-check`** (بايتاتٌ محسوبة لا مقدَّرة)، **وكُنست `vchip`** (٤٢ سطراً ميتاً من البذرة). وقشرةُ `sw` إلى **`v20`**، والسَّوقةُ ١٩ أمراً خضراء (١٧٨ فحصاً في المتصفّح · ١٠ جولات جهاز · ٤٥ في التعريفيّ). **وتقريرُ جاهزية الإطلاق مكتوبٌ في `SESSIONS.md` محسوباً من مصادره — وقرارُ الإطلاق للمالك وحدَه.**

**و🚀 أُطلق التطبيق بأمر المالك** (١٣ أغسطس ٢٠٢٦، بلفظه: «اطلق… الآن نطلق ونعده منتهي حاليا») — **حيٌّ على `https://calc.mishkat.qa/`** بقشرة `v20`، الرحلةُ ٥٩ عقدةً منطوقةً كلُّها (٧٢ ملفاً، القائمةُ صفر)، والسَّوقةُ ١٩ أمراً خضراء، وبلاغاتُ الميدان الخمسة مغلقة. **وما بعد الإطلاق ميدانٌ**: كلُّ بلاغٍ يُقيَّد خاماً في `FIELD.md` ثم تُفتَح له جلسةُ إصلاحٍ بحدودها، **ولا يُغلَق إلا ببلاغٍ معاد**. إن كنتَ جلسة تنفيذ: اقرأ `METHOD.md` ثم `SEED.md` ثم بند جلستك في `SESSIONS.md`، ونفّذ حدودها فقط، وحدّث حالتها عند الإتمام.

**وأُنجزت الجلسة ش** (١٣ أغسطس ٢٠٢٦ — بعد الإطلاق): **المرحلة ٩ «الأشكالُ الأولى» تُلعَب في ذيل الرحلة قبل بوابة الختام** فصارت الرحلةُ **٦٣ عقدة** (يطبعها `test_journey` من ثلاثة مصادر ويقابلها المتصفّحُ برسم الخريطة)، **ولا محطةَ قائمة تبدّلت ولا ق٣ مُسّ** (أكبرُ عددٍ فيها خمسة). أربعُ محطات: **متباعدا الصورة** (دائرةٌ ومثلث) ثم **الشبيهان قصداً** (مربّعٌ ومستطيل) ثم **«كم ضلعاً؟»** ثم **«الأشكال حولنا»**، ومفاتيحُها الثلاثة (`shape|4|name` · `sides` · `world`) تدخل ليتنر والمراجعةَ **وبوابةَ الختام** (تبعت الرحلةَ فصارت `after: stage9`، وأثبته `test_gate` بسجلٍّ مصنوع: مفاتيحُ الأشكال تتصدّر جلستَها)، ولوحةُ الوالد كسبت **قسماً ثامناً**. **والشكلُ يرسمه المصيِّرُ هندسياً** (صنفٌ سادس: كلُّ ضلعٍ قِطعةٌ تُعلن نفسَها، و**`data-sides` يُعَدّ من الـDOM بعد الرسم**) — **لا إيموجي ملتبس**، ومنتصفُ الضلع موضعُ لمسه من المصيِّر نفسِه. **والتناظرُ الفرديّ في عدّ الأضلاع** (الملموسُ يُعطَّل)، **والخطأُ يُعَدّ أمامه** (يُضاء المطلوبُ وتُعَدّ أضلاعُه ثم يُسمّى، والدائرةُ تُضاء وتُسمّى إذ لا ضلعَ لها)، **ولا نصَّ تشغيلياً واحداً**. **والترحيلُ الرحيم شرطُ قبولٍ نُفِّذ**: **ما أنجزه الطفلُ يبقى مفتوحاً** ولو دخلت الرحلةَ محطاتٌ قبله — فلا تُقفَل على صاحبها بوابةٌ عبرها، وحارسُه يقيس **كلَّ مرحلةٍ كأنّها الداخلةُ اليوم** مُجرَّباً سالباً. **وأربعةُ حرّاسٍ امتدّت مُجرَّبةً سالباً** (`check_range` بابا ١د و١د‌ب: بنيةُ الجدولين و**لا شكلَ قبل موضعه** · `check_render` بابٌ ٣ح: **الأضلاعُ المرسومة هي المُعلَنة** والمربّعُ سواءٌ والمستطيلُ ليس · `test_journey` بابٌ ٦ · `test_gate` بابٌ ٨)، **ومعها عشرةُ أحكامٍ في المتصفّح بجدول حقيقةٍ مكتوبٍ بيد** (`SHAPE_TRUTH`) وجردُ الصنف امتدّ إلى ١٦ شاشةَ طفل. **والصفحاتُ التعريفية تبعت الرحلة في جلستها** (بطاقةُ المرحلة ٩ ولقطتُها والأرقامُ المحسوبة وسطرُ قرار الموضع في «الأسس» و`METHOD §١٢`). وتسعةُ نصوصٍ منطوقة في القائمة (أسماءُ الأشكال وأسئلتُها) **لم تُولَّد**، والسَّوقةُ **١٩ حارساً خضراء** (١٩١ فحصاً في المتصفّح · ١٠ جولات جهاز · ٤٥ في التعريفيّ)، وقشرةُ `sw` إلى **`v21`**، **ولم يُدفَع شيءٌ إلى GitHub** — وبوابةُ عين المالك على الأشكال قبل النشر. **وأربعةُ بنودٍ مرفوعة**، أوّلُها موضعُ قرار: **مادّةُ «الأشكال حولنا»** — عناصرُ `OBJECTS` الثمانية ليس فيها مثلثٌ ولا مربّعٌ صادق، وإدخالُ رسومٍ جديدة يقتضي عينَ المالك سلفاً، فبُنيت أشياءُ العالم (بيتٌ وقطارٌ وشجرةٌ وقارب) **من أشكالنا نفسِها**.
**وأُنجزت الجلسة ع١** (١٣ أغسطس ٢٠٢٦ — تقليمُ السَّوقة وهمزةُ الوصل): **العدّةُ صارت تُقلَّم باشتقاق** (أعلاه) — ستةَ عشرَ حارساً يعلنون مادّتهم، و`--touched` يشتقّ، وبطاريةُ المتصفّح ثلاثةَ نطاقات **تُعلن ما لم تقسه** (لا سقفَ صامت)، وأدواتُ السَّوقة المشتركة رُفعت فوق النطاقات فلا يسقط سائرُها بسقوط أحدها. **وقرارُ همزة الوصل نُفِّذ**: بابٌ في `check_speech` **بلا قائمة إعفاء** (كلُّ `ا` عاريةٍ تليها حركة — والقطعُ بهمزته والمدُّ والتنوينُ يمرّون) مُجرَّبٌ سالباً، **والسبعةُ بُدِّلت في مواضعها كلِّها** (٢٩ موضعاً: `NUMBER_NAME` و`SAY` و`SPOKEN` وعناوينُ المحطات والتلميحاتُ والصفحتان التعريفيتان — **والمعروضُ يتبع المنطوق**)، **والقديمةُ تُقاعَد بجردها لا بيد** (`queue_texts --retire`: يبقى سجلُّ ما سُمع ويخرج من المنجَز والمنتظِر معاً، وحذفُ الملفّ لجلسة الصوتيات). **والنطقُ الآليّ صار يُقاس كما يُقاس الملفّ** في حارس المتصفّح (`earLog` يجمع القناتين) فلا ينجو منه احتياطٌ ولا يحمرّ الحارسُ على نصٍّ ينتظر تصريفَه. **وشقُّ التقليم دُفع وحدَه** (لا ملفَّ تطبيقٍ فيه)، **وقيدُ نشرِ شقِّ الهمزة انكسر** — نصَّ بلاغُ ع١ أنّه «ملتزَمٌ لم يُدفَع» وكان مدفوعاً منشوراً (سيرُ Pages ١٠:٣٧ ت.ع)، **فمضى على الحيّ نحوَ ساعةٍ وربعٍ بسبعة نصوصٍ بلا ملفّاتها** يسمعها الطفلُ نطقاً آلياً، ومنها «اثْنَانْ» في كلِّ عدّ — **وأغلقته ص٦** بتصريف السبعة ودفعها. **وبندٌ مرفوع**: أتُعمَّم قاعدةُ همزة الوصل على المعروض غير المنطوق (عناوينُ «اِجْمَعْ» و«اِطْرَحْ»، والعلامةُ «اِحْسِبْ» نفسُها)؟

**وأُنجزت الجلسة ث** (١٣ أغسطس ٢٠٢٦ — حزمةُ الإثراء كاملةً): **ثمانِ محطاتٍ دخلت الذيلَ فصارت الرحلةُ ٧١ عقدة** (يطبعها `test_journey` من ثلاثة مصادر: ٦٨ محطة و٣ بوابات و١٤ قسماً) — **ولا محطةَ قائمة تبدّلت ولا ق٣ مُسّ بحرف**. **والمرحلة ١٠ «القِسْمَةُ بِالعَدْل» تُلعَب** (`share.js` — `share|10|make` و`share|10|each`): **القسمةُ تُصنَع باللمس** بلوح «اجعلهما سواء» بعينه (كومةٌ وأوعية، لمسةٌ داخل الوعاء تأتي بواحدٍ ولمسةٌ على ما فيه تعيده) **والجوابُ يُعلَن كبساً** (`markButton` بمعلم `share`، تكبسه نمذجتُها بنفسها)، **وحدُّ العدل مُعلَن**: تستوي الأوعيةُ **ولا يبقى في الكومة ما يُقسَم** — فيصدق على المنقسم وذي البقيّة بحكمٍ واحد، **والباقي يُرى ويُسمّى ولا يُعَدّ خطأً**، **وعدُّ الخطأ توزيعٌ بالتناوب أمامه** بـ`countMarks` الواحدة. **والمرحلة ١١ «القِيَاسُ بِوَحْدَة» تُلعَب** (`units.js` — `measure|20|units`) **بصنف مصيِّرٍ سابع**: **الوحداتُ تُرصّ متلاصقةً من طرف الشريط — بنيةً لا وعداً** (موضعُ الوحدة مشتقٌّ من دليلها وعرضُها واحد، فلا فجوةَ ولا تراكب بالحساب)، **والوحدةُ واحدةٌ في السؤال**، وثلاثةُ أوجه (عدٌّ ببطاقة رمز · شريطان **يفترقان بوحدةٍ واحدة** فيحكم العدُّ حيث تخدع العين · و«قِسْ بيدك» **لا يستطيع تجاوزَ الشريط** ثم يُعلن قياسَه كبساً بمعلم `ruler`). **ومحطةُ الترتيبيّة في ذيل المرحلة ٨** (`rank|five` — `order|5|rank`، والتعارفُ أُزيح إلى ذيلها): **لغتُها أسماءُ الترتيب لا الأعداد** (`ORDINALS` في المنهج) **ولا رقمَ على الشاشة البتّة**، **والأوّلُ من اليمين**، ومشيُها `walkRanks` لا `countMarks` **بعلّةٍ مكتوبة** (تلك تنطق أسماءَ الأعداد بحكم بنائها، وهو عينُ ما تنفيه المحطة). **وبوابةُ الختام تبعت الرحلة** (`after: stage11`) فشملت المفاتيحَ الثلاثة، **والترحيلُ الرحيم مُثبَتٌ بسجلٍّ مكتمل** (١١ مرحلةً تُقاس كأنّها الداخلةُ اليوم). **ولوحةُ الوالد كسبت قسمين** (٨→١٠): **القسمة**، **والقياسُ أُفرد عن الأنماط** يومَ صار للمفهوم وجهان — وصفيٌّ بالعين وعدديٌّ بالوحدة، **والمدى يُسمّى بلسان مفهومه** (`numeric` في `CONCEPTS`: «بالوحدات حتى ٨» لا «٢٠» خاماً) **وما أتقنه يُقال كلُّه** (كان العدديُّ يرجّح فيُسقِط الوصفيَّ من سطر الوالد). **وأربعةُ حرّاسٍ امتدّت مُجرَّبةً سالباً**: `check_range` بابٌ ١هـ (جداولُ الإثراء الثلاثة — **وباقي كلِّ مجموعٍ يوافق موضعَه**، **ولكلِّ موضعٍ في أطول صفٍّ اسمُ ترتيبه**) · `check_render` بابٌ ٣ط (**لا فجوةَ ولا تراكب** ولا مسطرتان، **ويُكنَس بكلِّ طولٍ وبكلِّ ما رُصّ** لأنّ الغلطَ في نصف الطريق لا في تمامه) · `browser_test` **§١١ل بستّةَ عشرَ حكماً** وجردُ الصنف ومسطرةُ اللمس إلى **٢٥ شاشةَ طفل** · `test_measure` بثلاثة صفوف. **وأمسك حارسُ المتصفّح عيباً لا يمسكه القرصُ**: زرُّ «ضَعْ وَحْدَة» كان ينادي دالّةً أُعيدت تسميتُها فتُلقي خطأً عند كل نقرة — **والسَّوقةُ السريعة كلُّها خضراء** والشاشةُ تُرسَم، والطفلُ لا يستطيع وضعَ وحدةٍ واحدة. **وأمسك بابُ الجلسة الافتتاحيّ عيباً حيّاً على المنشور**: «الْمِسْ وَعُدَّ» له ملفٌّ في الشجرة وليس في الفهرس المنشور (وملفُّه غيرُ متتبَّعٍ في git) — **بندٌ مرفوع**. والصفحاتُ التعريفية تبعت في الجلسة نفسِها (٣ بطاقات وقسمان ولقطتان — التشكيلةُ ٢٠ — وأرقامٌ محسوبة وصفُّ قرارٍ في «الأسس»)، **و١٦ نصّاً منطوقاً في القائمة لدفعة ص٧ ولم يُولَّد صوت**، **والسَّوقةُ ١٩ حارساً خضراء** (٢٠٨ فحصاً في المتصفّح · ١٠ جولات جهاز · ٤٥ في التعريفيّ)، وقشرةُ `sw` إلى **`v22`**، **ولم يُدفَع شيءٌ إلى GitHub** — وبوابةُ عين المالك على لوحَي القسمة والقياس (`?dev=1#/render/share` و`#/render/units`). **وأربعةُ بنودٍ مرفوعة**.

**وأُنجزت الجلسة م٧** (١٥ أغسطس ٢٠٢٦ — البلاغ ٦: «بعد اختيار الرمز الخطأ، لم يسمح بالعودة واختيار الرمز الصحيح»): **العلّةُ قِيست ولم تُخمَّن، وهي غيرُ التي في الخيط** — لا مخرجاً مبكراً بل **عطباً يُلقى**: بطاقةُ الاختيار (`cardOf`) كانت تُرجِع **نسخةً مُنتقاة** من الشكل بلا `ticks`، **وهي البطاقةُ التي تُعَدّ أمام الطفل** في «أَيْنَ هَذَا الْعَدَدْ؟» (عهدُ `numerals.js` الرابع)، فتُلقي `countAloud` عطباً (`TypeError: fig.ticks is not iterable` — مُلتقَطاً في متصفّحٍ حيّ، **وصفرُ عدٍّ وقع**) فيُرفَض وعدُ المعالِج **ويبقى القفلُ مرفوعاً وأثرُ الأحمر**. فصار جردُ البطاقة **`figureOf` نفسَه** كأختها `figureBox` — موضعٌ واحد للجرد لا انتقاءٌ يفوته ما يُزاد فيه. **وعلّةُ نجاته من الحرّاس مقيسة**: بابُ «الخيارُ الخاطئ لا ينقل» يقع على أوّل جولةٍ موجَّهة وهي **ذهاباً دائماً**، والعطبُ **إياباً وحدَه**. **والعلاجُ بنيويّ: `roundGate` في `station.js` مالكُ القفل الواحد** — يُؤخَذ بدخول المعالِج **ويُرَدّ في كل حال** (عودةً أو مخرجاً مبكراً **أو عطباً يُلقى**) إلا أن تُعلن الجولةُ انتهاءها (`end`)، **والعطبُ يُطبَع باسم شاشته ولا يُبتلَع**، **وللألواح `onFree`** يُعاد به رسمُ اللوح كلّما رُدَّت الحرية فلا يبقى زرٌّ معطَّلاً. **و`let locked` سقطت من الشجرة كلِّها** (٢٩ بوابةً في ١١ ملفّاً، ومعها `judged` في الألواح الثلاثة) — **ولا سطرَ `locked = false` في نهاية مسارٍ في التطبيق**. **وحارسٌ جديد `§١١م` يقيس ما رأى الميدان**: خطأٌ ← يُنتظَر ذهابُ الأحمر ← **ثمّ صوابٌ يجب أن يُقبَل**، **بالمُصيِّر لا بالمحطة** (عشرُ شاشات، وألواحُ الكبس مقيسةٌ في §١١ي‌ج و§١١ل)، **ولوحُ الأجزاء يُخطَأ ببنيته** (تُملأ خاناتُه بما لا يُتمّ الكلّ، **والأجزاءُ الأولى أصغرُ ما في الحوض** وإلّا لم يبقَ للأخيرة إلا الصحيحة — عيبٌ أمسكه الحارسُ في نفسه يومَ كُتب)، **وبابٌ ثانٍ يمشي محطةَ الرموز حتى يقع الخطأُ في الوجهين معاً** فلا ينجو وجهٌ بقرعةِ أوّلِ جولة. **والتجربةُ السالبة نطقت بلفظ البلاغ**: رُدَّت `numerals.js` إلى ما قبل الجلسة فاحمرّ الوجهُ الثاني ستَّ مرّات بجملة «بقي أثرُ الخطأ ولم تُرَدَّ الشاشةُ — الجولةُ أُغلقت» **ومرّ الوجهُ الأول** (لم يكن معطوباً قطّ) **وبقيت العشرُ خضراء**. و`answerOf` كسبت وجهاً كان ناقصاً («أَيُّهُمَا أَكْثَرْ؟» كان جوابُها `NaN` — أمسكه الحارسُ الجديد في أوّل تشغيل). **وحكمُ المدير على بند ث الرابع نُفِّذ**: `measure|8|units` **مُشتقٌّ من `UNIT_SPAN.max`** لا مكتوباً بيد (فلا يفترق المفتاحُ عن الشريط)، وتبعته لوحةُ الوالد. **والسَّوقةُ الكاملة خضراء — ١٩ حارساً** (٢١٠ فحصاً في المتصفّح · ١٠ جولات جهاز · ٤٥ في التعريفيّ)، واللقطتان المرجعيتان أُعيدتا (`shots/render/`)، وقشرةُ `sw` باقيةٌ على `v22` (رفعةٌ واحدة للحزمة، ولا ملفَّ جديد)، **ولم يُدفَع شيءٌ البتّة** — **جلسةُ ص٧ تدفع الكلَّ بعد توليد أصوات ث** (قرار المالك). **والبلاغ ٦ لا يُغلَق إلا ببلاغٍ معادٍ من الميدان بعد النشر.**

**وأُنجزت الدفعة الصوتية السادسة** (الجلسة ص٦، ١٣ أغسطس ٢٠٢٦): **بدائلُ همزة الوصل السبعُ مُصرَّفةٌ** بصوت سُلافات (١٧٫٠ث · ١٣٣٫١ ك.ب) **والقائمةُ صفر** — **استبدالٌ لا زيادة**: فالبنكُ **٨١ ملفاً كما كان** (١٩٦٫٨ث)، **والسبعُ المُقاعَدةُ حُذفت ملفّاتُها** بجردِ اليتائم من الفهرس مقابَلاً ببصمة كلِّ نصٍّ مُقاعَد (سبعٌ بسبع)، ومدخلاتُها باقيةٌ سجلاً لما سُمع. **وقاعدةُ «فحصٌ بقدر العمل» عملت**: `--touched` اشتقّ **٤ حرّاسٍ** ومرّ على **١٢ بأسمائهم**، و`--scope audio` **١٢ فحصاً خضراء** بصفر لجوءٍ إلى النطق الآليّ، **والكاملةُ عند الدفع**. **والمراجعةُ بالأذن معلَّقةٌ على المالك** (الدفعتان الخامسة والسادسة).

**وأُنجزت الدفعة الصوتية السابعة** (الجلسة ص٧، ١٥ أغسطس ٢٠٢٦): **ستةَ عشرَ نصّاً لحزمة ث مُصرَّفةٌ** بصوت سُلافات (١٤ `instruction` و٢ `modeling` — القسمةُ بالعدل والقياسُ بوحدة والترتيبيّة، ٣١٫٦ث · ٢٤٦٫٨ ك.ب) **والقائمةُ صفر والرحلةُ كلُّها منطوقة** — **زيادةٌ لا استبدال**: فصار البنكُ **٩٧ ملفاً** (٢٢٧٫٤ث) بعد أن كان ٨١. **صفرُ فشل وصفرُ محجوز**، والتحقّقُ الختاميّ ٩٧ بـ٩٧ بلا يتيمٍ ولا مبتورٍ ولا بصمةٍ كاذبة. **ولم يُلمَس ملفُّ تطبيقٍ واحد** — إلا **أربعةَ أرقامٍ في الصفحات التعريفية طلبها حارسُها** (`data-stat="audio"`: ٨١ ← ٩٧، احمرّ `test_welcome` في ثلاثة مواضعَ لحظةَ تمّ التصريف). **والمسطرةُ تحرّكت ثانيةً والبايتاتُ لم تتحرّك**: دخولُ ١٤ نصّاً قصيراً خفّض وسيطَ `number_name` فارتفع فوقه ثلاثةُ أسماءٍ من الدفعة الثالثة («سِتَّةَ عَشَرْ» و«ثَلَاثَةَ عَشَرْ» و«تِسْعَةَ عَشَرْ» — ١٫٩× الوسيط) **وقد أقرّتها الأذنُ سلفاً ولم يتغيّر فيها بايت** — فالتنبيهُ خبرٌ لا مطالبة، **والقرارُ مرفوع**. **وحزمةُ ث وإصلاحُ م٧ دُفعا معها** (قرار المالك: جلستان ثم نشرٌ واحد) — والسَّوقةُ الكاملة خضراء قبل الدفع، **وبابُ «منطوقٌ بلا صوتٍ على المنشور» أخضرُ على الحيّ** (٩٧ نصّاً لكلٍّ ملفُّه في الفهرس المنشور). **والمراجعةُ بالأذن معلَّقةٌ على المالك** (الخامسة والسابعة).

**وأُنجزت الجلسة غ** (١٥ أغسطس ٢٠٢٦ — «بلِّغنا» نمطُ عائلة، بلاغ `owner-decisions-family-structure` §٥): **بابُ البلاغ والاقتراح في ذيل لوحة وليّ الأمر** — `app/js/feedback.js` **وحدةً مستقلة**: رابطا `<a>` (واتساب `+974 3388 2806` وبريد `info@mishkat.qa`) **مملوءان بسطر سياق** (اسمُ التطبيق ونسخةُ قشرته — **تُسأل من عاملها ولا تُكتب بيد**، والرابطان يعملان قبل جوابها)، **وصفرُ شبكةٍ من التطبيق نفسِه** (`<a>` تُفتح ولا تُجلَب) **وصفرُ بياناتِ طفلٍ في الرسالة**، والمرسِل يرى نصَّه كاملاً قبل الإرسال، **ولا زرَّ في شاشة طفل** (خلف البوابة الحسابية — فعلُ راشدٍ متعمَّد). **والمرجعُ بالتزامٍ مبصوم `read@cee7bfb`** لا `read@HEAD` (قاعدةُ النقل) **وقد صحّت عملاً**: هو الالتزامُ الذي أصلح أحمرَ الفصل هناك، فلو نُسخ سلفُه لَنُقل العيبُ معه. **ودرسُ الفصل يسري بنيةً لا وعداً**: هناك يمنع حارسُ الخصوصية `https://` في وحدةٍ يمرّ بها صوتُ الطفل ولوحتُهم تحمل «تسجيلات طفلي» — **وعندنا لا صوتَ طفلٍ أصلاً**، فالفصلُ **حصانةٌ لِما قد يُستجدّ**. **وحارسُه `test_feedback.mjs` منقولٌ بجدوله وممتدّ** (٣٠ حكماً): **وحكمُ الفصل عُمِّم** — هناك يُسأل `parent.js` وحدَه عن العنوان الخارجيّ، **وعندنا تُسأل وحداتُ `js/` الأربعُ والعشرون** فالعنوانُ **في وحدته وحدَها**؛ وبابان زائدان للتكييف (**لا اسمُ أخيه فيما يُرسَل** — يُقاس على الشيفرة بعد نزع الترويسة، **ولا تعرف الوحدةُ سجلَّ الطفل**) — **مُجرَّبٌ سالباً بأربع**، **ويدخل السَّوقةَ بالجرد** لا بسطرٍ يُضاف. **ولم تُمَسّ شاشةُ تمرينٍ واحدة ولا المنهجُ ولا الصفحاتُ التعريفية** ولا نصَّ منطوقاً جديداً، وسطرٌ واحد في `app.css` (`a.btn` — رابطٌ يلبس زيَّ الزرّ)، وقشرةُ `sw` إلى **`v23`** (ملفٌّ جديد فيها)، والسَّوقةُ الكاملة خضراء ثم دُفعت الحزمةُ ونُشرت.
- **الوصلة التي تتعلّق بها الوحدات**: الرحلةُ تُقرأ من `sections()` في `app/js/curriculum.js` (وحدَه)، والقسمُ يصف نفسَه للخريطة، و**الشاشةُ تسجّل نفسَها** بـ`registerScreen` من **`registry.js`** (لا من `main.js` — دَورُ الاستيراد وعقدُ المولّد النقيّ، `SEED.md §٢`)، و**التمرينُ يسجّل نفسَه** بـ`registerExercise` في `station.js` الذي يحقن المراجعةَ مرّةً واحدة (والسجلُّ **نوعٌ ← قائمةُ مالكين**: نوعُ التمرين قد يشترك فيه مفهومان — `equal|5|make` و`bond|10|make` — فيُسأل كلُّ مالكٍ بدوره ويجيب صاحبُها، والجوابُ يحمل مالكَه فيرسمه مُصيِّرُه هو) — فمحطةٌ جديدة تدخل الرحلة بلا سطرٍ يُعدَّل خارج بيانات المنهج، ووحدةُ تمارينٍ جديدة سطرُ استيرادٍ واحد في `main.js` لتصلها الشجرة. **والكميةُ تُرسم من `render.js` وحدَه**: `plan()` هندسةٌ خالصة بلا DOM، و`paint()` يبني الشكل **ويقرأ ما رسم من الـDOM لا من العدد المطلوب** — ولكلِّ صنفِ رسّامٍ قارئُه، **وهي سبعةٌ** (الكمُّ تُعَدّ عناصرُه · الرمزُ يُقرأ نصُّه عدداً · الخطُّ تُعَدّ علاماتُه فيُرَدّ مداه · **الشريطُ تُعَدّ خاناتُه** وخانةُ السؤال منها · **المشهدُ تُعَدّ أشياؤه** ولكلٍّ **رمزُه المُعلَن**، ومقدارُه معه في وجه المقادير وحدَه · **والشكلُ تُعَدّ أشكالُه** ولكلٍّ أضلاعُه معدودةً من قِطَعه · **والشريطُ المقيسُ تُعَدّ وحداتُه** وطولُه `data-span` محسوبٌ من عرضه المرسوم) — فلا تكتب شاشةٌ كميةً ولا رقماً بيدها ولا تدّعي عدداً، **وجوابُ التمرين يُقرأ من `drawn` لا من العدد المطلوب** (وفي النمط من `data-item`، وفي المشهد من `data-size` في وجه المقادير، ومن `data-item` مقابَلاً برتبته في `SCENES` في وجه الأشياء — الجلسة ٨ب).
- **عهودُ شاشات التمارين** (الجلسات ٣–٥، `DESIGN.md §٦`): **النصُّ في شاشات الطفل زينةٌ للوالد لا وسيلةُ تشغيل — ولا فعلَ يلزم الطفلَ وسيلتُه الوحيدة قراءة** (م٥ ببلاغ الميدان ٤: جمهورُنا ٤–٧ **قبل-قارئ**، ووقف طفلٌ حقيقيّ عند زرٍّ مكتوب): فكلُّ زرٍّ تتوقف عليه اللعبة إمّا **أيقونةُ بطلٍ تُرى ونطقٌ أوّلَ ظهور** وإمّا **يسقط الوسيطُ** إلى فعلٍ مباشر، ويحرسه **حارسُ الصنف** في `browser_test.html` بقائمة إعفاءٍ **بسببٍ مكتوب** مُجرَّباً سالباً بزرٍّ نصيٍّ مصنوع. **والجوابُ يُعلَن ولا يقع** (م٦ ببلاغ الميدان): ما يُتِمُّه الطفلُ **حالاً تُرى** قد يُبلَغ عرَضاً بالنقر المتكرّر، فلكلِّ تمرينٍ **فعلُ إعلانٍ** صورتُه معلمٌ يُرى (`markButton`) **تكبسه نمذجتُه بنفسها** (`reveal.press`)، **وحيث يُعلَن الجوابُ يقع الخطأ** — لا يُكافَأ وصولٌ بلا قصد، ولا يُعاقَب تجريبٌ بلا دعوى. **وكلُّ حلقةٍ تموت بمغادرة شاشتها** (م٥ ببلاغ الميدان ٥): حياتُها **بقاءُ جذر شاشتها في المستند** (`onScreen` في `ui.js`) لا رقمٌ فيها — فالخريطةُ صامتة والدخولُ من صفرٍ دائماً، وما يُصَفّ بمهلةٍ يسأل الحياةَ **عند وقوعه**. · حلقةُ المحطة أربعُ خطوات و«وحدك» وحدَها مقيسةٌ في ليتنر · **الرمزُ لا يُعرَض إلا بعد عدّ كمّه ويُكشَف فوقها**، والمطابقةُ ذهاباً وإياباً في المحطة الواحدة · **مفتاحُ ليتنر لكلِّ رمزٍ على حدة** مقروءاً من `skills` في المنهج · **لا رمزَ عدديّ على شاشات المرحلتين ١–٢** (الخطواتُ أسماؤها والجولاتُ خرزات والجوابُ بطاقةُ كمّ) · التناظرُ الفرديّ **امتناعٌ في البنية** (زرُّ العنصر المعدود يُعطَّل) · الخطأُ **يُعَدّ أمامه** ولا ينقل ولا يُلقَّن · الخطفُ يُعاد **بلا حدٍّ ولا احتساب** ولا عدّادَ يقيس زمناً · وكلُّ نصٍّ منطوقٍ **يُعلَن** في `SPOKEN` ويُصَفّ في القائمة · **والصوتُ قناةٌ واحدة** (م١): لا عنصرَ صوتٍ خارج `audio.js`، و`say` تُرجِع وعداً يتمّ بتمام الكلام، **وكلُّ انتقالٍ ينتظره** (`praiseThen` للصواب)، والإسكاتُ يُفرغ الطابور. · **والمرحلةُ ٥ بلا رمز عمليةٍ البتّة** (يفرضه الفاحصُ وحارسُ المتصفّح): الكلُّ يُسمّى رمزاً و**الأجزاءُ تُبنى كمّاً**، و**الشقُّ من المصيِّر** (`split`) لا من الشاشة، والتجاوزُ **امتناعٌ في البنية** (بطاقةٌ تزيد على ما بقي تُعطَّل) وما وُضع **يُرَدّ بلمسة**. · **والمرحلةُ ٦ عمليةٌ تُفعَل قبل أن تُسمّى**: **رمزُ العملية من جبهة المحطة حصراً** (لا تكتبه شاشةٌ بيدها، وجولةٌ تعلن `ops` و`signs` فيقابلهما الفاحص)، و**كلُّ حقيقةٍ تُعرَض كمّيةً ورمزاً معاً أوّلَ لقاء** ثم بالرمز وحدَه **والكمّيةُ عوناً عند النقر** بلا حدٍّ ولا احتساب، و**الجوابُ من المرسوم** في الأوجه الثلاثة، و**الخطأُ يُعَدّ بعدّ درسِه** لا بعدٍّ واحدٍ للجميع. · **والمرحلةُ ٧ تُبنى ولا تُعَدّ فرادى** (`METHOD.md §٢.٢` المعتمد): لا لمسَ ولا عدَّ آحادٍ فوق العشرة — **حزمةٌ تُعلَّم دفعةً واحدة وتُسمّى تامّة ثم تُعَدّ الآحادُ فوقها**، والبناءُ خانتان (حزمةٌ وآحاد) والقراءةُ **بعده** لا قبله، والعبورُ صعوداً **سؤالان** («كم بقي لتمتلئ العشرة» ثم «كم صارت معاً») ونزولاً **وقفةٌ عند العشرة**. · **والمرحلةُ ٨ تُقاس بالوجه لا بالعدد** (مدياتُ مفاتيحها وصفية): النمطُ **إيقاعٌ يُرى** وجوابُه ما يبعد دورةً كاملة **مقروءاً من الشريط المرسوم**، والقياسُ **وجهان يقفان على خطٍّ واحد** (الجلسة ٨ب): الطولُ مقاديرُ الشيء الواحد والمطلوبُ من `data-size` المرسوم · **والثِّقَلُ والزمنُ أشياءُ مختلفة بمقدارٍ واحد** من `SCENES` (جدولٌ مستقلٌّ عن المعدود) والمطلوبُ **رتبةٌ معلَنةٌ في المنهج يُقابَل بها الرمزُ المقروء من `data-item`** — «الفيلُ أثقل» بيانُ منهجٍ لا هندسةُ رسم، **ولا حكمَ في الحجم** (يفرضه حارسُ المصيِّر)، ولا رتبتين متساويتين في سؤال (يفرضه الفاحص)، **ومحطةُ التعارف عرضٌ بلا سؤالٍ ولا خطأ** ونجمتُها بلا محاولة — وهي وحدَها فيها رقمٌ مغربيّ (من `latinNum` لا مكتوباً بيد). · **والمرحلةُ ٩ شكلٌ يُرسَم ولا يُستعار** (الجلسة ش): أربعةُ أشكالٍ ببياناتها في `SHAPES` (اسمٌ يُنطَق · أضلاعٌ تُدرَّس · موضعُ تقديمٍ يُقرأ منه حوضُ الخيارات)، **وعددُ الأضلاع محسوبٌ من المرسوم** (`data-sides` يُعَدّ من قِطَع الشكل في الـDOM) لا من الجدول، **والتناظرُ الفرديّ في عدّها** كما في «المس وعُدّ»، **والخطأُ يُعَدّ أمامه** (يُضاء المطلوبُ وتُعَدّ أضلاعُه ثم يُسمّى)، **وأشياءُ «الأشكال حولنا» مبنيّةٌ من أشكالنا** (`SHAPES_WORLD` — بندٌ مرفوع) فلا دعوى شكلٍ على صورةٍ لم نرسمها. · **والمرحلةُ ١٠ قسمةٌ تُصنَع باللمس بلا رمز** (الجلسة ث): لوحُ «اجعلهما سواء» بعينه — كومةٌ وأوعية — **وحدُّ العدل أن تستوي الأوعيةُ ولا يبقى ما يُقسَم**، **والباقي يُرى ويُسمّى ولا يُعَدّ خطأً**، **وعدُّ الخطأ توزيعٌ بالتناوب** بـ`countMarks` الواحدة، ومجاميعُ كلِّ محطةٍ **بيانُ منهجٍ بموضعها** (`SHARE`) يفرض الفاحصُ أن **يوافق باقيها موضعَها**. · **والمرحلةُ ١١ قياسٌ بوحدةٍ تُرصّ متلاصقةً — بنيةً لا وعداً**: موضعُ الوحدة **مشتقٌّ من دليلها** وعرضُها واحد (فلا فجوةَ ولا تراكب ولا مسطرتان — يفرضه المصيِّرُ وحارساه على الخطة وعلى البكسل)، **والتجاوزُ ممتنعٌ** (وحدةٌ فوق طول شريطها `RangeError`)، **والجوابُ من المرسوم** (`[data-unit]` تُعَدّ و`data-span` من عرض الشريط). · **والترتيبيّةُ لغتُها أسماءُ الترتيب لا الأعداد** (`ORDINALS` في المنهج) **ولا رقمَ على شاشتها**، **والأوّلُ من اليمين**، ومشيُها يسمّي المواضعَ ولا يعدّها (فـ`countMarks` تنطق أسماءَ الأعداد بحكم بنائها، وهو عينُ ما تنفيه المحطة). · **ولكلِّ بوابةٍ مدَاها المعلَن** (`from` في المنهج): الأولى ١–٤ · **والعمليات ٥–٦** · والثالثة الرحلةُ كلُّها — والتنويعُ وحدَه من الحصيلة كلِّها. · **وميداليةُ الختام معلمُ مرحلتها** (الجلسة ٨): لا أيقونةَ واجهةٍ محايدة — من `mark` المعلَن عبر `medalOf`، والمعلمُ **يتبع حبرَ موضعه** (`currentColor`) فيقول كلُّ موضعٍ لونَه مرّةً واحدة في اللوح.
- **لوحةُ وليّ الأمر تُترجم ولا تخترع** (الجلسة ٨، `METHOD.md §٦` و§١٣): **مفهومٌ بعملية لا درجة** — العباراتُ وأقسامُها السبعة **بياناتُ منهج** في `curriculum.js` (`CONCEPTS`)، **والمدى يُقرأ من ليتنر الحيّ** لا يُكتب بيد (أقصى ما بلغ صندوقَ الإتقان في «يفعل»، وأضعفُ مهارةٍ في «يحتاج»)، ولا رقمَ مغربيّ ولا مفتاحَ خامٍ في سطرٍ يقرؤه والد. **وتدريسٌ وقياسٌ لا تشخيص**: «راجِع مختصاً» **إشارةٌ تُرفَع باسم المفهوم** عند تعثّرِ ثلاثةِ أسابيع في الصندوق الأدنى (`since` في السجلّ)، لا حكمٌ ولا نسبة. **والنسخةُ الاحتياطية تحفظ الرحلةَ كاملةً** (نجومٌ ومهاراتٌ بمواعيدها ودقائقُ ومراجعات) ويحرسها `test_parent.mjs` بمحوٍ واستعادةٍ ومقابلةٍ حقلاً حقلاً.
- **الصفحاتُ التعريفية تتبع الرحلة** (أمر المالك، ١٣ أغسطس ٢٠٢٦ — بعد الإطلاق): كلُّ جلسةٍ تزيد في المنهج أو تبدّل شاشةً **تُحدِّث `app/welcome/` في جلستها** — بطاقاتُ المنهج (`data-covers`/`data-count`) واللقطاتُ من مولّدها والأرقامُ المحسوبة وقراراتُ الأسس. وحارسُ التغطية يفرض البطاقاتِ والأعدادَ بنيوياً (يحمرّ يومَ تدخل محطةٌ بلا بطاقتها)، **وما لا يفرضه الحارس** (نصُّ قرارٍ جديد في «الأسس»، لقطةٌ تمثّل مدىً جديداً) **مسؤوليةُ الجلسة نصّاً** — فالتعريفيةُ وجهُ المنشور، ولا تتأخر عن التطبيق يوماً.
- **فحصٌ بقدر العمل، والكبرى عند الرفع والنشر** (أمر المالك، ١٣ أغسطس ٢٠٢٦ — بلاغُ العائلة `guard-scope-rule`؛ **ونُفِّذ اشتقاقُه في الجلسة ع١**): ثلاثُ طبقات — **السريعةُ في كل جلسة دائماً** (`node tools/guards.mjs` بلا متصفح، ~٩ث) · **وبطاريةُ المتصفح بقدر العمل** (`--scope map|screens|audio` — نطاقُ الصوت ٢٩ث بدل ٩ دقائق للكاملة) · **والكاملةُ قبل الدفع إلى المنشور**. **والتقليمُ يُجرَد ولا يُكتب بيد**: كلُّ حارسٍ يعلن مادّتَه في ترويسته (`يحرس: …`، ولسَوقة المتصفّح `يحرس نطاق …` و`يحرس جولة …`)، و`guards.mjs --touched` يشتقّ من فارق `origin/main` مَن يُشغَّل **ويسمّي كلَّ متخطّىً بعلّته** — والافتراضُ الكلُّ، **والشكُّ يُوسِّع** (تعذُّرُ المقابلة يُلغي التقليم)، **وحارسٌ لُمس ملفُّه يُشغَّل**. مُجرَّبٌ بسجلٍّ مصنوع في `guards.mjs --self-test`، وبابُ «لا حارسَ بلا سطر مادّة» في `test_selftests`. **ويبقى الشرطُ الأعلى: لا يُدفَع أحمر** — تقليمُ ما يُشغَّل أثناء العمل شيء، وتخفيفُ بوابة النشر ممنوع.
- **حرّاسٌ نائمون لا مُطفأة**: ما يفترض محتوىً غير موجودٍ يجرد شرطَه بنفسه ويطبع `⏸ نائم، يستيقظ ذاتياً`، ويعمل من تلقائه يومَ يوجد ما يقيسه (`SEED.md §٥`). فلا تُطفئ فحصاً بيد، ولا تحذفه ليمرّ.
- **منقولاتٌ ميدانية من اقرأ** (أمر المالك، ١١ أغسطس ٢٠٢٦): إصلاحُ تكدّس الأصوات والزوم (بلاغا طفلةٍ حقيقية، `read@9220ab1` — الجلسة م٢ ☑) وبابُ التثبيت (`read@260bf48`، جرّبه المالك وأقرّه — ضمن الجلسة ٩). مرجعُها شيفرة اقرأ الحية تُقرأ ولا تُمَسّ، وتُقيَّد في `SEED.md` بالبصمات عند النقل. **وقاعدةُ النقل** (مراجعة م٢): **يُقابَل الالتزامُ المبصوم (`git show`) لا شجرةُ العمل** — فالمرجع الحيّ فيه جلساتٌ تعمل وشجرتُه قد لا تستقرّ، والبصمةُ وحدَها هي المرجع.

## قرارات المالك المعتمدة (١١ أغسطس ٢٠٢٦)
- مستودعٌ مستقل (لا مونوريبو)، والبذرة تُنسخ من اقرأ وتُقيَّد في `docs/SEED.md`.
- التسمية ثلاثُ طبقات (تقنية إنجليزية · علامة عربية مشكولة · لاتينية للعالمية).
- **الأرقام المشرقية (١٢٣) لغةَ التعليم** + محطة تعارفٍ على المغربية آخر المستوى (ق١).
- **العدُّ المجرد فقط** (ق٢): «واحد، اثنان…» موحّداً، ولا يُنطق معدودٌ مقروناً بعددٍ أبداً — السبب المعلَن في `METHOD.md §٨`.
- **مدى المستوى ١ حتى ٢٠** (ق٣) — المئةُ والمنازل لـ«اِحْسِبْ ٢».
- جدولُ المراحل الثماني والبوابات الثلاث معتمدٌ كما في `METHOD.md §٣`.

## قواعد ثابتة (عهود العائلة — لا تفاوض)
- فاحصُ محتوىً آليّ: لا تمرينَ يستعمل عدداً/عمليةً فوق جبهة الطفل (`check_range.py`).
- مسارٌ واحد متصل بمحطات مقفلة · بواباتُ إتقانٍ بلا رسوب · ليتنر (المفهوم×المدى×نوع التمرين).
- لا مؤقّت ضغطٍ ولا شاشة «خطأ» ولا عقاب.
- خصوصية مطلقة: لا حساب ولا تتبّع ولا سحابة؛ التقدّم في localStorage؛ PWA يعمل دون إنترنت.
- **همزةُ الوصل لا تُشكَّل في النصوص المنطوقة** (قرار المالك، ١٣ أغسطس ٢٠٢٦): «الْمِسْ» لا «اِلْمِسْ» — رسماً محقَّقاً وأذناً سليمة، يحرسه بابٌ في `check_speech` بلا إعفاء، والمعروضُ يتبع المنطوق. **والألفُ العاريةُ تُقرأ بحركتها الأصلية**: كسراً في «الْمِسْ» و«اجْعَلْ»، وضمّاً في «انْظُرْ» — لا تُكتَب وتُنطَق.
- الصوت مولَّد مسبقاً (سُلافات) ومراجَعٌ بالأذن ومبصومُ المحتوى — عبر `AUDIO_QUEUE.md`، وجلساتُ التطوير لا تولّد صوتاً. **وعدّةُ الصوت خمسٌ** (`generate_audio` · `audio_panel` · `queue_texts` · `trim_lead` · `test_audio_cache`)، **ونموذجٌ واحد لا ثلاثة**، **ومصدرٌ واحد للنصوص: القائمة**، **والفئةُ تُشتقّ من موضع النصّ ولا تُخمَّن**.
- الكمياتُ تُرسم برمجياً والعددُ المرسوم يُثبَت آلياً (حارس المصيّر).
- واجهة RTL بالكامل، أزرار ≥ ٦٤ بكسل، أرقامُ المشرق في الواجهة.
- **نظافةُ الحظيرة** (بلاغ العائلة `2026-08-12-stale-headless-chrome.md`): فحصٌ يترك متصفحاً خلفه عيبٌ كفحصٍ يترك منفذاً مقبوضاً — كلُّ عدّةٍ تُطلق كروم **تكنس يتائمَها عند الإقلاع** (`sweep_stale` في `browser_test.py`): نسخُها هي بسوابقها (`ihsib-*`) **ووالدُها ميّت** (`ppid == 1`) وحدَها — ولا تُقرَب نسخُ الجيران ولا متصفحُ المالك ولا جولةٌ حيّة. وكلُّ `user-data-dir` جديد يُسمّى بسابقة `ihsib-` ليكون كنسُه آمناً بالبناء.

## بريد العائلة — لجلسة الإدارة وحدها (تصحيح المالك، ١١ أغسطس ٢٠٢٦)
- البريدُ شأنُ **جلسة الإدارة (فابل) وحدَها** — جلساتُ التنفيذ لا تقرؤه ولا تكتب فيه: حدودُ بند جلستها تكفيها، وما يعنيها من بلاغٍ يُدخله المديرُ في بند جلستها.
- إن كنتَ جلسةَ الإدارة: في **أول كل جلسة** اقرأ ما استجدّ في `../family/bulletins/` (عقدُ البريد في `../family/README.md`)، وقيّد إيصالَ كل بلاغٍ في `docs/FAMILY_MAIL.md` (اسم الملف · قُرئ بتاريخ · ما فُعل به). **بلاغُ إجراءٍ لا يُغلق إلا بإيصاله.**
- وبلاغُك للعائلة يُكتب ملفاً هناك ويُلتزم — لا يُنقل عبر المالك.
