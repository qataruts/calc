# مشروع «اِحْسِبْ» — الحسّ العددي المبكر لطفل ٤–٧

تطبيق ويب (دون أدوات بناء، HTML/CSS/JS خالص بوحدات ES) يعلّم الحسَّ العددي والعمليات الأساسية على **مسارات التعلّم (Clements & Sarama)** — ثاني عائلة تطبيقات التأسيس بعد «اِقْرَأْ»، ومسارُه (ب — حساب) مستقلٌّ يُبنى بالتوازي.

**الهوية**: العلامة **«اِحْسِبْ»** (Marhey) · تقنياً `calc` · النطاق `calc.mishkat.qa` · لاتينياً `Calc`.

## طريقة العمل — اقرأ هذا أولاً
- **مدير المشروع**: جلسة فابل ٥ خاصة بهذا التطبيق — تضع المنهج والخطة وتراجع المنتجات. **جلسات التنفيذ**: أوبوس ٥، كلُّ جلسةٍ ببند حدودٍ ومعيار قبول في `docs/SESSIONS.md`.
- **الوثيقتان المؤسِّستان**: `docs/FAMILY.md` (خارطة العائلة ومبادئها التسعة وبذرة المنصة) ثم `docs/PLAN.md` (خطة احسب المفصّلة: المنهج، الحرّاس، الجلسات، القرارات). **اقرأهما قبل أي عمل.**
- **المرجع الحيّ**: تطبيق اقرأ في مجلد `read` بهذا الـworkspace (يُقرأ ولا يُمَسّ) — `read/CLAUDE.md` يشرح الثقافة: حرّاسٌ يحرسون الغياب، أرقامٌ محسوبة لا مكتوبة، بلاغُ الميدان سيدُ الأدلة، فصلُ الصوت عن التطوير.
- **الحال الآن**: **`docs/METHOD.md` هو المرجع الملزم** و`docs/SESSIONS.md` تحمل بنود الجلسات ٠–١٠ وص بمعايير قبولها. **الجلسات ٠–٣ أُنجزت** (١١ أغسطس ٢٠٢٦): بذرةُ المنصة منسوخةٌ من `read@ea07f350` ومجرَّدة (الجردُ في **`docs/SEED.md`**)؛ و**الرحلةُ مكتوبة**: المراحل الثماني (٤٩ محطة) والبوابات الثلاث في `app/js/curriculum.js`، كلُّ محطةٍ تعلن جبهتَها ومفاتيحَ ليتنر، والخريطةُ تعرض ٥٢ عقدةً بالقفل التسلسلي، و`tools/check_range.py` يحرس الجبهة مُجرَّباً سالباً؛ و**الكمياتُ تُرسم**: `app/js/render.js` بستّة أنماط، وحارساه `tools/check_render.mjs` (هندسةً في node) و`browser_test.html` (رسماً على الشاشة)؛ و**المرحلتان ١–٢ تُلعبان**: `station.js` (حلقةُ المحطة الأربعية ومعالجةُ الخطأ وطبقةُ اللمس) و`quantity.js` و`counting.js` بخمس شاشات. **وأُنجزت الدفعةُ الصوتية الأولى** (الجلسة ص): الخمسةُ والعشرون نصّاً مُصرَّفةٌ بصوت **سُلافات** في `app/audio/` ببصمات محتوى، والقائمةُ فارغة، واحتياطُ النطق الآليّ لم يعد يعمل لشيء — **والمراجعةُ بالأذن معلَّقةٌ على المالك** (`tools/audio_panel.py --serve`). **التنفيذ عند الجلسة ٤** (المرحلتان ٣–٤: الرموز والمقارنة وليتنر بمفاتيحه)، **وميدانُ المالك على المرحلتين ١–٢ صار ممكناً** بعد سماع الدفعة. إن كنتَ جلسة تنفيذ: اقرأ `METHOD.md` ثم `SEED.md` ثم بند جلستك في `SESSIONS.md`، ونفّذ حدودها فقط، وحدّث حالتها عند الإتمام.
- **الوصلة التي تتعلّق بها الوحدات**: الرحلةُ تُقرأ من `sections()` في `app/js/curriculum.js` (وحدَه)، والقسمُ يصف نفسَه للخريطة، و**الشاشةُ تسجّل نفسَها** بـ`registerScreen` من **`registry.js`** (لا من `main.js` — دَورُ الاستيراد وعقدُ المولّد النقيّ، `SEED.md §٢`)، و**التمرينُ يسجّل نفسَه** بـ`registerExercise` في `station.js` الذي يحقن المراجعةَ مرّةً واحدة — فمحطةٌ جديدة تدخل الرحلة بلا سطرٍ يُعدَّل خارج بيانات المنهج، ووحدةُ تمارينٍ جديدة سطرُ استيرادٍ واحد في `main.js` لتصلها الشجرة. **والكميةُ تُرسم من `render.js` وحدَه**: `plan()` هندسةٌ خالصة بلا DOM، و`paint()` يبني الشكل **ويَعُدّ ما رسم من الـDOM لا من العدد المطلوب** — فلا تكتب شاشةٌ كميةً بيدها ولا تدّعي عدداً، **وجوابُ التمرين يُقرأ من `drawn` لا من العدد المطلوب**.
- **عهودُ شاشات التمارين** (الجلسة ٣، `DESIGN.md §٦`): حلقةُ المحطة أربعُ خطوات و«وحدك» وحدَها مقيسةٌ في ليتنر · **لا رمزَ عدديّ على شاشات المرحلتين ١–٢** (الخطواتُ أسماؤها والجولاتُ خرزات والجوابُ بطاقةُ كمّ) · التناظرُ الفرديّ **امتناعٌ في البنية** (زرُّ العنصر المعدود يُعطَّل) · الخطأُ **يُعَدّ أمامه** ولا ينقل ولا يُلقَّن · الخطفُ يُعاد **بلا حدٍّ ولا احتساب** ولا عدّادَ يقيس زمناً · وكلُّ نصٍّ منطوقٍ **يُعلَن** في `SPOKEN` ويُصَفّ في القائمة.
- **حرّاسٌ نائمون لا مُطفأة**: ما يفترض محتوىً غير موجودٍ يجرد شرطَه بنفسه ويطبع `⏸ نائم، يستيقظ ذاتياً`، ويعمل من تلقائه يومَ يوجد ما يقيسه (`SEED.md §٥`). فلا تُطفئ فحصاً بيد، ولا تحذفه ليمرّ.

## قرارات المالك المعتمدة (١١ أغسطس ٢٠٢٦)
- مستودعٌ مستقل (لا مونوريبو)، والبذرة تُنسخ من اقرأ وتُقيَّد في `docs/SEED.md`.
- التسمية ثلاثُ طبقات (تقنية إنجليزية · علامة عربية مشكولة · لاتينية للعالمية).
- **الأرقام المشرقية (١٢٣) لغةَ التعليم** + محطة تعارفٍ على المغربية آخر المستوى (ق١).
- **العدُّ المجرد فقط** (ق٢): «واحد، اثنان…» موحّداً، ولا يُنطق معدودٌ مقروناً بعددٍ أبداً — السبب المعلَن في `METHOD.md §٨`.
- **مدى المستوى ١ حتى ٢٠** (ق٣) — المئةُ والمنازل لـ«اِحْسِبْ ٢».
- جدولُ المراحل الثماني والبوابات الثلاث معتمدٌ كما في `METHOD.md §٣`.

## قواعد ثابتة (عهود العائلة — لا تفاوض)
- فاحصُ محتوىً آليّ: لا تمرينَ يستعمل عدداً/عمليةً فوق جبهة الطفل (`check_range.py`).
- مسارٌ واحد متصل بمحطات مقفلة · بواباتُ إتقانٍ بلا رسوب · ليتنر (المفهوم×المدى×نوع التمرين).
- لا مؤقّت ضغطٍ ولا شاشة «خطأ» ولا عقاب.
- خصوصية مطلقة: لا حساب ولا تتبّع ولا سحابة؛ التقدّم في localStorage؛ PWA يعمل دون إنترنت.
- الصوت مولَّد مسبقاً (سُلافات) ومراجَعٌ بالأذن ومبصومُ المحتوى — عبر `AUDIO_QUEUE.md`، وجلساتُ التطوير لا تولّد صوتاً. **وعدّةُ الصوت خمسٌ** (`generate_audio` · `audio_panel` · `queue_texts` · `trim_lead` · `test_audio_cache`)، **ونموذجٌ واحد لا ثلاثة**، **ومصدرٌ واحد للنصوص: القائمة**، **والفئةُ تُشتقّ من موضع النصّ ولا تُخمَّن**.
- الكمياتُ تُرسم برمجياً والعددُ المرسوم يُثبَت آلياً (حارس المصيّر).
- واجهة RTL بالكامل، أزرار ≥ ٦٤ بكسل، أرقامُ المشرق في الواجهة.
