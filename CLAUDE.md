# مشروع «اِحْسِبْ» — الحسّ العددي المبكر لطفل ٤–٧

تطبيق ويب (دون أدوات بناء، HTML/CSS/JS خالص بوحدات ES) يعلّم الحسَّ العددي والعمليات الأساسية على **مسارات التعلّم (Clements & Sarama)** — ثاني عائلة تطبيقات التأسيس بعد «اِقْرَأْ»، ومسارُه (ب — حساب) مستقلٌّ يُبنى بالتوازي.

**الهوية**: العلامة **«اِحْسِبْ»** (خطُّها يُختار في الجلسة هـ — حكمُ المالك ١٢ أغسطس: ميثاقُ الهوية يسود وMarhey لاقرأ وحدها، `docs/REVIEW_IDENTITY.md`) · تقنياً `calc` · النطاق `calc.mishkat.qa` · لاتينياً `Calc`.

## طريقة العمل — اقرأ هذا أولاً
- **مدير المشروع**: جلسة فابل ٥ خاصة بهذا التطبيق — تضع المنهج والخطة وتراجع المنتجات. **جلسات التنفيذ**: أوبوس ٥، كلُّ جلسةٍ ببند حدودٍ ومعيار قبول في `docs/SESSIONS.md`.
- **الوثيقتان المؤسِّستان**: `docs/FAMILY.md` (خارطة العائلة ومبادئها التسعة وبذرة المنصة) ثم `docs/PLAN.md` (خطة احسب المفصّلة: المنهج، الحرّاس، الجلسات، القرارات). **اقرأهما قبل أي عمل.**
- **المرجع الحيّ**: تطبيق اقرأ في مجلد `read` بهذا الـworkspace (يُقرأ ولا يُمَسّ) — `read/CLAUDE.md` يشرح الثقافة: حرّاسٌ يحرسون الغياب، أرقامٌ محسوبة لا مكتوبة، بلاغُ الميدان سيدُ الأدلة، فصلُ الصوت عن التطوير.
- **الحال الآن**: **`docs/METHOD.md` هو المرجع الملزم** و`docs/SESSIONS.md` تحمل بنود الجلسات ٠–١٠ وص بمعايير قبولها. **الجلسات ٠–٣ أُنجزت** (١١ أغسطس ٢٠٢٦): بذرةُ المنصة منسوخةٌ من `read@ea07f350` ومجرَّدة (الجردُ في **`docs/SEED.md`**)؛ و**الرحلةُ مكتوبة**: المراحل الثماني (٤٩ محطة) والبوابات الثلاث في `app/js/curriculum.js`، كلُّ محطةٍ تعلن جبهتَها ومفاتيحَ ليتنر، والخريطةُ تعرض ٥٢ عقدةً بالقفل التسلسلي، و`tools/check_range.py` يحرس الجبهة مُجرَّباً سالباً؛ و**الكمياتُ تُرسم**: `app/js/render.js` بستّة أنماط، وحارساه `tools/check_render.mjs` (هندسةً في node) و`browser_test.html` (رسماً على الشاشة)؛ و**المرحلتان ١–٢ تُلعبان**: `station.js` (حلقةُ المحطة الأربعية ومعالجةُ الخطأ وطبقةُ اللمس) و`quantity.js` و`counting.js` بخمس شاشات. **وأُنجزت الدفعتان الصوتيتان** (الجلسة ص): **٣٨ نصّاً مُصرَّفةٌ** بصوت **سُلافات** في `app/audio/` ببصمات محتوى (٢٥ للمرحلتين ١–٢ **أقرّتها أذنُ المالك**، و١٣ للمرحلتين ٣–٤ **تنتظر أذنَه** — `tools/audio_panel.py --serve`)، **والقائمةُ فارغة**، واحتياطُ النطق الآليّ لم يعد يعمل لشيء والفحصُ صارمٌ مُجرَّبٌ سالباً. **والفئةُ تتبع موضعَ النصّ**: جملُ الكشف الثلاث نُقلت إلى `SAY` فصارت نمذجةً، وبابٌ خامسٌ في `check_speech` يحكم و`queue_texts --retag` يُصلح. **وأُنجزت الجلسة ٤**: **المرحلتان ٣–٤ تُلعبان** — `numerals.js` (حلقةُ تقديم الرمز: كميةٌ تُعَدّ ← يُكشَف رمزُها فوقها ← مطابقةٌ ذهاباً وإياباً، والصفرُ آخراً بإطارٍ فارغ) و`compare.js` (`compare` · `line` · `order` · `neighbor`)، **والمصيِّر صار ثلاثةَ أصنافٍ** (كمّيةٌ تُعَدّ · رمزٌ يُقرأ نصُّه · خطٌّ تُعَدّ علاماتُه) ولكلٍّ قارئُ مرسومه وحارسُه، **والقياسُ حيٌّ في المراحل ١–٤ كلِّها** (١١ نوعَ تمرين، ١٠ أنواع شاشات) **والمراجعةُ اليومية تعمل من سجلٍّ حقيقيّ** (٦ تمارين، الأضعف أولاً). و١٣ نصّاً منطوقاً جديداً **صُرِّفت في الدفعة الثانية** (أعلاه). **وفُتح الميدان فجاء البلاغ ١** (`docs/FIELD.md`: صوتٌ متراكبٌ مقطوش، ولا انتظارَ للكلام) **فنُفِّذت جلسةُ الإصلاح م١**: **للصوت قناةٌ واحدة بطابور** — `play()` تُرجِع وعداً يتمّ بتمام الكلام و`stop()` يُفرغ الطابور، **والانتقالُ بعد تمام الجملة** لا بمهلةٍ ثابتة (و`BEAT` باقٍ فاصلاً بين الجمل)، **وحارسا التعاقب** يقيسان ما سمعته الأذن لا عددَ التشغيلات: بابٌ سادس في `check_speech` (لا صوتَ خارج القناة، ونداءان في لحظةٍ يتتابعان) وخمسةُ أحكامٍ زمنية في `browser_test` — **مُجرَّبةً سالباً** بردّ الشيفرة القديمة. **والبلاغ ١ لا يُغلَق إلا ببلاغٍ معادٍ من المالك** (وقشرةُ `sw` رُفعت إلى `v6` كي يجرّب الشيفرةَ الجديدة لا المخزونة). **وأُنجزت الجلسة ٥**: **المرحلة ٥ تُلعَب** — `bonds.js` بتمرين «اِصْنَعِ العَدَد» (`METHOD.md §٧`) بوجهيه: جزآن يُوضَعان في خانتَي الكلّ (و«بطريقةٍ أخرى» في ٥·٣)، و«ما بقي للعشرة» حيث **خاناتُ الإطار الفارغة هي السؤال** — **ولا رمزَ عمليةٍ واحد** يفرضه الفاحصُ وحارسُ المتصفّح معاً. **والكمّيةُ تنشقّ شقّين في المصيِّر لا في الشاشة** (`split` — الشقُّ يتبع ترتيبَ الرسم فيوافق ما يُعَدّ، وبابُه في `check_render`). **وبوابةُ الكمّ تعمل** — وقد كانت لا تُرسَم أصلاً (`renderSession` تطلب مُصيِّرَ التمرين ونسيته البوابة) — وحارسُها `test_gate.mjs` يسألها **بسجلٍّ مصنوع** فيُثبت أنّها من **أضعف** المهارات لا من المستحقّ. **وسِجلُّ التمارين صار نوعاً ← قائمةَ مالكين** (`equal|5|make` و`bond|10|make` اسمُ نوعهما واحد، فكان أحدُهما يمحو الآخر صامتاً). **والسَّوقةُ تُشغَّل بأمرٍ واحد**: `node tools/guards.mjs --browser` يجرد الحرّاسَ **بالنمط** (`test_*.mjs` · `check_*.py` · `check_*.mjs`) ويشغّلهم ويفشل بفشل أيٍّ منهم باسمه، ويسمّي سَوقةَ المتصفّح أُشغِّلت أو تُركت. **وأُنجزت الجلسة ٦**: **المرحلة ٦ تُلعَب** — `ops.js` بخمسة أنواع شاشات (`add` · `sub` · `diff` · `zero` · `fluent`) وثلاثةِ أوجهِ رسم، **و«الجمعُ تركيبُ معلومٍ لا طقسٌ رمزيّ» على ثلاث درجات**: ٦·١ **بلا علامةٍ البتّة** (كميتان تلتقيان فتُعَدّ الحصيلة — ولوحُها **صورةُ الجسر مقلوبةً**: مجموعتان فوق وحصيلتُهما تحتهما، **ولونُ كلِّ مجموعةٍ لونُ شقّها** من المصيِّر)، ثم **الجملةُ وكمّيتُها معاً أوّلَ لقاء**، ثم **الرمزُ وحدَه والكمّيةُ عوناً عند النقر**. **ورمزُ العملية من جبهة المحطة حصراً** (`signFor`) يحرسه `check_range` جولةً جولة **مُجرَّباً سالباً** وحارسُ المتصفّح بجرد النصّ المعروض. **والجوابُ من المرسوم** (مجموعُ الجمع `drawn` · وباقي الطرح `split` · والفرقُ فرقُ الصفّين)، **ولكلِّ عمليةٍ عدُّ درسِها** عند الخطأ (تصاعديٌّ من الأكبر · تنازليٌّ مع كلِّ مغادر · عدُّ الفائض) بـ`countMarks` **واحدة**. **وبوابةُ العمليات من مدَاها المعلَن** (`from` في `curriculum.js` و`gateSkills` — فلا يتصدّر جلستَها ضعفٌ في التقدير الفوريّ) وحارسُها بسجلٍّ مصنوع **أضعفُ ما فيه خارج المدى**، مُجرَّباً سالباً. **والتنفيذ عند الجلسة ٧** (المرحلتان ٧–٨ وبوابة ٣)، **وميدانُ المالك على المرحلتين ١–٢ دَينٌ مفتوح لا يتأخر عن بوابة قبول الجلسة ٦**. إن كنتَ جلسة تنفيذ: اقرأ `METHOD.md` ثم `SEED.md` ثم بند جلستك في `SESSIONS.md`، ونفّذ حدودها فقط، وحدّث حالتها عند الإتمام.
- **الوصلة التي تتعلّق بها الوحدات**: الرحلةُ تُقرأ من `sections()` في `app/js/curriculum.js` (وحدَه)، والقسمُ يصف نفسَه للخريطة، و**الشاشةُ تسجّل نفسَها** بـ`registerScreen` من **`registry.js`** (لا من `main.js` — دَورُ الاستيراد وعقدُ المولّد النقيّ، `SEED.md §٢`)، و**التمرينُ يسجّل نفسَه** بـ`registerExercise` في `station.js` الذي يحقن المراجعةَ مرّةً واحدة (والسجلُّ **نوعٌ ← قائمةُ مالكين**: نوعُ التمرين قد يشترك فيه مفهومان — `equal|5|make` و`bond|10|make` — فيُسأل كلُّ مالكٍ بدوره ويجيب صاحبُها، والجوابُ يحمل مالكَه فيرسمه مُصيِّرُه هو) — فمحطةٌ جديدة تدخل الرحلة بلا سطرٍ يُعدَّل خارج بيانات المنهج، ووحدةُ تمارينٍ جديدة سطرُ استيرادٍ واحد في `main.js` لتصلها الشجرة. **والكميةُ تُرسم من `render.js` وحدَه**: `plan()` هندسةٌ خالصة بلا DOM، و`paint()` يبني الشكل **ويقرأ ما رسم من الـDOM لا من العدد المطلوب** — ولكلِّ صنفِ رسّامٍ قارئُه (الكمُّ تُعَدّ عناصرُه · الرمزُ يُقرأ نصُّه عدداً · الخطُّ تُعَدّ علاماتُه فيُرَدّ مداه) — فلا تكتب شاشةٌ كميةً ولا رقماً بيدها ولا تدّعي عدداً، **وجوابُ التمرين يُقرأ من `drawn` لا من العدد المطلوب**.
- **عهودُ شاشات التمارين** (الجلسات ٣–٥، `DESIGN.md §٦`): حلقةُ المحطة أربعُ خطوات و«وحدك» وحدَها مقيسةٌ في ليتنر · **الرمزُ لا يُعرَض إلا بعد عدّ كمّه ويُكشَف فوقها**، والمطابقةُ ذهاباً وإياباً في المحطة الواحدة · **مفتاحُ ليتنر لكلِّ رمزٍ على حدة** مقروءاً من `skills` في المنهج · **لا رمزَ عدديّ على شاشات المرحلتين ١–٢** (الخطواتُ أسماؤها والجولاتُ خرزات والجوابُ بطاقةُ كمّ) · التناظرُ الفرديّ **امتناعٌ في البنية** (زرُّ العنصر المعدود يُعطَّل) · الخطأُ **يُعَدّ أمامه** ولا ينقل ولا يُلقَّن · الخطفُ يُعاد **بلا حدٍّ ولا احتساب** ولا عدّادَ يقيس زمناً · وكلُّ نصٍّ منطوقٍ **يُعلَن** في `SPOKEN` ويُصَفّ في القائمة · **والصوتُ قناةٌ واحدة** (م١): لا عنصرَ صوتٍ خارج `audio.js`، و`say` تُرجِع وعداً يتمّ بتمام الكلام، **وكلُّ انتقالٍ ينتظره** (`praiseThen` للصواب)، والإسكاتُ يُفرغ الطابور. · **والمرحلةُ ٥ بلا رمز عمليةٍ البتّة** (يفرضه الفاحصُ وحارسُ المتصفّح): الكلُّ يُسمّى رمزاً و**الأجزاءُ تُبنى كمّاً**، و**الشقُّ من المصيِّر** (`split`) لا من الشاشة، والتجاوزُ **امتناعٌ في البنية** (بطاقةٌ تزيد على ما بقي تُعطَّل) وما وُضع **يُرَدّ بلمسة**. · **والمرحلةُ ٦ عمليةٌ تُفعَل قبل أن تُسمّى**: **رمزُ العملية من جبهة المحطة حصراً** (لا تكتبه شاشةٌ بيدها، وجولةٌ تعلن `ops` و`signs` فيقابلهما الفاحص)، و**كلُّ حقيقةٍ تُعرَض كمّيةً ورمزاً معاً أوّلَ لقاء** ثم بالرمز وحدَه **والكمّيةُ عوناً عند النقر** بلا حدٍّ ولا احتساب، و**الجوابُ من المرسوم** في الأوجه الثلاثة، و**الخطأُ يُعَدّ بعدّ درسِه** لا بعدٍّ واحدٍ للجميع. · **ولكلِّ بوابةٍ مدَاها المعلَن** (`from` في المنهج): الأولى ١–٤ · **والعمليات ٥–٦** · والثالثة الرحلةُ كلُّها — والتنويعُ وحدَه من الحصيلة كلِّها.
- **حرّاسٌ نائمون لا مُطفأة**: ما يفترض محتوىً غير موجودٍ يجرد شرطَه بنفسه ويطبع `⏸ نائم، يستيقظ ذاتياً`، ويعمل من تلقائه يومَ يوجد ما يقيسه (`SEED.md §٥`). فلا تُطفئ فحصاً بيد، ولا تحذفه ليمرّ.
- **منقولاتٌ ميدانية من اقرأ** (أمر المالك، ١١ أغسطس ٢٠٢٦): إصلاحُ تكدّس الأصوات والزوم (بلاغا طفلةٍ حقيقية، `read@9220ab1` — الجلسة م٢ ☑) وبابُ التثبيت (`read@260bf48`، جرّبه المالك وأقرّه — ضمن الجلسة ٩). مرجعُها شيفرة اقرأ الحية تُقرأ ولا تُمَسّ، وتُقيَّد في `SEED.md` بالبصمات عند النقل. **وقاعدةُ النقل** (مراجعة م٢): **يُقابَل الالتزامُ المبصوم (`git show`) لا شجرةُ العمل** — فالمرجع الحيّ فيه جلساتٌ تعمل وشجرتُه قد لا تستقرّ، والبصمةُ وحدَها هي المرجع.

## قرارات المالك المعتمدة (١١ أغسطس ٢٠٢٦)
- مستودعٌ مستقل (لا مونوريبو)، والبذرة تُنسخ من اقرأ وتُقيَّد في `docs/SEED.md`.
- التسمية ثلاثُ طبقات (تقنية إنجليزية · علامة عربية مشكولة · لاتينية للعالمية).
- **الأرقام المشرقية (١٢٣) لغةَ التعليم** + محطة تعارفٍ على المغربية آخر المستوى (ق١).
- **العدُّ المجرد فقط** (ق٢): «واحد، اثنان…» موحّداً، ولا يُنطق معدودٌ مقروناً بعددٍ أبداً — السبب المعلَن في `METHOD.md §٨`.
- **مدى المستوى ١ حتى ٢٠** (ق٣) — المئةُ والمنازل لـ«اِحْسِبْ ٢».
- جدولُ المراحل الثماني والبوابات الثلاث معتمدٌ كما في `METHOD.md §٣`.

## قواعد ثابتة (عهود العائلة — لا تفاوض)
- فاحصُ محتوىً آليّ: لا تمرينَ يستعمل عدداً/عمليةً فوق جبهة الطفل (`check_range.py`).
- مسارٌ واحد متصل بمحطات مقفلة · بواباتُ إتقانٍ بلا رسوب · ليتنر (المفهوم×المدى×نوع التمرين).
- لا مؤقّت ضغطٍ ولا شاشة «خطأ» ولا عقاب.
- خصوصية مطلقة: لا حساب ولا تتبّع ولا سحابة؛ التقدّم في localStorage؛ PWA يعمل دون إنترنت.
- الصوت مولَّد مسبقاً (سُلافات) ومراجَعٌ بالأذن ومبصومُ المحتوى — عبر `AUDIO_QUEUE.md`، وجلساتُ التطوير لا تولّد صوتاً. **وعدّةُ الصوت خمسٌ** (`generate_audio` · `audio_panel` · `queue_texts` · `trim_lead` · `test_audio_cache`)، **ونموذجٌ واحد لا ثلاثة**، **ومصدرٌ واحد للنصوص: القائمة**، **والفئةُ تُشتقّ من موضع النصّ ولا تُخمَّن**.
- الكمياتُ تُرسم برمجياً والعددُ المرسوم يُثبَت آلياً (حارس المصيّر).
- واجهة RTL بالكامل، أزرار ≥ ٦٤ بكسل، أرقامُ المشرق في الواجهة.
